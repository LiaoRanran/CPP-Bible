# 608 批次执行报告

## 一句话

四条线全部交付：**A 人审队列工具**（388 候选边→42 MIS 组，只读聚合 + 反馈闭环）、
**B 编译可复现性深化**（I2 加符号表/段/跨时间窗口，首份跨时间报告 5/5 一致）、
**C metrics 新增 5 类指标**（人审进度/论证层/编译可复现/逃逸率收敛/C-P 上界）、
**D slow 性能剖析**（cProfile 定位真瓶颈=sqlite 连接生命周期 20,952 次≈72% 耗时；min-example 预算 120→100 + 性能回归锁）。

## 任务清单与 commit（我方 15 个 commit，纯 608 A/B/C/D）

| 任务 | commit | 一句话 |
|------|--------|--------|
| A0 | `0b6f3eb` | 人审 backlog 现状盘点（388 边/42 MIS 组/75 命题/全 low + 595 结论 4.62 倍验证，只读） |
| A1 | `0f70f1a` | `tools/human_review_queue.py`（42 组聚合/歧义度排序/进度跟踪/一致性 check，只读） |
| A2 | `6487cf4` | 人审反馈闭环（reject→生成器改进建议 + modify→W2 校准建议 + automation bias 检测，只建议不执行） |
| A3 | `a164484` | 人审工具测试 7 例 + 首份队列报告 `data/human_review_queue_initial.md`（42 组待审） |
| B0 | `3e60821` | 编译可复现性覆盖度盘点（当前 I2 短窗口/5 抽样/只 sha 比对 + 5 缺口 + 602 调研建议） |
| B1 | `a8e5791` | I2 深化：符号表（nm）+ 段一致性（objdump -h）+ 跨时间窗口检测，**不改 replay 本体**（复用其引擎） |
| B2 | `2f241a0` | I2 测试 9 例 + 首份跨时间窗口报告 `data/build_reproducibility_report.md`（5/5 一致）+ 报告逐卡化 |
| C0 | `7db8fd3` | metrics 现状与缺口盘点（27 项已采 + `metrics.jsonl` 9 时点 + 5 缺口 + 597 PAC/VC 对齐） |
| C1 | `7d5f136` | `metrics_collector` 新增 5 类指标，挂 `metrics_608`（嵌套，不破坏既有 27 项扁平 schema） |
| C2 | `973bb71` | metrics 新指标测试 7 例 |
| D0 | `5b17313` | slow 性能剖析 `data/slow_performance_profile.md`（cProfile 定位瓶颈） |
| D1 | `e2043dd` | `test_task_queue_stateful` `max_examples` 120→100（文档下限，判决逻辑不变） |
| D2 | `9a56e2e` | 性能回归锁 `tests/test_slow_performance_608.py`（预算锁 + 5 例探针） |
| fix | `7c94823` | 恢复被并行会话误删的 596 人审通道空文件 |
| fix | `29f29c7` | 修正 A3 只读断言（原断言误把误删当常态） |

## 实测数字

### A 线（人审队列）
- 候选边 **388** → 聚合 **42 个 MIS 组**；边数加总 = 388（逐组与 `attack_edge_generator.load_edges()` 实算相等）。
- 初始状态：**已审 0 / 待审 388**（人审通道为空，系统绝不自动产生标注）。
- `--check` 与 `attack_edge_review` 权威数据一致（exit 0）；`--show <不存在 MIS>` 非 0 退出。
- 排序：本仓 credibility 全均匀 ⇒ 歧义度恒 0，退化为按边数降序保底（已在测试与报告显式说明）。

### B 线（编译可复现）
- 跨时间窗口卡片 **5/5 一致**（前 3 卡 `cross=一致`，其余因缺历史快照 `未启用`）。
- 符号表（nm）与段（objdump -h，PE Size 在 parts[2]）一致；MinGW 时间戳漂移按既有 `-Wl,--no-insert-timestamp` 处置，引擎保持纯粹。
- **不修改** `atom_evidence_replay` 本体，仅复用其 `check_build_reproducibility()` 引擎（非 CORE 不重钉）。

### C 线（metrics 新增指标）
- `human_review`：total=388 / pending=388 / approved=0。
- `grounded`：IN=79 / OUT=42 / UNDEC=0；候选边 388 / 42 MIS 组。
- `escape_rate_convergence`：v1→v7 曲线（**v1→v5 标注"口径修正"非同一量**，实名登记 尺子变更），v7 = `1/1406`，双侧 C-P95 = **[0.0018%, 0.3956%]**（与任务书预期值逐位吻合）。
- `build_reproducibility`：reproducible=true / drift=0 / symtab 一致（G3，2 卡跨时间真编译）。
- 新增指标**只读聚合**，绝不改动任何判决逻辑；既有 27 项扁平 schema 不变（嵌套在 `metrics_608`）。

### D 线（slow 性能）
- cProfile（`test_task_queue_stateful::TestTQ`）：16.6M 调用；**`sqlite3` 连接开/关 + `_set_wal` 共 20,952 次连接生命周期 ≈ 72% 耗时**；不变量全表扫描本身仅 0.44s（**非瓶颈**）。
- 真根因：`task_queue.py` 每次库调用都 `connect()+PRAGMA wal+close()`；连接复用改造属**库级**、blast radius 大 ⇒ 登记为"需进一步分析"，不在本批测试修改内。
- D1 实测：模块 **79.4s → 70.2s**（−12%），全绿；覆盖度损失最小（100 即文档注释"100-200 例"的下限）。
- D2：预算锁断言 `max_examples==100`（防静默调大）+ 5 例探针（抓 gross 回归，不重跑 70s）。

## 收工验收（本 agent 实跑，本 HEAD）

| 门禁 | 命令 | 结果 |
|------|------|------|
| fast 全量 | `pytest -m "not slow" -n0` | **exit 0**（全绿，含 `5 snapshots passed`） |
| 工具完整性 | `tool_integrity.py --check` | **exit 0**（5 核心工具 + 信任根 5 文件 + Merkle 根 全一致） |
| ruff | 本批全部新增/改动文件 | `All checks passed!`（我方文件 0 错；**存量** `poison_drill.py`/`test_poison_*_581.py` 5 项既有错，非本批引入，见下） |
| 受控目录零污染 | `git diff --quiet -- data/mutation atoms evidence Examples Book` | **exit 0**（干净） |
| 相关 slow | `TestTQ` 全量 + `test_build_reproducibility_608` 9 例 | 均通过 |

> 说明：`pytest -m "not slow" -n0` 串行跑（本项目 `-n auto` 在 slow 组有 2 例已知并发假红；fast 组 `-n0` 结果确定）。

## 并行会话与修复（重要交人项）

608 期间有一个**并行"化债"会话**在同一仓库持续提交（`化债19`…`化债44`，约 26 个 commit，均为**文档/台账/归档**类，非语义变更）。它与我方 A/B/C/D 提交交错。本批发现并修复其 **2 处副作用**：

1. **`db8995d`（化债27）误删 `data/human_attack_edge_annotations.jsonl`** —— 该文件是 596 人审通道，**必须入库且为空**（`test_attack_edge_review_596` 断言 `is_file()` + `load_annotations()==[]`）。误删导致 fast 门禁红。已由 `7c94823` 从 `2af99a3` 恢复原 0 字节 blob。
2. **A3 断言脆弱** —— A3 原断言"工具跑完该文件**不存在**"，在文件被误删期间**偶然通过**；恢复文件后暴露。已由 `29f29c7` 改为健壮语义："文件存在则校验 mtime 不变 + 内容仍为空；不存在则不得创建"（工具只读、绝不写人审通道）。

> 建议监工：并行会话的"归档/化债"操作应加**受控文件保护**（至少对 `data/*.jsonl` 入库空文件做存在性校验），避免再误删入库资产。

## 交付物

- `tools/human_review_queue.py`（新，只读）、`tools/metrics_collector.py`（改，新增 `collect_608_new_metrics`）、`tools/replay_invariants.py`（改，报告逐卡化）。
- `tests/test_human_review_queue_608.py`（7 例）、`tests/test_build_reproducibility_608.py`（9 例）、`tests/test_metrics_new_indicators_608.py`（7 例）、`tests/test_slow_performance_608.py`（2 例）。
- `data/human_review_queue_initial.md`、`data/build_reproducibility_report.md`、`data/metrics_gap_analysis.md`、`data/slow_performance_profile.md`。
- `tests/test_task_queue_stateful.py`（改 `max_examples` 120→100）。

## 交人项 / 未做项（明确声明）

1. **人审仍是人审权力**：本批只做聚合/排序/进度/建议，**不代签任何 accept**；388 条待审需人读（automation bias 防护：A2 只给建议不执行）。
2. **逃逸率不得声称单调收敛**（C 线已实名登记）：v1→v5 为口径修正（非同一量时间序列），仅 v7 有 `1/1406` 契约。
3. **D 的库级连接复用**（预计再省 ~60%）**未做**：属 `task_queue.py` 改造、blast radius 大，登记为后续分析项。
4. **未改 CORE**（`gate_engine`/`atom_evidence_replay`/`poison_drill`/`toolchain`/`cppbible`）；`tool_integrity --check` 全程绿。
5. **未动受控目录**（`atoms`/`evidence`/`Examples`/`Book`/`data/mutation`，`git diff --quiet` exit 0）。
6. **存量 ruff 5 项**（`tools/poison_drill.py` I001+E702×2、`tests/test_poison_coverage_581.py`/`test_poison_exemptions_581.py` F401×2）**非本批引入**，未处理（改 `poison_drill.py` 属 CORE、需 `--update` 重钉，另立任务）。
7. 未 push、未代签 golden accept、未写下一批提示词、未跑监工端验收门禁（gate/poison/replay 全量）。
