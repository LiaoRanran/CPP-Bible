# 607 批次执行报告

## 一句话
治理台账从「全量重签」升级为「增量机械登记」（`auto-update`：扩边扫描面 + 只追加 + 标记 missing），
并新建只读的命题活性锚审计工具（79 命题 / observation 50 全部缺锚 / needs_review 0）。

## 三个必答数字（任务书要求）

| 指标 | 数值 |
|------|------|
| 治理台账更新了多少条 | manifest **344 → 505**（607 净增 **161** 条：`_arch_*/**` **156** + `_auto/inbox/*.md` **4** + 根级 `PM_*.md` **1**）；另在收工前又续登记 1 条（见 D4），最终 505 |
| 命题活性锚缺多少条 | **50 条 / 27 卡**（全库 79 命题中 observation 50、inference 29；**50 条 observation 无一填过 `liveness`**） |
| needs_review 多少条 | **0**（`kind: external_basis` 的 observation 需先有人填过 liveness 才可能出现；全库 0 条填过 ⇒ 现为 0，且这是**结构性为 0**，不是"审计没查"） |

## 各任务 commit

| 任务 | commit | 一句话 |
|------|--------|--------|
| 任务0 | `79b61e3` | 两个债务现状盘点（`data/debt_inventory_607.md`，纯只读） |
| 任务1 | `ca23346` | 治理台账增量机械登记：`iter_governed_docs()` 单一扫描面 + `auto_update()` + CLI `auto-update`；manifest 344→504 |
| 任务2 | `41a2768` | `tools/proposition_liveness_audit.py`（只读审计 + `data/proposition_liveness_audit.md`） |
| 任务3 | `f53b247` | 两个回归锁测试（11 例）+ 收工门禁（含 606 遗留修复、台账续登记、基准重钉） |

## 实测数字

### 任务1 扫描面扩边（`iter_governed_docs()` 为 verify / auto-update 共用唯一真源）
| 位置 | 条数 |
|------|------|
| `References/architecture_架构演进/**` | 345（原 344 + 新落地 1 篇） |
| `_arch_*/**`（**仓库根**，非 References 下） | 156 |
| `_auto/inbox/*.md` | 4 |
| 根级 `PM_/PUSH_/INDEX_/MATRIX_/CHECKLIST_*.md` | 1 |
| **合计** | **505**（missing=0） |

- 扩边扫描面命中 **160** 条此前完全不在台账内的文档 —— 与任务0 盘点预测的 160 **逐条相等**。
- 幂等实测：无变更时连跑 `auto-update` 第二次输出「无变更（未写盘）」，manifest 字节不变、exit 0。
- `verify` 已改为跳过 `status == "missing"` 条目：消失的文档**保留**在台账里（历史 hash 不抹平）。
- `data/governance_docs_manifest.json` 在 supply_chain 的 pin 内 ⇒ 台账一变必须 `tool_integrity.py --update`（本批跑 2 次：台账 + conftest，均随 commit 带 `tools/.tool_checksums`）。

### 任务2 命题活性锚审计（与 gate 50 条 warn 交叉一致）
- 命题 79（observation 50 / inference 29）、涉及 **27 卡**；`liveness` 有 = **0**。
- observation 状态：`ok=0  missing=50  missing_symbol=0  unknown_kind=0  needs_review=0`。
- 两条独立路径互证：gate 规则 `OBSERVATION-LIVENESS` 的 **50** 条 warn ↔ 本工具 50 条 missing。
- 工具**不 import CORE**（纯标准库 + 自实现 frontmatter 子集）⇒ 与 gate 判据漂移时本报告**不会**自动跟着变（已在报告 §5 显式登记为"需人发现"的边界）。

### 任务3 测试
- `tests/test_governance_auto_update_607.py` **6 例**（发现新增/不删历史/标记 missing/verify exit 0/不刷新既有 hash/无写盘时仍如实计数）。
- `tests/test_proposition_liveness_audit_607.py` **5 例**（计数口径/异常分类/已有锚不误标/external_basis→needs_review/`--json` 合法且全程只读指纹不变）。
- 两文件均用 `tmp_path` 假仓/假库 + `monkeypatch` 注入，**不碰真实仓**。

## 偏差表

| # | 任务书假设 | 实测 | 处理 |
|---|-----------|------|------|
| D1 | verify「报错」是因为 `_arch_v12–v17`/PM 文档**未被登记** | 任务0 时点 `verify` **exit 0**；`_arch_*`（在**仓库根**）与根级 `PM_*` 从不属于扫描面 ⇒ 债务性质是**扫描面过窄**，不是"漏登记" | 债务定义按实测改写，登记在 `data/debt_inventory_607.md` 与任务1 注释里 |
| D2 | 清单「每条附命题 id 和 brief」 | 本仓命题卡**无 `brief` 字段**（对应 `statement`） | 取 `statement` 前 60 字，JSON 输出给全文，报告 §4 抬头显式说明 |
| D3 | `needs_review` 应能列出条目 | 全库无任何 `liveness` ⇒ 结构性 0 条 | 工具保留该分类（人填锚后即生效），报告中说明"0 的成因" |
| D4 | 台账「更新后 verify exit 0」暗示一次改完即可 | 607 期间**又有 1 篇文档落地**（`References/architecture_架构演进/PM_加工作密度规划_20260919.md`）⇒ verify 再次报未登记 | 收工前走一次 `auto-update`（净增 1 → 505），随任务3 commit 提交 —— 正是本批新增的机械通道 |
| D5 | 606 收工门禁应已全绿 | `pytest -m "not slow" -n auto` 在 607 收工时**红**：① `test_replay_invariants_605.py` 两处 `len(results) == 3` 在 606 给 `INVARIANTS` 加 `lock_consistency` 后**过期**；② 605/606 读真实仓可变状态（真实锁/工件指纹/metrics），`-n auto` 下被挂 `replay_serial` 的 **fast** 用例合法持锁 ⇒ `test_lock_no_stale_in_real_repo` 把"别人正持锁"误判成"锁泄漏"（实测：`-n auto` 红 / `-n0` 绿） | ① 计数断言改为按名字比对 `set(ri.INVARIANTS)`；② 按 592/583 既有机制把两模块并入 `SERIAL_EXTRA`（`-m slow -n0` 仍完整执行，覆盖率不减）。conftest 属 test_config ⇒ `--update` 重钉 |
| D6 | `auto-update` 与 `verify` 的 exit 语义 | 只"内容变更"、无新增/消失时 `auto-update` 不写盘且 **exit 0**（变更仍由 `verify` exit 1 报红） | 行为如实锁定进测试并登记为交人项（若要求 exit 1 属策略变更，需人定） |

## 收工验收（本 agent 实跑）

| 门禁 | 命令 | 结果 |
|------|------|------|
| fast 全量 | `pytest -m "not slow" -n auto` | **exit 0**（全绿，含 `5 snapshots passed`） |
| ruff | 4 个指定文件 + `conftest.py` + `test_replay_invariants_605.py` | `All checks passed!`（exit 0） |
| 受控目录零污染 | `git diff --quiet -- atoms evidence Examples Book` | exit 0 |
| 台账一致性 | `governance_doc_guard.py verify` | exit 0（`manifest 一致 ✓ self_hash 已校验`） |
| 审计 | `proposition_liveness_audit.py --check` | exit 0（79 / 50 / 0） |
| 串行组回归 | `pytest tests/test_replay_invariants_605.py tests/test_replay_invariants_606.py -m slow -n0` | 22 passed |
| 未跑（按任务书分工留监工） | `tool_integrity --check` / `gate --check` / `poison` / `replay` | — |

## 交付物

- `tools/governance_doc_guard.py`（改）：`iter_governed_docs()` / `auto_update()` / `verify_manifest` 跳过 missing / CLI `auto-update`。
- `tools/proposition_liveness_audit.py`（新）：只读，纯标准库，CLI `--check` / `--json` / `--atoms-root` / `--report`。
- `tests/test_governance_auto_update_607.py`、`tests/test_proposition_liveness_audit_607.py`（新，共 11 例）。
- `data/debt_inventory_607.md`、`data/proposition_liveness_audit.md`、`data/governance_docs_manifest.json`（505 条）。

## 交人项

1. **50 条 observation 缺活性锚**：`liveness` 填什么是人审权力（要判"哪个夹具符号真能证伪这条命题"）；工具只列清单、**不自动补**。
2. **扩边后的弱化指令扫描**：扫描面从 344 → 505，`governance_doc_guard.py scan` 的 high 命中需要人读语义（机械登记 ≠ 语义认可）。
3. **`auto-update` 的 exit 语义边界（D6）**：只有内容变更时不写盘、exit 0；若监工认为应 exit 1，需人定策略。
4. **606 的"收工门禁全绿"结论不准（D5）**：当时应为模块级跑绿，全套 `-n auto` 是红的；本批已修，建议后续批次把 `-m "not slow" -n auto` 作为硬门禁而非只跑本模块。
5. **manifest 自身不在校验范围**（592 已知信任边界），本批未变。
6. 新落地的 `PM_加工作密度规划_20260919.md` 已机械登记（台账一致性恢复），**不代表对其内容的语义认可**。

## 未做项（明确声明）

- 未自动补 `liveness`；未改任何命题卡（`atoms/` 零改动，`git diff` 已证）。
- 未删除 manifest 历史条目（只追加 + 标记 `missing`）；未做治理文档语义审查。
- 未改 `gate_engine.py` / `atom_evidence_replay.py` / `poison_drill.py` / `toolchain.py` / `cppbible.py`（CORE）与 `metrics_collector.py`。
- 未动 `atoms` / `evidence` / `Examples` / `Book`。
- 未 push、未代签 golden accept、未写下一批提示词、未跑验收门禁。
