# algorithms — 算法题模板 / 算法实现

平行核心章：`Book/part09_algorithms/`（ch95–113）。

- 主参考：`cppref:cpp/algorithm` `cppref:cpp/container` `book:templates` `book:josuttis17`
- 计划内容（可摘抄/改写 cppreference 可编译示例，须带 `[cppref:...]`）：
  - 常用 STL 算法模板：排序/二分/前缀和/双指针/滑动窗口的「标准写法」
  - 数据结构手写实现：单调栈/队列、并查集、线段树（用 `std::vector` 表达）
  - 复杂度标注（时间/空间），与 part09 互证
  - 模板技巧：用 `std::ranges`(C++20) 改写旧算法（`[std-cpp20]`）
- 引用键示例：`[cppref:cpp/algorithm/sort]` `[book:templates:chxx]` `[std-cpp20]`
