# 642 C1 · auto_executor 白名单扩展评估（**只评估，不实际扩展**）

> 现有白名单（**代码事实** `auto_executor_640.WHITELIST`）：`['control_chars', 'final_newline', 'trailing_ws', 'ruff_fix', 'snapshot_update']`
> 受保护路径前缀（`PROTECTED_PREFIXES`）：**12** 条（含 CORE_TOOLS / 账本 / 受控目录）

## 一、评估方法（机械可复算）

```
score = zone_weight(影响面) × (1 if 可逆 else 2) + blast_weight(影响范围)
zone_weight : 判决/信任面 6 · 治理面 4 · 数据产物 3 · 文档/报告 2
blast_weight: 单文件 0 · 数文件 +1 · 仓库级 +2
verdict: 判决/信任面 ⇒ 拒绝；不可逆 ⇒ 拒绝；score ≥ 6 ⇒ 延后；否则技术可入选（仍需过 C2 门槛）
```

## 二、候选逐条评估

| 类别 | 影响面 | 可逆 | 范围 | score | 已在白名单 | 结论 |
|---|---|---|---|---|---|---|
| `protector_mark_write` | 数据产物 | ✅ | 数文件 | 4 | 否 | **技术可入选（本批不扩大）** |
| `rule_metadata_update` | 治理面 | ✅ | 单文件 | 4 | 否 | **技术可入选（本批不扩大）** |
| `report_regen` | 文档/报告 | ✅ | 数文件 | 3 | 否 | **技术可入选（本批不扩大）** |
| `test_assertion_number_update` | 治理面 | ✅ | 数文件 | 5 | 否 | **技术可入选（本批不扩大）** |
| `add_check_flag` | 治理面 | ✅ | 单文件 | 4 | 否 | **技术可入选（本批不扩大）** |
| `ledger_write` | 判决/信任面 | ❌ | 仓库级 | 14 | 否 | **拒绝** |
| `gate_rule_edit` | 判决/信任面 | ✅ | 仓库级 | 8 | 否 | **拒绝** |
| `knowledge_card_edit` | 判决/信任面 | ✅ | 仓库级 | 8 | 否 | **拒绝** |

### 2.1 逐条：护栏需求 / 回滚方案 / 结论理由

| 类别 | 护栏需求 | 回滚方案 | 结论理由 |
|---|---|---|---|
| `protector_mark_write` | ① 只写 `data/` 下的**声明前缀**（如 `642_`）；② 写入幂等（同 (batch,category) 不重复追加）；③ **append-only**：不得改写已有标记文件的历史条目；④ 标记**不得回写判决面**（不得改 gate_engine 输入）；⑤ 写入前备份原字节、写入后校验 JSON 可解析 | 删除/还原本次新建的标记文件即可（不改判决 ⇒ 回滚零语义影响） | score=4 < 6，但 C2 校准度门槛未达（最高观测 50.0% ≤ 60%）⇒ **本批不扩大白名单** |
| `rule_metadata_update` | ① **不得**改 `tools/gate_engine.py`（判决面铁律）；② **不得删除**被拒规则（只追加结论）；③ 必须带版本号 + 生成来源 + 阈值常量快照；④ 只允许写**新规则**的准入结论（现有 67 条零触碰） | 还原侧车 JSON 即可；因不改 gate_engine，回滚不影响任何判决 | score=4 < 6，但 C2 校准度门槛未达（最高观测 50.0% ≤ 60%）⇒ **本批不扩大白名单** |
| `report_regen` | ① 只允许重生成**本批前缀**的报告（如 `642_*`），不得跨批覆盖；② 重生成后 diff 必须**有界**（仅数字/时间戳漂移；结构变化 ⇒ 拒绝）；③ **不得**写入受控目录（atoms/evidence/Examples/Book）；④ 报告必须**从现算派生**（禁「报告另算」）——与 640c 单一权威源口径一致 | `git checkout -- <report>` 还原；因报告是派生量，重生成无副作用 | score=3 < 6，但 C2 校准度门槛未达（最高观测 50.0% ≤ 60%）⇒ **本批不扩大白名单** |
| `test_assertion_number_update` | ① **禁止**把断言改成「当前值」（等于把测试降级为快照，会掩盖真实回归）；② 只允许改**派生自单一权威源**的数字断言（640b/640c 口径）；③ 改动必须与权威源现算值一致（不变量 + 跨源校验） | 还原测试文件；但它直接降低回归发现力 ⇒ **建议拒绝**而非延后 | score=5 < 6，但 C2 校准度门槛未达（最高观测 50.0% ≤ 60%）⇒ **本批不扩大白名单** |
| `add_check_flag` | ① 新增 `--check` 必须**只读幂等**（不得写盘、不得改状态）；② 必须有单测覆盖 `--check` 返回 0 且零写盘 | 还原脚本文件；无运行时副作用 | score=4 < 6，但 C2 校准度门槛未达（最高观测 50.0% ≤ 60%）⇒ **本批不扩大白名单** |
| `ledger_write` | ① **永不允许自动化**：账本写入 = 代签（铁律 §零.4） | 不可回滚（append-only 哈希链；改历史即破坏链） | 影响面是判决/信任面 ⇒ 违反铁律（CORE_TOOLS/账本/受控目录不可自动化） |
| `gate_rule_edit` | ① **永不允许自动化**：CORE_TOOLS 判决逻辑零改动（铁律 §零.2） | 理论可 git 还原，但一旦在自动化批次里发生，无法保证无中间污染 | 影响面是判决/信任面 ⇒ 违反铁律（CORE_TOOLS/账本/受控目录不可自动化） |
| `knowledge_card_edit` | ① **永不允许自动化**：受控目录零污染（铁律 §零.3） | 可 git 还原，但会污染 Merkle 根与完整性台账 | 影响面是判决/信任面 ⇒ 违反铁律（CORE_TOOLS/账本/受控目录不可自动化） |

### 2.2 逐条补充说明

- `protector_mark_write`：642 A6 已实现 `rollback_marks()`（按保护器精确摘除）⇒ 回滚路径已被单测覆盖
- `rule_metadata_update`：风险不在文件本身，而在「有人把侧车当成规则源」——需配套「侧车只读」约定
- `report_regen`：风险最低且收益明确；**唯一真实风险**是覆盖别人的手写报告 ⇒ 前缀白名单可解
- `test_assertion_number_update`：🔴 **实测不在** `auto_executor_640.WHITELIST` 内（inbox 口径与代码事实不一致，见 §四 honest_diff）
- `add_check_flag`：🟡 **实测不在** WHITELIST 内（inbox 口径与代码事实不一致）；但 634 A2 是**人工批量**补 --check，未走 auto_executor
- `ledger_write`：🔴 明确**拒绝**：这是「机器不代签」红线的直接违反
- `gate_rule_edit`：🔴 明确**拒绝**
- `knowledge_card_edit`：🔴 明确**拒绝**；`auto_executor_640.PROTECTED_PREFIXES` 已硬编码拦截

## 三、结论分布与最终建议

- 分布：`{'技术可入选（本批不扩大）': 5, '拒绝': 3}`
- **是否真的扩大**由 C2 门槛裁决：门槛 **60%**，当前最高观测 **50.0%** ⇒ ****未达门槛 ⇒ 本批不扩大 auto_executor 白名单****
- 附加条件：样本量极小（已判定 8 + 4），且 R1/R2 口径不同 ⇒ **不足以判趋势**

1. **本批不扩大白名单**（门槛未达；且 C1 铁律要求只评估）；
2. 若 643 门槛达成，**优先考虑** `report_regen`（score 3，风险最低、收益明确）；
3. `protector_mark_write`（score 4）次之 —— 其回滚路径已被 642 A6 单测覆盖；
4. `rule_metadata_update`（score 4）**需先确立「侧车只读」约定**再谈自动化；
5. `test_assertion_number_update` **建议拒绝**（把断言改成当前值等于降级测试），尽管 score 只有 4；
6. `snapshot_update` **已在白名单**，但其语义风险建议 643 复核。

## 四、诚实发现：inbox 口径 vs 代码事实

- inbox 声称的现有白名单：`['测试断言数字更新', '快照重锁', '补 --check', 'ruff', '控制字符清理']`
- 代码事实 `WHITELIST`：`['control_chars', 'final_newline', 'trailing_ws', 'ruff_fix', 'snapshot_update']`
- 一致：**False** —— 两处不一致：inbox 把「测试断言数字更新 / 补 --check」算作现有白名单，但 `auto_executor_640.WHITELIST` 实测**不含**二者；本工具以**代码事实**为准。另：`snapshot_update`（快照重锁）**在**白名单内，但其语义风险（可能掩盖真实行为变化）值得 643 复核。

## 诚实登记

1. **只评估不扩展**（§七.5）：本模块**未改动** `auto_executor_640.WHITELIST`、**未执行**任何自动修复（连 dry-run 都未调用）；
2. `CANDIDATES` 的「影响面/可逆性/范围」是**人工判据**，score 公式是本批新造 ——它**可复算**，但不是**被验证过**的风险模型（无历史事故数据校准）；
3. **风险等级 ≠ 闸门**：真正的闸门是「能不能回滚」与「是否触碰判决面」，score 只用于同类排序；
4. `report_regen` 的最大隐患是「覆盖别人的手写报告」，前缀白名单能降低但不能消除；
5. 门槛 60% 本身是**经验值**（C2 已登记样本极小）⇒ 是否降低门槛交人裁决。
