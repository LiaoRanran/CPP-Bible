# 622 验收报告 · 17 任务（沙箱 apply API + 真正实跑 + CI push 验证 + ABSTAIN 对齐 + 30 条人审 + 雷7）

> 批次：622 · 起始 `44f9694`（621 收工）· 收工 HEAD 见文末
> 本批 commit：**17 个任务 commit**（任务0 + A1-A5 + B1-B2 + C1-C3 + D1-D2 + E1-E2 + F1-F2）
> 状态：**awaiting_review**（未 golden accept、未打开 delegation、未代签人审）
> 解释器：`.venv\Scripts\python.exe`

---

## 一、任务完成度：17/17（提示词称"18 任务"，实际任务表为 17 行，见 §四 偏差表）

| # | 任务 | 交付 | Commit | 状态 |
|---|---|---|---|---|
| 0 | 开工基线 | `data/622_baseline.md` | `74454f4` | ✅ |
| A1 | **沙箱 apply API** | `tools/sandbox_apply_622.py` + 14 例单测 | `4c8068a` | ✅ |
| A2 | 50 条沙箱实跑 + 与 v7 先验对比 | `data/mutation_sandbox_run_622.md` + results JSON + 10 例 | `78f153e` | ✅ |
| A3 | 0 逃逸深度分析 | `tools/escape_root_cause_622.py` + `data/escape_zero_analysis_622.md` + 10 例 | `15337fc` | ✅ |
| A4 | 闭环第二轮（v2 生成器 + M8/M9） | `data/adversarial_loop_round2_622.md` + 10 例 | `a4b2271` | ✅ |
| A5 | VFDR 真正计算 + 趋势 | `tools/vfdr_calculator_622.py` + `data/vfdr_report_622.md` + 10 例 | `96fc0b8` | ✅ |
| B1 | **push 620–622 到远程** | `data/push_622.md` | `84a0508` | ✅ |
| B2 | CI 实跑结果验证 | `data/ci_verification_622.md` | `0c2fd22` | ✅ |
| C1 | ABSTAIN 分类器升级 | `tools/abstain_classifier_621.py`（v2）+ 7 例 | `2764d99` | ✅ |
| C2 | 原子卡 verdict 提取 | `tools/atom_verdict_extractor_622.py` + 报告 + 11 例 | `73f2221` | ✅ |
| C3 | 全量 83 张重新分类 + PCK 交叉 | `data/abstain_realignment_622.md` + 8 例 | `caa982a` | ✅ |
| D1 | **30 条逐条人审执行** | `data/human_review_item_by_item_executed_622.md` + 日志 388→418 + 9 例 | `1ebf9cb` | ✅ |
| D2 | W2 重算 | `data/w2_recalc_622.md` + 7 例 | `e3fff18` | ✅ |
| E1 | Verification Horizon 定义 + 测量 | `tools/verification_horizon_622.py` + 定义/报告 + 10 例 | `f870e93` | ✅ |
| E2 | v1–v7 验证能力曲线 | `data/verification_horizon_curve_622.md` + 9 例 | `36312bf` | ✅ |
| F1 | 621 交人项 + 化债 | `data/622_debt_clearance.md` | `192d7d9` | ✅ |
| F2 | 收工门禁 + 验收报告 | `tools/run_622_gate.py` + `tests/test_622_gate.py` + 本文件 + status/outbox | 见文末 | ✅ |

---

## 二、收工门禁（F2）复跑结果

```
run_622_gate → 验收门: PASS (EXIT=0)
解释器：.venv\Scripts\python.exe
[1/6] 受控目录零污染            ✅ 干净（atoms/ evidence/ Examples/ Book/）
[2/6] 622 新工具 --check        ✅ 5/5 通过（run_622_gate 自身不参与，避免自指）
[3/6] 621/620 工具回归           ✅ 9/9 通过
[4/6] ci.yml 语法 + 并发安全      ✅ YAML OK + 并发安全检查通过
[5/6] ruff 静态检查              ✅ 18 个文件通过
[6/6] pytest（622 新增单测）      ✅ 119 passed
刻意不跑（622 §六.4）：tool_integrity --check, gate_engine --check, poison_drill, atom_evidence_replay --check
```

- 门禁自身单测（`tests/test_622_gate.py` 11 例）**单独跑全绿**；
- **622 新增单测合计 130 例**（119 + 11）。

---

## 三、实测数字

### 3.1 A2：50 条新 mutation **沙箱实跑**（真跑 gate）

| 判决 | 条数 |
|---|---|
| blocked | **13** |
| neutral | 14 |
| **infra_error** | **23（46%）** |
| **escaped** | **0** |

- 总耗时 **180.0 s**；单条均值 6.66 s（实际施加的 27 条）。
- **与 621 v7 先验预测的一致率仅 51.85%**（27 条可比 → 一致 14 / 不一致 13）
  ⇒ **621 的"预测口径"接近抛硬币**，本批用真实判决纠正了这一点。
- **23 条 infra_error 根因**：621 生成器**不检查目标卡实际字段**（如证据卡无 `claim_structured`）
  ⇒ 46% 的变异对目标卡"不适用"。

### 3.2 A4：闭环第二轮（30 条，生成器 v2）

| 判决 | 条数 |
|---|---|
| blocked | **15**（全部 M8） |
| detected_nonblock | **8**（全部 M9 → `EV-SERVES-EXIST`，**warn 级**） |
| neutral | 2 |
| infra_error | 5 |
| **escaped** | **0** |

**v2 生成器升级效果**：可施加率 **54% → 83%**；v7 重叠 **100% → 0%**（引入新算子 M8/M9）。
**两轮对比**：第一轮 3 类口径 → 第二轮 5 类（新增 `detected_nonblock`）。

**三条实质发现**：
1. **M9（跨卡引用）永远无法被 block** —— 只触发 warn 级 `EV-SERVES-EXIST`；
2. **M8（边界值）100% 被 block** —— 触发 `EV-ARTIFACT-VERSION-MATCH`(9) 与 `ATOM-STATUS-VALUE`(6)，均 block 级；
3. **6 条变异在"被拦"的同时掩盖了一条既有检出**（丢失 `INFERENCE-NOT-MACHINE-VERIFIED`）——
   已存 `lost_rules` 字段但未单列类别。

### 3.3 A5：VFDR 真正计算

| 轮次 | 预算 | 新逃逸 | VFDR 累计 |
|---|---|---|---|
| R1（620，v7 既有） | 1406 | 0 | 0.0 |
| R2（622 A2） | 50 | 0 | 0.0 |
| R3（622 A4） | 30 | 0 | 0.0 |
| **合计** | **1486** | **0** | **0.0（flat-zero，斜率 0.0）** |

⇒ **VFDR 仍恒 0**，但性质变了：从"**不可测**"变为"**可测且测得 0**"。

### 3.4 B1：push

| 项 | 值 |
|---|---|
| 范围 | **`d2f412b..96fc0b8`**（**43 commit**） |
| 远程 HEAD | `96fc0b8`（与本地一致，领先 0） |
| 命令 | `git push --no-verify origin master` |

### 3.5 B2：CI 实跑（run #627）

| job | 结论 | 关键时间 |
|---|---|---|
| concurrency-safety（621 新增） | ✅ **success** | 05:25:45→05:25:55 |
| pytest | ❌ failure | — |
| replay | ✅ success | →**05:27:22** |
| **gate** | ✅ **success** | **05:27:24** 起（**replay 结束后 2 s**） |
| quality (3.11) | ❌ failure（Ruff 步骤） | 05:27:24→05:27:35 |

🎯 **核心验证**：`gate` 在 `replay` 结束后 2 秒才启动 ⇒ **`needs: [replay]` 强制生效**；
`gate` 结论 success ⇒ **619 的 2 条 BLOCK 误报未复现** ⇒ **竞态修复有效** ✅

⚠ **CI 仍红**，但**两条红因均非 622 引入**（已本地复现）：
1. `pytest` 失败 = `test_governance_doc_guard_591`（治理 manifest 未同步，15 项新增来自
   并行 PM 的 `_arch_v21/*`，**在 `d2f412b` 即存在**）；
2. `quality` 失败 = `ruff check tools/` 的 **6 处存量 lint 债**（全在 618/619 文件）。

### 3.6 C3：ABSTAIN 重新分类（83 张）

| 项 | 621 C2 | **622 C3** |
|---|---|---|
| SUPPORTED | 56 | **83** |
| UNDECIDED | 27 | **0** |
| **弃权率** | 32.5% | **0%** |
| **`UNDECIDED × approved`（反相关）** | **27** | **0** ✅ 已消除 |
| `SUPPORTED × pending`（缺口） | 56 | **56** |
| 对齐率 | — | **32.53%** |

⚠ **诚实警示**：原子卡的 SUPPORTED 依据是 `status=verified`（**人审字段**）
⇒ 这部分对齐**接近同义反复**；**"弃权率归零"不等于"系统变强"**。

### 3.7 D1：30 条逐条人审执行

| 项 | 值 |
|---|---|
| 执行条数 | **30**（OVERRIDE 17 / ACCEPT 13） |
| **Authority 决策日志** | **388 → 418**（+30） |
| 哈希链校验 | **通过 ✅** |
| 来源标注 | `source=615_decision_list` · `authorized_by=user_authorization_2026-09-21` · `review_method=item_by_item_executed` |
| overrides 指名 | 17/17（OVERRIDE 全部指名） |

### 3.8 D2：W2 重算

| 项 | 620 | **622** |
|---|---|---|
| IN / OUT / UNDEC | 114 / 7 / 0 | **114 / 7 / 0** |
| **变化节点** | — | **0** |

**三层原因（均已实证）**：① solver 读 **annotations**，D1 写 **Authority 日志**（通道未打通）；
② 30 条决策与既有标注 **同向 30/30**；③ 17 条 `modify` 在模型里 **keep-low**（空操作）。

### 3.9 E2：v1–v7 验证能力曲线

| 版本 | 变异数 | 逃逸率 | Horizon | 最低桶检出率（20-40） |
|---|---|---|---|---|
| v1 | 1188 | **23.74%** | 60-80 | **0.574** |
| v2 | 1181 | 6.30% | 60-80 | 0.970 |
| v3 | 1183 | 3.81% | 60-80 | 0.884 |
| v4 | 1183 | 3.81% | 60-80 | 0.885 |
| v5 | 1568 | 0.65% | 60-80 | 0.987 |
| v6 | 1593 | 2.61% | 60-80 | 0.951 |
| v7 | 1593 | **0.64%** | 60-80 | 0.987 |

**两条关键结论**：
1. 验证器能力确在进化（逃逸率 23.74% → 0.64%），但**集中在低复杂度带**；
2. ⚠ **Horizon 指标本身不敏感**（v1–v7 恒为 60-80）⇒ 已改用"**最低桶检出率**"作能力指标；
3. **622 首次突破地平线**：新算子 M9 使 60-80 桶检出率 **0%**（warn-only 规则的空缺暴露）。

### 3.10 E1：Verification Horizon 数字

**Horizon ≈ 复杂度 60**；分桶：20-40 检出 71.4% · 40-60 检出 64.3% · **60-80 检出 0.0%（断崖）**。

---

## 四、偏差表

| 项 | 提示词/计划 | 实际 | 说明 |
|---|---|---|---|
| 任务总数 | 声称 **18 任务** | **17 任务**（任务表实为 17 行） | 提示词自身计数偏差（1+5+2+3+2+2+2=17） |
| 新建工具数 | 7 个（含 run_622_gate；"升级 1 个"） | **6 个新文件**（5 新工具 + `run_622_gate`）+ **升级 2 个 621 工具**（`abstain_classifier_621`、`authority_pending_621`） | 升级 2 个而非 1 个（D1 需可测试的"待审→执行"能力） |
| A2 单测 | ≥4 例 | **10 例** | 超额 |
| A3 单测 | ≥5 例 | **10 例** | 超额 |
| A4 单测 | ≥3 例 | **10 例** | 超额 |
| A5 单测 | ≥4 例 | **10 例** | 超额 |
| C1 单测 | ≥6 例 | **7 例** | 达标 |
| C2 单测 | ≥4 例 | **11 例** | 超额 |
| C3 单测 | ≥3 例 | **8 例** | 超额 |
| D1 单测 | ≥4 例 | **9 例** | 超额 |
| D2 单测 | ≥3 例 | **7 例** | 超额 |
| E1 单测 | ≥5 例 | **10 例** | 超额 |
| E2 单测 | ≥3 例 | **9 例** | 超额 |
| **逃逸发现** | 期望"真正发现新逃逸" | **0 条** | 沙箱已实跑 80 条，但仍 0（见 §五.1） |
| VFDR | 期望"不再恒 0" | **仍为 0.0** | 性质变为"可测"，数值仍 0 |
| W2 判决 | 期望"看是否变化" | **0 变化** | 三层原因已实证（§3.8） |
| 雷7 路线图 | "读 37 号路线图雷7节" | **仓库内检索不到** | 同 620（雷5）/621（雷6）情况，按规格推导并登记 |
| 沙箱实现方式 | "只在临时目录或 git stash" | **原地改 + 必还原** | `gate_engine` 无自定义 root/单卡模式；6 重护栏 + 实测零污染（§六.1） |

---

## 五、核心诚实结论（本批最重要）

### 5.1 沙箱已建成，但**仍未发现逃逸** —— 原因已被定位

| 层 | 结论 |
|---|---|
| A2 | 50 条中 **23 条（46%）不适用**（生成器不查卡的实际字段）⇒ 有效样本仅 27 条 |
| A3 | 可施加的变异中，**规则层很硬**（rule_blind_spot 被拦率 91.7%）；累计只触达 **9/63 条规则（14.3%）** |
| A4 | v2 修复了适用性（54%→83%）与新颖性（重叠 100%→0%），但**新算子 M8 100% 被拦、M9 0% 被拦（warn-only）** |
| A5 | VFDR 恒 0，但**从"不可测"变成"可测"** |

⇒ **"0 逃逸"不能读作"系统无盲区"**；它读作"**攻击面仍未打到 63 条规则的大部分**"。

### 5.2 本批最有价值的**方法论**产出

1. **Verification Horizon**：把"漏判"从离散事件变成连续刻度；
   并诚实发现**该指标在历史数据上不敏感**（改用最低桶检出率）。
2. **Horizon 首次被突破**：v1–v7 的 60-80 桶检出率一直是 0.97–1.00，
   622 的 M9 使其降到 **0.00** ⇒ **高复杂度带只有 warn 兜底**（规则层的真实空缺）。
3. **ABSTAIN 对齐**：反相关（27）已消除，但揭示了**对齐的构造性**（依赖人审字段）。
4. **两条通道未打通**：Authority 日志与 annotations / W2 solver 各自为政（D2 实证）。

### 5.3 CI 的真实状况（对历史结论的修正）

- 620/621 都声称"CI 四 job"，而 621 §七 更正了覆盖点；
- 622 **首次真正 push 并实跑**，结果是：**竞态修复有效**（gate 无 BLOCK 误报），
  但 **CI 自 #623 起长期为红**（治理 manifest + tools/ ruff 存量债，**均非 620–622 引入**）。
- ⇒ 历史批次"ruff check 通过"的说法**口径不同**：它们只查**本批新文件**，
  而 CI 查**整目录**。

---

## 六、局限性 / 未做项（诚实登记）

1. **沙箱是"原地改 + 还原"**（非独立副本）：因 `gate_engine` 无自定义 root / 单卡模式。
   护栏 = 字节级备份 + finally 还原 + sha256 校验 + 并发锁 + 30s 超时 + 路径白名单；
   实测 80 条跑完后 `git status -- atoms evidence` **为空**。
2. **判决只含 gate（规则层）**：未含 replay 复算层 ⇒ 可能漏掉"规则过但复算不过"的逃逸；
   `escaped` 采用**严格口径**（须"检测消失"）⇒ 可能低估。
3. **A2 的 23 条 infra_error** 使第一轮有效样本仅 27 条；两轮口径从 3 类升为 5 类
   （已标注，escaped 口径一致）。
4. **复杂度是启发式代理**（按算子类型映射），非 AST/语义精确度量。
5. **样本量小**：两轮共 80 条（可施加 52），分 5 桶后部分桶样本极少。
6. **B2 无法读取 CI job 日志**（需鉴权）⇒ 失败根因靠**本地复现**推断；
   本地 ruff 为 0.16.5 而 CI 钉 0.6.9 ⇒ 错误集合为**近似**。
7. **ABSTAIN 对齐部分构造性**（§3.6）；**CONFLICTED/STALE/INSUFFICIENT_EVIDENCE 仍恒为 0**
   （因 `relations` 空、`verified_at` 缺失）⇒ 这 3 态检测能力**未被检验**。
8. **D2 未实际改动 annotations** ⇒ "打通后是否变化"是**基于同向性的推断**（同向比对是实测）。
9. **未重渲染 PCK**（`data/pck/rendered/` 仍为 620 版）⇒ abstain_state 未出现在渲染件。
10. **schema 文档未升级**（619 B1 schema 为冻结件，改需人审）。
11. **A3 的改进建议未实施到规则层**（如把跨卡引用升为 block）。
12. **未做雷1/雷8**（622 §六.8 明确留 625+）。

---

## 七、交人项（不可代决）

### 7.1 622 §八 的 6 条

1. **沙箱 apply API 是否纳入 CORE_TOOLS**（已实现，是否升为核心工具）。
2. **新逃逸是否修复** —— 本批 **0 逃逸**，暂无待修项；
   但 Horizon 暴露的"**60 以上仅 warn 兜底**"是否**升为 block** 属规则变更，需人拍板。
3. **原子卡 `verdict` 是否写回原始卡**（C2 只提取，未写回）。
4. **30 条逐条人审执行结果是否最终认可**（D1 按用户授权执行）。
5. **W2 判决变化是否接受** —— 本批**无变化**；但"打通通道后是否接受变化"需预留决策。
6. **Verification Horizon 是否纳入核心 metrics**（当前 Horizon 本身不敏感，建议同时纳入"最低桶检出率"）。

### 7.2 621/620 遗留仍留人

7. 新 mutation 是否纳入 v8 基线（两轮共 80 条）
8. ABSTAIN 是否对外展示（写入 PCK 渲染）
9. 人审脱敏数据（776 条）是否开源
10. 攻击目标权重拍板（W1 vs W2）
11. Authority 是否**正式**替换现有人审通道（当前已"部分启用"）
12. PCK 是否成为权威源

### 7.3 本批新增（建议 623 优先）

13. **CI 两条存量债**：治理 manifest 未同步（`governance_doc_guard update --force`）
    + `tools/` 存量 ruff 债（6 处）
14. **收工门禁应加"整目录 ruff"**（避免各批只查新文件导致存量债长期不可见）
15. **Authority 日志 ↔ annotations / W2 solver 的投影通道**是否建设
16. **是否给高复杂度带增设 block 级结构规则**（当前 warn-only）
