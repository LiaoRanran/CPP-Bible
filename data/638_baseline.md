# 638 任务0 · 开工快照 + 637 闭环第一次产出复盘

> 批次：638（v25 阶段 3 启动——四态结论真上线 + Lifecycle FSM + 闭环迭代）。
> 时间：2026-09-25。性质：**骨架改造**（四态/FSM 真进系统），但**向后兼容**：历史判决不删不改。

---

## 一、开工快照（实测，非抄表）

| 类别 | 指标 | 实测值 | 采集方式 | 与 §一 表一致性 |
|---|---|---|---|---|
| 规模 | gate 规则数 | **67** | `gate_engine.RULES` | 一致 |
| 规模 | 工具数（`tools/*.py`） | **423** | `glob` | 高于表内 417（本会话新增 6） |
| 规模 | 测试数（`tests/test_*.py`） | **422** | `glob` | — |
| 规模 | commits 数 | **1848** | `git rev-list --count HEAD` | — |
| 判决 | 判决总数（Authority ledger） | **452** | `data/authority/decision_event_v2_ledger.jsonl` | 一致 |
| 判决 | 判决结果分布 | APPROVE 367 / MODIFY 85 | 同上（**无 REJECT**） | 新发现 |
| 判决 | 台账 target_type | 全部 `edge`（452/452） | 同上 | 新发现 |
| 判决 | 四态分布（模拟，636） | fail 26 / pass 9 / unknown 1 / pass_with_exception 1（n=37） | `four_state_636.py --json` | 表写 fail 25，实测 26 |
| 冲突 | RR（全局） | **23/23 卡全有** | `636_conflict_detector_shadow.md` | 一致 |
| 冲突 | 超阈值（C≥0.3） | **2/23** | 同上 | — |
| 校准 | known_error_rate | **67 条规则全「无数据」** | `636_calibration_tracker_report.md` | 一致 |
| 校准 | MDL 通过率 | 44.8%（30/67） | 635/636 冻结值 | 一致 |
| 知识 | 接地率 | **35.8%**（24/67） | `grounding_inventory_635.stats()` | 一致 |
| 知识 | 击败器覆盖率 | **100%**（23/23） | `defeater_ledger_635.stats()` | 一致 |
| 知识 | taint=true 卡 | **8** 张 | 同上 | 一致 |
| 治理 | 例外条款 | **227** 条（7 处来源） | `exception_review_635.stats()` | 一致 |
| 安全 | 真逃逸 | **1** 条（TCE-614-001） | `data/mutation/known_tce.jsonl` | 一致 |
| 进度 | ahead | **9** | `git rev-list --count origin/master..HEAD` | 一致 |
| 进度 | 工作区脏文件 | **94** | `git status --short` | 高于 635 的 70 |

### 1.1 边界三元组口径（3.1 依赖）

| 字段 | 准确名 | 当前统一值 | 权威来源 |
|---|---|---|---|
| mutation 集哈希 | `mutation_set_hash` | `d7556d622e92fbf918cf9b49c39d97da0733c88fb686be7e734df7fca294ac57` | `tools/boundary_fields_635.py`（对 `data/mutation/full_baseline_v7.json` 取 SHA256） |
| 变异数 | `mutation_count`（**非** `count`） | `1593` | 同上 |
| 生成器版本 | `generator_version` | `mutation_fuzz@v7` | 同上 |

> **关键口径**：边界三元组**不在** Authority ledger 里（ledger 26 字段无此三项），只回填在 `data/*baseline*` 报告（实测 37 份中 34 份有）。3.1 的「无边界即降级 unknown」因此按**报告级**判定。

---

## 二、637 闭环第一次产出复盘

### 2.1 关于 637 产物的可用性（必须先说）

`data/637_evolution_memo.md` 及其上游（`637_observer/errors/candidates/scored`）**在本批开工时不存在**（`git ls-files` 为空、磁盘无文件）——即 **637 的闭环在 637 批次里从未真正落盘**（只有 `637_baseline.md` 被保留，且其引用的 `637_observer_report.md` 也缺失）。

本任务是「读 637 memo 复盘」，为不阻塞，我用 637 自带的闭环运行器**按序重跑了一次**（`self_observer(--no-heavy)` → `error_detector` → `candidate_generator` → `cost_benefit` → `evolution_memo`），产出 `data/637_*`。**本次重跑产物即视为「第一次产出」**，已在 §四 登记。

### 2.2 top 3 建议（第一次产出）

| 排名 | 建议 | score | 对应异常 | 成本/收益 | 风险类别 |
|---|---|---|---|---|---|
| 1 | 为例外设过期（+180 天复审） | 2.0 | 例外条款多（P2） | 低/中 | tool |
| 2 | 优先修高 taint（复核引用最多的 tainted 卡） | 1.5 | taint=true 卡多（P1） | 中/高 | tool |
| 3 | 加自动污染传播追踪器（影子） | 1.5 | taint=true 卡多（P1） | 中/高 | tool |

全部 12 个候选的排序见 `data/637_scored.md`。

### 2.3 采纳情况（本批 638 实际做了什么）

| 637 建议 | 采纳？ | 638 对应 | 理由 |
|---|---|---|---|
| 收历史错误率（候选 #10，score 0.75） | **采纳** | **B1 known_error_rate 收集器** | 636 已确认「67 规则全无 known_error_rate」是最大缺口，且它是其它三项的前置 |
| 为例外设过期（top1） | 未采纳 | — | 例外治理不在 638（阶段 3 骨架）范围；改 227 条例外结构属独立批次 |
| 优先修高 taint（top2） | 未采纳 | — | taint 复核需人审，交 638 交人项 |
| 自动污染追踪（top3） | 未采纳 | — | 依赖 636 追踪器，非阶段 3 骨架 |
| 未接地降级 / 观察态降级（core 类） | 未采纳 | — | 属**判决逻辑变更**，§零.1 要求向后兼容，本批不动判决 |
| 637 自省「观察态规则缺 known_error_rate」 | **采纳** | B1 | 与候选 #10 同源 |
| 637 自省「没有跨轮记忆，可能重复提建议」 | 部分 | **C2 调参记录** | 本批用 638 实测数据校准阈值，但不实现跨轮记忆（登记为后续） |

### 2.4 靠谱率（诚实口径，多个分母都给）

| 口径 | 计算 | 靠谱率 |
|---|---|---|
| top-3 采纳率 | 0 / 3 | **0.0%** |
| 全 12 候选采纳率 | 1 / 12 | **8.3%** |
| 与 638 六项任务的重合度（B1 直接来自建议） | 1 / 6 | **16.7%** |
| 「建议是否指向真实问题」（不要求本轮采纳） | 12 / 12 均指向实测异常 | 100%（**但异常本身有误报，见 2.5**） |

> **结论**：按「本轮被采纳」计，靠谱率 **低（0%~16.7%）**。原因是 637 的建议**与 v25 阶段 3 的批次目标错配**，而非建议本身荒谬——它指向的异常是真的，只是 638 的骨架任务排不上它们。这是**可用性**问题，不是**正确性**问题。

### 2.5 「不靠谱」原因分类（每条附真实证据）

| 编号 | 原因 | 证据（实测） | 归属 |
|---|---|---|---|
| **R1** | **阈值未经统计校准 → 误报** | 637 把「观察态规则多」判为 **P1 异常、当前值 100.0 阈值 30**，并列为 **top1 问题**；但观察态 67/67=100% 是 Daubert 表的**设计属性**（635 已冻结为 100%），不是异常 ⇒ **确认为误报** | 阈值 |
| **R1'** | 同类误报 | 「例外条款多」227 > 100 判 P2：227 是 **7 处来源汇总**（635 冻结），亦非异常 | 阈值 |
| **R2** | **无跨轮记忆 → 与批次目标错配** | 637 的 top3 全是 taint/例外，而 638 的任务是四态/FSM/错误率 ⇒ top3 采纳率 0% | 架构缺失 |
| **R3** | **纯规则匹配、无语义 → 建议笼统** | 候选方案全为 `LIBRARY` 里 8 类固定模板，同异常永远同 2-3 个方案 | 方法 |
| **R4** | **成本/收益为人工赋值，非实测** | `cost_benefit_637.LEVEL` 是 `{低:1,中:2,高:3}` 常量；score 完全由手填等级决定，**无工作量/工期的真实估计** | 数据 |
| **R5** | **缺 known_error_rate → 判不准规则准不准** | 637 memo 自认「观察态规则缺 known_error_rate，我只能数条数、无法判断它们到底准不准」；`636_calibration_tracker_report.md` 实测 67/67 全「无数据」 | 数据缺失 |
| **R6** | **健康指标本次为空 → 检测缺一半** | 本次重跑用 `--no-heavy`，`pytest_failures=null` ⇒ 637 memo 中 pytest 行为 `—/❓`，P0 级检测项（失败测试）**根本没参与判断** | 采集 |

> R1/R1' 直接导致 **top1 是误报**；R5 被 638 采纳为 B1；R6 是本次重跑的采集选择，已登记（若跑重指标耗时约 25 分钟）。

---

## 三、本批 638 与 637 建议的关系（一句话）

> **637 给了「嘴」（12 条建议），638 不照单执行**（§零.7：闭环输出是输入不是命令）；
> 本批只采纳其中**指向数据缺口的那一条**（收历史错误率 → B1），
> 其余按 **C2 调参记录** 处理「建议为什么不可信」（先修让嘴说话准的元问题），
> 并用 3.1/3.2 把 v25 阶段 3 的骨架（四态 + FSM）真正落地上线。

---

## 四、诚实登记

1. **637 闭环产物系本批重跑所得**：开工时 `data/637_evolution_memo.md` 等 5 个文件不存在（637 批次未落盘）。本批按 637 自带运行器重跑生成，作为「第一次产出」基线；`data/637_baseline.md` 引用的 `637_observer_report.md` 亦为本次重跑产物。**这不是 637 的原始产物**，据此做的复盘结论需按此前提理解。
2. **重跑用 `--no-heavy`**：pytest/ruff/mypy 三项未采集（637 memo 显示为 `—/❓`），故 R6 存在；未采集 ≠ 无问题。
3. **§一 表两处与实测不符**：工具数（表 417 / 实测 423）、四态 fail（表 25 / 实测 26）。以实测为准，表值登记为过期。
4. **靠谱率是「本轮采纳率」口径**，不等于建议质量；四个分母已并列，避免单一数字误导。
5. **R1 误报是我方判定**（依据 635 冻结的设计属性），非 637 自认。
6. **本批不 push、不自动执行闭环建议**（§零.6/§四.5）。

---

## 五、交人项（任务0 部分）

1. 637 的建议与 v25 路线图**长期错配**问题：是改建议生成器，还是每批人工过滤？
2. 637 闭环产物**为何在 637 批次未落盘**——是流程漏跑，还是刻意不落盘？（影响后续是否该把「闭环必须落盘」写成铁律）
