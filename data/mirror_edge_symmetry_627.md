# 627 A4 · 镜像边对称性验证

- 候选边总数：388
- 镜像边（mis_to_prop）：**194**
- 结构对称（存在反向边）：194
- 结构非对称（缺反向边）：0

> **全部 194 条镜像边的 `symmetry_proof_id` 均为 null** ⇒ 均需人工对称性验证（626 判据 9）。

## 非对称镜像边（需人工核查）

（无 — 全部结构对称）

> 本工具**只生成报告，不写入** `attack_edges_candidates.jsonl` 或任何 ledger。
