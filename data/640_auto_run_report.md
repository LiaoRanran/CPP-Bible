# 640 B2 · 闭环自动执行试运行报告

## 一、dry-run（真实仓库）

- 扫描 `data/*.md|json|jsonl`：**检出 378 项**（全部为 `trailing_ws` 行尾空白）；
- 默认 `--max 10` ⇒ 计划截取前 10 项；
- 首批 apply 成功后实测：这 10 项全部是 **CRLF 归一**（git `diff` 内容为空，
  无信息量）⇒ 已还原工作区，未提交。

## 二、夹具试运行（机制验证，`--selftest-fixture`）

| 项 | 结果 |
|---|---|
| `control_chars`（a.md，含 0x00/0x07） | ✅ applied，复扫清零 |
| `final_newline`（b.md，无末尾换行） | ✅ applied，复查有换行 |
| `trailing_ws`（c.md，3 行尾空白） | ✅ applied，复扫清零 |
| `gate_rule_edit`（伪装白名单） | ✅ rejected（护栏①） |
| `tools/gate_engine.py`（保护路径） | ✅ rejected（护栏①路径检查） |

**自动修复 ≥3 项达成（夹具 3 项，文件状态逐一验证）**。

## 三、真实仓库运行（诚实结论）

A1 清算后，真实仓库**没有带内容量的可自动修项**（ruff 0、控制字符 0、快照最新、
老工具 --check 已补）。本批真实执行的 10 项为行尾 CRLF 归一，git 内容零 diff，
无信息量 ⇒ 还原。**"无事可做"正是闭环该有的健康状态**；下次批次产生新的
真实小修项（新报告的行尾空白、新快照漂移等）时，执行层可直接接管。

## 四、统计

| 指标 | 夹具 | 真实仓库 |
|---|---|---|
| 检出 | 5 | 378 |
| applied | **3** | 10（CRLF 归一，已还原） |
| rejected | 2 | 0 |
| rolled-back | 0（失败回滚由单测强制验证） | 0 |
| 越界修改 | **0** | **0**（受控目录 git status 干净） |
| 日志留痕 | 全部（jsonl） | 全部（jsonl） |
