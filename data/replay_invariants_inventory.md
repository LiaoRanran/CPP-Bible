# replay 隐式不变量盘点（605 任务 0）

> 生成时间：2026-09-19 · 从 atom_evidence_replay.py 代码中提取

## 不变量清单

| # | 不变量名称 | 位置 | 当前是否有检查 | 检查是否独立 | 说明 |
|---|-----------|------|---------------|-------------|------|
| I1 | 工件还原不变量 | `_restore_artifact()` 行1082 | ✅ 有（write_bytes 还原） | ❌ 隐式（散落在 run_fuzz 流程中） | replay 跑完后，真实仓库 Examples/ 工件必须与跑前逐字节一致 |
| I2 | 编译可复现不变量 | `_recompile_invariant()` 行1593 | ✅ 有（独立重编译+CCACHE_DISABLE=1+比sha） | ⚠️ 半独立（是 replay 内部函数，不是独立工具） | 同一张卡的同一条命令，两次编译产物 sha 必须一致（短窗口内） |
| I3 | 锁一致性不变量 | `_replay_lock_path()` 行944 / acquire 行966 / release 行1004 | ✅ 有（文件锁互斥） | ❌ 隐式（锁的获取释放在流程中） | 并发跑 replay 时，锁文件必须正确互斥，不能两进程同时写同一工件 |
| I4 | 沙箱隔离不变量 | `run_root()` 行80 / `batch_root()` 行86 | ✅ 有（579/580 实现跑批根隔离） | ❌ 隐式（contextvar 切换） | 跑批根（batch_root）内的操作不能泄漏到真实仓库 |
| I5 | manifest 一致性不变量 | `_manifest_read_path()` 行1974 / `_manifest_key()` 行1979 | ✅ 有（操作历史记录） | ❌ 隐式（replay 内部） | replay manifest 必须正确记录所有操作，可追溯 |

## 统计

- 总不变量：5 个
- 有检查：5/5（100%）
- 独立可验证：0/5（0%）——全部是隐式的，散落在 replay 代码中
- 需要显性化：全部 5 个

## 本批范围

本批新建独立工具 `tools/replay_invariants.py`，实现 I1（工件还原）、I2（编译可复现）、I4（沙箱隔离）三个不变量的独立检查。

I3（锁一致性）和 I5（manifest 一致性）留到下一批——它们需要并发测试和更复杂的 mock，本批预算内不做。
