# MIS-MEM-010（misconception）

## 正面

【误解】MIS-MEM-010 Rule of Five 只是理论，含裸指针成员的类写个析构就够了
触发说法：有裸指针就写析构，别的用编译器生成的

## 背面

为什么错：只写析构而拷贝仍为编译器生成的逐成员拷贝 → 浅拷贝，两个对象析构同一指针 → double free
反例 1： Rule of Five：析构/拷贝构造/拷贝赋值/移动构造/移动赋值要么全自定义要么全 default，五缺一即埋雷
