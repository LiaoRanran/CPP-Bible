# MIS-STL-001（misconception）

## 正面

【误解】MIS-STL-001 只有 erase（删除）才会让迭代器失效，插入不会
触发说法：我只是 push_back，原来的迭代器还能用

## 背面

为什么错：vector 插入可能触发重分配 → **全部**迭代器/引用/指针失效；不重分配时插入点之后的也失效
反例 1：各容器失效规则不同（list 插入不失效，deque 插入使全部失效），须逐容器查
