# 615 B2 · EV-MATRIX 双实现分歧修复报告

> 承接 B1（`data/ev_matrix_dual_impl_615.md`）。铁律：不改 `evidence/`；不改 `gate_engine.py`。

## 一、一致率变化
| 口径 | 一致 | 适用 | 一致率 |
|---|---|---|---|
| 修复前（第二实现 natural：仅剥 `actual`） | 13 | 19 | **68.4%** |
| 修复后（补官方隐性预处理：再剥 `artifact_sha256` 行） | **19** | 19 | **100.0%** |

- 复算：`python tools/ev_matrix_unbacked_v2.py`（natural）与 `... .compare_aligned()`（修复后）。
- 与历史记录（`_arch_v19/probes/output/p03_meta_verification.out`）**一致**（2a 68.4% / 2b 100%）。

## 二、分歧分类
| 类型 | 条数 | 说明 |
|---|---|---|
| **A · 官方隐性预处理**（规则文档没写、代码做了） | **1**（根因：剥 `artifact_sha256` 行） | 影响 6 张卡 |
| B · 第二实现理解错误 | **0** | natural 忠实实现了**文档**语义，无错 |
| C · 真语义歧义 | **0** | 未发现 |
- 6 张分歧卡（B1 列出）**同一根因**：`EV-CONC-001/002`、`EV-MEM-001/039/042/043`。

## 三、修复动作
- **类型 A** → 写规则定义补全提案：`data/ev_matrix_rule_definition_proposal_615.md`
  （建议把「锚统计输入 = 原文 − `actual:` 段 − `artifact_sha256:` 行」显式写入规则定义文档）。
- **类型 B** → 无（无需修正第二实现主逻辑）。
- 演示：第二实现采用该预处理后（`compare_aligned()`），一致率 **100%**（19/19）。

## 四、仍存在的分歧
- **无**（对齐后 19/19）。但须诚实：100% 是**在补上官方隐性预处理之后**——这正说明
  「规则正确性知识部分埋在预处理里，读文档重写会丢失」。

## 五、口径与边界
- 官方 `gate_engine.py --check` **未重跑**（615 铁律）；官方口径由文档化预处理重建 + 历史记录交叉验证。
- **未修改** `evidence/`；**未修改** `gate_engine.py`；第二实现只做对比，不替换官方。
- 提案执行（写入规则定义文档）需**人审授权**，下批进行。
