# 611 C3 · 论证图碎片化修复影响分析（what-if · 只读）

> 把 C2 的 98 条桥接候选（作为 low 可信度 `mis_to_mis` 边）加入图，看碎片化是否改善。**不**写权威边文件、**不**改 W2 判决。

## 一、连通性 before → after

| 指标 | 加桥前 | 加桥后 | 变化 |
|---|---|---|---|
| 连通分量 | 11 | 7 | -4 |
| 最大分量节点 | 80 | 97 | 17 |
| 最大分量覆盖率 | 66.1% | 80.2% | +14.0% |
| 孤立节点 | 4 | 4 | 0 |
| 加入候选边数 | — | 98 | — |
| 判决变化的节点数 | — | 0 | （应为 0：候选皆 low，不构成击败） |

## 二、结论

- 加入全部 98 条候选后，连通分量从 **11** 降到 **7**，最大分量覆盖率从 66.1% 升到 80.2% —— **碎片化显著改善**（仍非单一连通分量，说明补桥是必要非充分）；
- 判决变化节点 = **0**（=0 符合预期：候选边两端皆 low，W2 同档不构成击败，不翻转任何 IN/OUT）；
- 因此「补桥」对论证**胜负**零影响，只让「谁和谁在同一个论证子图里」更清楚；
- 是否真把候选落库为攻击边、重跑 W2，是**人审权力**（须逐条判断方向 + replay 证据），本工具不改仓。

## 三、口径与边界

- 连通性用忽略方向的 BFS（与 C1 同源）；
- 候选数据来自 `data/bridge_edge_candidates_611.jsonl`（C2 产物，本工具只读它）；
- 本分析是 **what-if**，仓内只留本 `.md`，不碰 `attack_edges_candidates.jsonl` / `grounded_labels_w2.json`。
