# 633 D2 · 文档债清理

> 依据任务0 的「文档债」盘点。原则：**过期数字加标注（不删原文）**、**死链接更新或标 [已迁移]**、
> **矛盾描述只登记不改**（§七.D2）。

## 一、死链接：修了 3 个真断链

| 文件 | 原链接 | 修复为 | 依据 |
|---|---|---|---|
| `References/architecture_架构演进/306_…知识体系.md` L131 | `(ATOM-MEM-PERF-001.md)` | `(../../atoms/mem/ATOM-MEM-PERF-001.md)` | 裸文件名在 `References/` 下无法解析；目标真实存在于 `atoms/mem/` |
| 同上 L132 | `(ATOM-MEM-UNIQUE-002.md)` | `(../../atoms/mem/ATOM-MEM-UNIQUE-002.md)` | 同上 |
| 同上 L133 | `(ATOM-MEM-PERF-003.md)` | `(../../atoms/mem/ATOM-MEM-PERF-003.md)` | 同上 |

修复前该文档 3 处死链；修复后**复查为 0 真实死链**（3 个目标文件 `Test-Path` 均为 True）。

## 二、假阳性（登记，不改）

`References/architecture_架构演进/368_独立红队对抗报告.md` 有 **6 处** `file:///C:/…/tools/*.py`
形式的链接被扫描器判为「死链接」。**实为假阳性**：目标文件（`gate_engine.py`/
`atom_evidence_replay.py`/`golden_lock.py`/`golden_state.json`）**都存在**，是扫描器未处理
`file://` 协议前缀所致。**不修改文档**（记入 §四）。

## 三、过期标注：0 处

任务0 口径（`截至 6XX` 且批次已过）本轮复扫为 **0**。此前若有过期标注，已在历批处理。

## 四、新发现（任务0 之外，登记交人）

1. **`.pytest_tmp/` 残留 652 个目录**：历次 pytest 运行留下的临时目录（含整库副本
   `test_copy_of_repo_dir_*`），**体积可观的磁盘/工作区债**。属 `.pytest_tmp` 未清理；
   建议：加进 `.gitignore`（若未加）+ 收工定期清理（**本批不删，登记**，§零.17 删除改移动）；
2. **`file://` 死链假阳性**：是**扫描器口径**问题（任务0 `scan_docs` 未过滤 `file://`），
   非文档债；建议后续版扫描器补 `file://` 过滤。

## 五、矛盾描述

未做全量矛盾核查（工作量大）。**登记不擅自修改**（§七.D2：可能需人裁决哪个对）。

## 六、诚实登记

1. 仅修 3 个**可确证**的死链接（目标文件已核实存在）；其余一律不动；
2. `file://` 6 处为**假阳性**，未改文档；
3. `.pytest_tmp/` 652 目录**登记不删**（§零.17）；
4. 矛盾描述**未核查/未修改**，如实登记；
5. 修改仅触及 `References/`（**非受控目录**），受控目录零污染。
