# 621 B2 · CI 并发竞态复现报告

> 模式：**dry-run** · 迭代 10 次

## ⚠ 本批未真跑门禁

原因：621 §六.3 硬边界：不跑监工门禁；且 replay 会重写受控目录工件

真跑需显式 `--real`，且应由人（或门禁开放批次）执行。

## 二、历史实测（620 任务1，本批未重跑，仅引用）

| 场景 | 运行 | 命中 | block | 说明 |
| concurrent_observed | 619 独立验收（gate ∥ replay 并发） | 195 | **2** | 2 条 EV-ARTIFACT-FILE-EXISTS 假 BLOCK |
| serial_observed | 620 任务1（串行连跑 3 次） | 191 | **0** | 与冻结基线逐字一致 |

## 三、结论

- **并发**（gate ∥ replay）：**观测到 2 条假 BLOCK**（619 验收实例）。
- **串行**（先 replay 后 gate）：**block=0**，连跑 3 次一致（620 任务1）。
- ⇒ 竞态确实存在，方向性修复（`gate needs: [replay]`，621 B1）**对症**。
- ⚠ **修复后频率为 0 尚未在 CI 实跑确认**（621 不 push，见 §八 人拍板项 4）。
