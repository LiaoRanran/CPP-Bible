# 633 C2 · 他验遗留债清理（移动不删除）

> 纪律：§零.11 **不删改 JSONL 账本**（`data/transparency_log.jsonl` 全程只读）；
> §零.17 **删除改移动**（移动目标 `data/_archive_633/`）。

## 一、检查了什么

| 检查项 | 结果 |
|---|---|
| 锚 `data/vsa/anchor_20260924.json` 与透明日志根哈希一致？ | **❌ 不一致**（锚记 37 条，实际日志 40 条）→ 已处置（见 §二） |
| 透明日志链完整？（`transparency_verify_632.py --check`） | 处置前 ❌「锚不一致」；处置后 ✅ **40 条链自洽，锚一致** |
| VSA 凭证目录孤儿凭证（存在但日志无对应条目） | **7 张**（全部 untracked，见 §三） |
| `data/vsa/` 测试尘埃（`*_test_*` / `*_tmp_*`） | 0（无此类命名文件） |
| 日志引用了但文件缺失的凭证 | **2 处**（dangling，见 §四） |

## 二、锚不一致的处置（重新生成锚，不改日志）

- 现象：`transparency_anchor_632.py --check` FAIL「锚与日志不一致（日志已变）」。
- 根因：**透明日志在本会话前的测试运行中被追加**（提交版 35 条 → 工作区 40 条，
  `+5` 条：索引 35–39，时间 09-24 05:53/10:52/12:29/12:55/13:31），而 632 B1 的锚
  写于 37 条时刻 ⇒ 锚自然失效。（**这是 A2 已登记的「测试有数据副作用」在透明日志上的再现**。）
- 处置（按 §六.C2）：
  1. **旧锚移动**到 `data/_archive_633/anchor_20260924.json.pre_633`（不删除）；
  2. **重新生成锚**（`transparency_anchor_632.py`，不触碰日志）；
  3. 复核：`--check` → **OK 锚与日志一致**；链 **40 条自洽**。
- **未修改透明日志**（append-only，§零.11）。

## 三、孤儿凭证（7 张）→ 移动归档

以下 7 张 `data/vsa/attestation_*.json` **无对应日志条目**（全部为 **untracked**，
即测试/旧流程产生、从未入库）：

```
attestation_20260923T144453Z.json   attestation_20260924T030009Z.json
attestation_20260923T153122Z.json   attestation_20260924T050848Z.json
attestation_20260923T162340Z.json   attestation_20260924T053110Z.json
attestation_20260923T171512Z.json
```

处置：**移动到 `data/_archive_633/vsa_orphans/`**（§零.17，不删除）。移动后文件数校验 = 7。

## 四、登记不处置（dangling 引用）

透明日志有 **2 条**条目引用的凭证文件**已不存在**：

| 日志引用文件 | 状态 |
|---|---|
| `authority_event_chain_629_anchor.json` | 缺失（629 C2 的历史锚，未入库或已清） |
| `authority_chain_anchor_credential_629.json` | 缺失 |

**不处置**：日志 append-only 不能改；这两条属**历史 dangling 引用**，登记交人
（若要闭合，需补回对应文件或在新日志中加 supersede 说明——均需人裁决）。

## 五、诚实登记

1. **未修改透明日志**（§零.11），仅重建了锚；
2. **移动而非删除**（§零.17）：7 张孤儿凭证 + 1 个旧锚移入 `data/_archive_633/`（该目录未入库）；
3. 锚的重建**钉住了含测试副作用的当前日志**（40 条）——根本问题是「测试往生产日志追加」
   （A2 §一 已登记），本批只能恢复一致性，**无法在不改日志的前提下剔除测试条目**；
4. dangling 引用 2 处**登记不修**（需人裁决）；
5. 受控目录 `git diff --quiet -- atoms evidence Examples Book` **exit 0**（零污染）。
