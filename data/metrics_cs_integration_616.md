# 616 A3 · metrics_collector 集成置信序列（报告）

> 铁律：`metrics_collector.py` 不在 CORE_TOOLS（可改）；**不动 27 项扁平 schema**；跑 metrics_collector 是度量采集（允许）。

## 一、新增字段（`mutation_escape_rate` 及 history）
| 字段 | 含义 |
|---|---|
| `cs_alpha` | 置信序列显著性水平（0.05） |
| `cs_lower` / `cs_upper`（+ `_raw`） | 置信序列 **anytime** 区间（**推荐**） |
| `peeking_correction` | `True` = 已用置信序列修正统计偷看 |
| `cp_vs_cs_note` | 口径说明文本 |

**旧字段 `point` / `cp_low` / `cp_high` 全部保留**（历史参考，未删）。

## 二、新旧字段对比（v7）
| 口径 | 下界 | 上界 |
|---|---|---|
| 固定样本 CP（双侧 95%） | 0.0018% | **0.3956%** |
| 置信序列（anytime 95%，**推荐**） | 0.00% | **0.9062%** |

- `cs_upper_raw > cp_high_raw` 成立（anytime 更保守，符合预期）。
- 0 逃逸轨迹对齐 A1/A2（n=1000,x=0 ⇒ CS 0.9856%）。

## 三、对下游消费者的影响
| 消费者 | 影响 | 建议 |
|---|---|---|
| 报告/看板（引用 `cp_*`） | 连续查看下 95% 不成立 | 改用 `cs_*`；或在里程碑终点注明"一次性 CP" |
| `escape_rate_honest_613` / `escape_rate_trend` | 消费 CP 上界 | 下批迁到 `cs_*`（本批不改历史工具） |
| 验收契约（1/1406） | 不变 | 上界引用须标口径（CS/CP） |

## 四、迁移建议
1. **保留双口径**：`cp_*` 历史参考、`cs_*` 推荐；报告**标注引用哪个**。
2. **逐步迁移**：新报告一律用 `cs_*`；历史报告追加一行口径注（不回溯改写数字）。
3. **验收停止规则**：未来批次可用 **CS 停止规则**（达契约上界即合法宣布，无需偷看）。

## 五、边界
- 未删任何旧字段；未改 27 项扁平 schema；未跑监工门禁。
- `cs_*` 的计算实现见 `data/confidence_sequence_impl_616.md`（A1）。
