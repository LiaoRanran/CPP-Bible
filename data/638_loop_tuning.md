# 638 C2 · 闭环规则调参记录

> 生成时间：2026-09-25T09:38:42。工具：`tools/loop_tuning_638.py`。
> 指标来源：`self_observer_637.collect(heavy=False)（实时）`（637 闭环中间产物）。

## 一、改了什么（3 项，逐条含理由与预期）

| 编号 | 对象 | 调前 | 调后 | 为什么改 | 预期效果 |
|---|---|---|---|---|---|
| T1 | `observation_pct` | 30 | **停用** | 观测态 67/67=100% 是 Daubert 表**设计属性**（635 冻结），不是异常；637 把它列为 top1 P1 异常属**误报**（任务0 §2.5 R1） | 消除 1 条 P1 误报，top1 不再被设计属性占据 |
| T2 | `exception_clauses` | 100 | 250 | 227 条是**7 处来源汇总**（635 冻结），单看总数不构成异常（任务0 §2.5 R1'） | 消除 1 条 P2 误报 |
| T3 | `risk_factor.report` | 0.5 | 0.75 | 637 top1「为例外设过期」正是 report 类（成本最低、可立即交付），却被最低风险系数压制 ⇒ 人工赋值失真（任务0 §2.5 R4） | report 类排名上移，低成本可交付建议不再被系统性压低 |

## 二、阈值全表（调前 → 调后）

| 指标 | 调前阈值 | 调后阈值 | 方向 |
|---|---|---|---|
| `grounding_pct` | 50.0 | 50.0 | 越小越异常 |
| `observation_pct` | 30.0 | **停用** | 越大越异常 |
| `pytest_failures` | 5.0 | 5.0 | 越大越异常 |
| `taint_cards` | 5.0 | 5.0 | 越大越异常 |
| `exception_clauses` | 100.0 | 250.0 | 越大越异常 |
| `ahead` | 20.0 | 20.0 | 越大越异常 |
| `tools_per_test` | 1.05 | 1.05 | 越大越异常 |

## 三、调参前后差异（同一份 637 指标）

- 调前检出异常：**4** 项；调后：**2** 项；
- **消除**（误报）：`['exception_clauses', 'observation_pct']`；
- **新增**：`[]`；
- **保留**：`['grounding_pct', 'taint_cards']`。

### 3.1 调前异常

| 指标 | 严重度 | 当前值 | 阈值 |
|---|---|---|---|
| `observation_pct` | P1 | 100.0 | 30.0 |
| `taint_cards` | P1 | 8 | 5.0 |
| `grounding_pct` | P1 | 35.8 | 50.0 |
| `exception_clauses` | P2 | 227 | 100.0 |

### 3.2 调后异常

| 指标 | 严重度 | 当前值 | 阈值 |
|---|---|---|---|
| `taint_cards` | P1 | 8 | 5.0 |
| `grounding_pct` | P1 | 35.8 | 50.0 |

### 3.3 风险系数（T3）

| 风险类别 | 调前 | 调后 |
|---|---|---|
| `report` | 0.5 | **0.75** |
| `tool` | 1.0 | 1.0 |
| `core` | 1.5 | 1.5 |

## 四、诚实登记

1. **未修改 637 的任何工具**（§零：不碰上一批产物）——调优是**覆盖层**：
   本模块自带调后阈值/系数，对 637 的指标重跑检测与打分；
2. `detect_with()` 是 `error_detector_637.detect` 的**同构副本**（阈值参数化），
   两者的规则集一致，仅阈值不同；**重复实现是刻意的**（避免改 637 文件）；
3. T1/T2 的依据来自 635 的**冻结设计属性**，判断「误报」是我方结论，非 637 自认；
4. **只调闭环自己的阈值**，不碰 gate 的判决规则（§零.1 向后兼容）；
5. 调参是**迭代过程**：本轮只改 3 项，R2（无跨轮记忆）/R3（无语义）/R5（缺错误率）
   /R6（健康指标空）**未修**，留给后续批次（交人项）；
6. **指标来源改用实时采集** `self_observer_637.collect(heavy=False)`：
   落盘的 `data/637_observer.json` 在本批曾被其它进程/测试改写（出现 `pytest_failures=82`、
   `tools_total=428` 等**过期或污染**值），据此对比会失真 ⇒ 不采用落盘值；
7. R6 未修的直接后果：**实时采集是轻量的**，`pytest/ruff/mypy` 为 `null`，
   故本轮调参**看不到代码健康类异常**（P0 级），只覆盖结构与治理类指标。
