# 631 D1 · coverage 未跑向量清单 + 探针优先级

- 当前 coverage：**16/35 = 45.7%**
- 从未跑过的向量：**19** 个（其中 **6** 个连探针都没有）
- 优先级：**P0 2 / P1 16 / P2 1**

> 630 C1 还发现一处**向量表缺口**：`M1 删字段` 在向量表里没有对应项（630 D2 跑了 10 条删字段变异，却无处归类）⇒ 见 §三。

## 一、19 个未跑向量逐条清单（按 优先级→难度→ID）

| # | 优先级 | 向量 | 层 | 名称 | risk | 有探针 | 补探针难度 | 难度理由 |
|---|---|---|---|---|---|---|---|---|
| 1 | **P0** | `L1.2` | L1 | 命题多义指代 | high | **否** | 高 | 多义指代需语义/指代消解，模板生成没有判别力 |
| 2 | **P0** | `L8.4` | L8 | 透明日志伪造 | high | **否** | 高 | 跨层组合需多工具联动，成本最高 |
| 3 | **P1** | `L1.1` | L1 | 命题等价改写 | high | 是 | 低 | 有现成工具可驱动（`probe` 字段非 null）⇒ 补一次执行即 |
| 4 | **P1** | `L1.3` | L1 | 命题范围偷换 | high | 是 | 低 | 有现成工具可驱动（`probe` 字段非 null）⇒ 补一次执行即 |
| 5 | **P1** | `L1.4` | L1 | 命题-证据错配 | high | 是 | 低 | 有现成工具可驱动（`probe` 字段非 null）⇒ 补一次执行即 |
| 6 | **P1** | `L2.4` | L2 | 证据凭空伪造 | high | 是 | 低 | 有现成工具可驱动（`probe` 字段非 null）⇒ 补一次执行即 |
| 7 | **P1** | `L3.1` | L3 | 解析歧义 | high | 是 | 低 | 有现成工具可驱动（`probe` 字段非 null）⇒ 补一次执行即 |
| 8 | **P1** | `L3.3` | L3 | 字段注入 | high | 是 | 低 | 有现成工具可驱动（`probe` 字段非 null）⇒ 补一次执行即 |
| 9 | **P1** | `L4.3` | L4 | regex heuristic 绕过 | high | 是 | 低 | 有现成工具可驱动（`probe` 字段非 null）⇒ 补一次执行即 |
| 10 | **P1** | `L5.3` | L5 | 验证者自身被投毒 | high | 是 | 低 | 有现成工具可驱动（`probe` 字段非 null）⇒ 补一次执行即 |
| 11 | **P1** | `L7.2` | L7 | 模板化人审 | high | 是 | 低 | 有现成工具可驱动（`probe` 字段非 null）⇒ 补一次执行即 |
| 12 | **P1** | `L7.4` | L7 | rubber-stamp | high | 是 | 低 | 有现成工具可驱动（`probe` 字段非 null）⇒ 补一次执行即 |
| 13 | **P1** | `L8.2` | L8 | 签名投毒 | high | 是 | 低 | 有现成工具可驱动（`probe` 字段非 null）⇒ 补一次执行即 |
| 14 | **P1** | `L8.3` | L8 | append-only 链断裂 | high | 是 | 低 | 有现成工具可驱动（`probe` 字段非 null）⇒ 补一次执行即 |
| 15 | **P1** | `L2.3` | L2 | 陈旧证据留痕 | medium | **否** | 中 | 证据漂移可用「改证据文件 + 重跑 replay」驱动，需造证据变体 |
| 16 | **P1** | `L4.2` | L4 | 阈值边界歧义 | medium | **否** | 中 | 规则绕过可用变异算子造「绕过某条规则」的样本 |
| 17 | **P1** | `L4.4` | L4 | 规则优先级冲突 | medium | **否** | 高 | 判据冲突需两个判据给出相反结论，需人工构造争议样本 |
| 18 | **P1** | `L7.5` | L7 | 人审覆盖不足 | medium | **否** | 高 | 调度/时序类需外部时钟或并发编排，纯静态难造 |
| 19 | **P2** | `L3.4` | L3 | 解析器特性绕过 | medium | 是 | 低 | 有现成工具可驱动（`probe` 字段非 null）⇒ 补一次执行即 |

## 二、优先级与难度规则（可审）

| 条件 | 优先级 |
|---|---|
| risk=high **且无探针** | **P0**（高风险 + 完全裸奔） |
| risk=high 且有探针（只是没跑过） | P1 |
| risk≠high 且无探针 | P1 |
| 其他 | P2 |

| 难度 | 判定 |
|---|---|
| 低 | 有现成工具（`probe` 非 null） |
| 中 | 无探针但属结构化层，可模板/变异生成 |
| 高 | 需语义判断或外部依赖（逐条见 `DIFFICULTY_OVERRIDE`） |

## 三、诚实登记

1. **难度是人工判断**（`DIFFICULTY_OVERRIDE` 逐条给理由），不是测量值；
2. **优先级只用了 risk + 有无探针两个维度**，没考虑「该向量实际发生过吗」——630 的 `real_event_count = 15` 说明有 15 个向量**真实发生过**，若按「真实发生过」加权，排序会变（本批未加权，避免与 630 口径打架）；
3. **向量表本身不完备**：`M1 删字段` 无对应向量 ⇒ 补全探针前应先补向量表（否则探针无处登记）⇒ 列入交人/后续；
4. 本工具只读：不跑任何探针、不改任何向量表。
