---
id: MIS-UB-015
name: 多个编译器/版本结果一致，说明标准规定了顺序
level: deep
domain: UB
trigger_patterns:
  - "GCC 和 Clang 都一样，所以行为是确定的"
  - "我这台机器几个版本跑下来都一样，可以依赖"
refutations:
  - "实测 GCC 输出 h/g（右→左）、Clang 输出 g/h（左→右）——**两个编译器选了相反顺序**，这恰恰证明顺序未被规定，而不是被规定"
  - "可移植性依赖的是标准给出的**合法结果集合**，不是抽样一致；实现一致随时可能随版本变化（本项目的取样一致只是当前实现的巧合）"
source: 三样板 B（CI Gray-zone 实测：GCC h/g vs Clang g/h）
related_atoms: [ATOM-UB-GRAY-001]
---

# MIS-UB-015 · 多个编译器/版本结果一致，说明标准规定了顺序

**层级**：deep —— 结构性误解，须 ≥2 条独立反例才可能纠偏（认知科学依据：一次纠正不够）

## 触发模式（学习者常这么说 / 这么写）
- GCC 和 Clang 都一样，所以行为是确定的
- 我这台机器几个版本跑下来都一样，可以依赖

## 为什么它不成立
1. 实测 GCC 输出 h/g（右→左）、Clang 输出 g/h（左→右）——**两个编译器选了相反顺序**，这恰恰证明顺序未被规定，而不是被规定
2. 可移植性依赖的是标准给出的**合法结果集合**，不是抽样一致；实现一致随时可能随版本变化（本项目的取样一致只是当前实现的巧合）

## 出处与关联

- 出处：三样板 B（CI Gray-zone 实测：GCC h/g vs Clang g/h）
- 关联原子：ATOM-UB-GRAY-001
