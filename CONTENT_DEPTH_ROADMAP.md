# 内容上台阶整体 Roadmap（CONTENT_DEPTH_ROADMAP）

> 版本：2026-08-30 · 阶段：quality_consolidation · HEAD 基线 `6d7d6e9`
> 目标：不是补工程护栏，而是让 147 章"骨架完整"之上**再上几个台阶**——血肉充盈、有独特深度、有贯穿性。
> 事实源：`STATE.json` / `REPOSITORY_AUDIT_AND_ROADMAP_2026-08-30.md` / `build/metrics.json`。

---

## 0. 现状基线：已经很强，缺口很明确

先把"这本书现在到底处在什么水平"说清楚，避免误判方向。

### 已经很强的（不是台阶，别重复造轮子）

| 维度 | 现状 |
|---|---|
| 主题覆盖 | 147 章 / 16 part，从 C 历史到 C++26，进阶主题全覆盖（TMP `ch68`、表达式模板 `ch72`、内存序 `ch108`、无锁 `ch110`、RCU `ch112`、SIMD `ch155`） |
| 章节深度 | 进阶章普遍上千行（`ch72` 1191 行 / `ch155` 1182 行 / `ch68` 952 行），非"点到为止" |
| 性能数据 | 已实测化（vptr 布局、同步代价 atomic 3.5ns / mutex 7.6ns、转型代价 dynamic_cast 慢 13.6x） |
| 汇编实证 | 已有 53+ 例 GCC15.3 真机 objdump（B4 / P4-1~5 / P5-1~9 系列，覆盖 RTTI/EBO/协程/内存序/完美转发/NRVO/SSO 等） |
| 配套资产 | UB 反例库 15 例、编译矩阵 466 处、WG21 TRACKER、面试题 30 题、术语/交叉引用/Mermaid 全审计 0 错 |
| 练习体系 | 705 题、章中位 5、零练习章 0（Phase APP 已闭环） |
| 质量门禁 | quality 16/16、compile 5/5、CI 八 job 真绿 |

**结论：这本书不缺"广度"、不缺"进阶主题"、不缺"证据起点"。**

### 真正的四个缺口（台阶就建在这里）

| # | 缺口 | 精确指标 | 本质 |
|---|---|---|---|
| G1 | 汇编实证**不成体系** | 485 个 ASM 块中 **391 未锚定（80%）、314 未验证（65%）** | 做了几十个精品点，但没形成覆盖全书的"实证矩阵" |
| G2 | 缺**贯穿全书**的实战项目 | `Examples/` 只有 `chNN_*_ex.cpp` 孤立示例，零贯穿项目 | 知识点是孤岛，读者学完不会"做东西" |
| G3 | 陷阱/反模式**未成体系** | MISCONCEPTIONS 仅 21 处、UB 库 15 例，散点存在 | 缺一本可背诵的"专家守则/陷阱手册" |
| G4 | 代码块**可执行率偏低** | 7530 cpp 块仅 54.9% 含 `main` | 大量代码是"展示"而非"可跑"，读者不能复制即验证 |

> G4 本质是"可验证性债"，已由你拆给弱模型慢慢磨（编译覆盖 55%→80%），本 roadmap 不重复它，但三阶产出须与它协同。

---

## 1. 台阶一：汇编实证矩阵（底层纵深 · 招牌级）

**价值主张**：把"从汇编与对象模型理解 C++"做成**全书主线招牌**——这是市面上绝大多数 C++ 书没有的硬核视角。

**现状**：53+ 例精品实证散落在少数章（RTTI/EBO/内存序/完美转发等），但 485 块里 80% 未锚定、65% 未验证。缺口不在"能不能做"，而在"**没做成全书的体系中轴线**"。

**目标态**：
1. 每个 ASM 块三件套齐全：对应 `_asm_demo/*.cpp` 源 + 真机 objdump 产物 + 正文锚定（引用 + 讲解）。
2. 产出「实证矩阵看板」：一个索引，读者/作者能查到"哪个结论有哪些汇编证据、证据状态"。
3. 形成一条显性的"从机器理解 C++"叙事线（每 part 有 3-5 个标杆实证锚点）。

**分阶段**（按 part 优先级，从"结论最有冲击力"的入手）：
- **P1 内存/对象模型**：vptr/虚表布局、多继承、EBO、SSO、`shared_ptr` 原子计数、`make_shared` 单次分配。
- **P2 模板零开销**：CRTP、表达式模板、`std::forward` 引用折叠、`std::visit`、`std::optional`。
- **P3 并发**：内存序屏障、无锁 CAS、ABA、fence、`std::atomic` RMW。
- **P4 现代特性**：移动语义、完美转发、RVO/复制消除、协程帧布局、`std::ranges` 零成本。
- **P5 其余 part**：按"结论是否有'反直觉机器真相'"筛选，只做高含金量结论。

**工作量**：147 章渐进，最大的一阶。**核心判断（哪些结论值得实证、怎么讲透）是强模型活；照模板写 `_asm_demo/*.cpp`、跑 objdump、归档产物是弱模型可磨的机械活。**

**验收**：未锚定 391→0、未验证 314→0；每块真机 objdump 可复现；「实证矩阵看板」上线并入 INDEX。

---

## 2. 台阶二：贯穿全书实战项目线（应用价值 · 最直观）

**价值主张**：让读者"学完 147 章能写出一个完整的东西"，从"参考书"升级为"教程 + 项目"。

**现状**：`Examples/` 有 `ch115_move_ex.cpp` 等孤立示例，无一条跨 part 的项目主线。

**候选项目**（需选定 1 个主项目 + 可选 1 个副项目）：

| 候选 | 覆盖 part | 难度 | 特色 |
|---|---|---|---|
| 手写 JSON 库 | 03 语言 / 05 OO / 06 模板 / 07 STL / 08 算法 | 中 | 最经典、读者熟悉、能一路用上 move/模板/迭代器/异常 |
| 泛型矩阵/张量库 | 04 内存 / 06 模板 / 07 STL / 14 性能 / SIMD | 高 | 能把表达式模板+内存池+SIMD+缓存优化串成一条线 |
| 无锁队列/对象池 | 04 内存 / 09 并发 / 06 模板 | 高 | 硬核、差异化，但受众偏窄 |
| 迷你日志/配置库 | 03/04/06/10/13 | 低 | 入门友好，演进空间大 |

**推荐首选项**：**手写 JSON 库**为主项目（读者价值最直接、覆盖 part 最广、可演示递归下降 + 模板 + 移动语义 + 异常安全全链路）；**泛型矩阵库**可作后期台阶二的进阶案例。

**分阶段**：
1. 选题 + 里程碑切分（把项目按 part 知识点切成模块，每模块对应若干章）。
2. 骨架（目录结构、公共接口、测试框架，放 `Examples/` 下独立子目录）。
3. 逐 part 落地模块（每 part 正文末尾加「项目实战」小节，链接到对应模块 + 讲清本 part 知识如何用在项目里）。
4. 全文交叉引用 + 单元测试 + 门禁纳入。

**工作量**：中等偏大，但与台阶一可并行（一个纵深、一个广度）。模块骨架由强模型定，模块实现可拆给弱模型磨。

**验收**：主项目可编译可测、每 part 有对应项目模块、正文有「项目实战」交叉引用、零孤立。

---

## 3. 台阶三：陷阱/反模式体系（专家守则 · 沉淀级）

**价值主张**：从"教程"到"可背诵的专家守则"——类似 Effective C++ 的条目化沉淀。

**现状**：MISCONCEPTIONS 21 处、UB 库 15 例、legacy 审计候选（10 unsafe C / 94 裸 new / 101 reinterpret_cast），都是**散点**。

**目标态**：一本成体系的「陷阱手册」——按类组织（内存/生命周期/并发/模板/性能/ABI），每条陷阱四要素：**现象 / 根因 / 防御 / 实证或反例**，并链回正文章节。

**分阶段**：
1. 陷阱清点：从 MISCONCEPTIONS + UB 库 + legacy 审计候选（1380 条）+ 议题里**萃取真正高频、致命、反直觉的陷阱**（目标 50-100 条精选，不堆砌）。
2. 分类沉淀：按上述四要素成文，每条配可复现反例（复用 `Appendix/ub/` 与 `_asm_demo/`）。
3. 交叉链接：正文关键处标注「⚠ 陷阱 N」回链手册，手册回链章节。

**工作量**：中等。清点/萃取需强模型判断（哪些值得收录），反例代码与格式化可拆给弱模型。

**验收**：陷阱手册成体系（>=5 类、>=50 条精选）、每条可复现、正文与手册双向链接。

---

## 4. 跨阶整合（三阶都要做的收口）

三阶产出不能又变成"三个散点"，须汇入统一知识骨架：
- 更新 `INDEX` / `GLOSSARY` / `MISCONCEPTIONS`，把新产出的实证索引、项目模块、陷阱手册纳入导航。
- 维护 `build/metrics.json` 与 `STATE.json`，让"实证覆盖率 / 项目模块数 / 陷阱条目数"成为可门禁的新指标。
- 全程受现有红线约束：**147 章上限不增章、汇编必须 GCC15.3 真机 objdump、不可用特性诚实标注、不注水**。

---

## 5. 分阶段顺序与分工建议

| 顺序 | 动作 | 主导 | 弱模型可磨部分 |
|---|---|---|---|
| 1 | 台阶一 P1（内存/对象模型实证矩阵） | 强模型定结论与叙事 | 照模板写 `_asm_demo/*.cpp` + 跑 objdump + 归档 |
| 2 | 台阶二选型 + 骨架设计 | 强模型 | — |
| 3 | 台阶二逐 part 模块实现 | 强模型定骨架/接口 | 模块实现代码 |
| 4 | 台阶一 P2-P5 批量实证 | 强模型选点 | `_asm_demo` 机械产证 |
| 5 | 台阶三陷阱清点与沉淀 | 强模型萃取 | 反例代码 + 格式化 |
| 6 | 跨阶整合 + 门禁 + 指标化 | 强模型 | 指标/脚本 |

**并行性**：台阶一与台阶二可并行（纵深 vs 广度互补）；台阶三建议在台阶一/二有产出后做（能复用它们的反例与实证）。

---

## 6. 一句话决策

> **先攻台阶一 P1**：它唯一复用你已埋下的 485 ASM 资产、最难、最能拉开差距；台阶二骨架可同步启动；台阶三等前两者有货再沉淀。工程三座大山（覆盖率/备份/单点）维持"弱模型慢慢磨 + 你抽空插 U 盘"即可，不再占主线。

---

_落地时本文件由 `NEXT_LLM.md` 引用为「内容深化主线」，与工程路线图（`REPOSITORY_AUDIT_AND_ROADMAP` / `UPGRADE_PLAN`）并列。_