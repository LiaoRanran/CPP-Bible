# 内容上台阶整体 Roadmap（CONTENT_DEPTH_ROADMAP）

> 版本：2026-09-02 · 阶段：deep_cultivation_30 · 主线见下方「当前主线」节
> 目标：不是补工程护栏，而是让 147 章"骨架完整"之上**再上几个台阶**——血肉充盈、有独特深度、有贯穿性、**有温度有灵魂**。
> 总纲：**既硬又暖** —— 硬货四阶（实证/项目/守则/形态）× 人文一阶。

---

## 当前主线（2026-09-02 起）：30 章真深耕——叙述密度治理

> **优先级高于下方五阶**（§7/§8 的旧排序让位）：五阶回答「加什么」，30 章深耕回答「把已有的凿深」——先深后广。

**为什么（2026-09-02 定调）**：外部锐评 + 本仓诊断共同结论——项目已做到「门禁全绿的形式完备」，但
以书论「深度贫血」：全书纯叙述仅约 32%（全部行口径，含代码块）；`tools/prose_density.py` 实测
正文行口径全书 65.5%、最贫血 20 章平均 48.9%。**停止形式完备推进，转入「少而准」**：30 章真深耕，
深度靠实证一点点凿，模型常识流不出来。

**范式（ch22 auto/decltype 样板章已验证，52.4%→65.5%，commit `dbc1af7`）**：

1. **字段卡→因果叙述**：把「定义 / 历史 / 编译器行为」式字段罗列改写为连贯因果链——每个论断先立判断、
   再给依据（标准条款 / 真机证据 / 一致性论证），段落之间有承接而不是并列填空。
2. **清编造随行**：凡提及编译器符号、预期输出、性能数字——真实存在者保留，不确定者改 `[实现-推断]`/
   `[UNVERIFIED]`/「示意」诚实标注；样板章清掉 `Sema::ActOnAutoDecl` 等编造符号。
3. **实证锚定（可锚处）**：按 NEXT_LLM SOP-1 真机锚定，锚定率只升不降。
4. **格式修复随行**：围栏闭合（```cpp 误闭合）、徽章前缀（`· ++14`→`· C++14`）、破折号误用，
   发现即修，不单开批次。

**30 章名单（`prose_density --top 40` 数据驱动，密度为 2026-09-02 基线）**：

| 类 | 章 | 基线密度 | 备注 |
|---|---|---|---|
| A 机制主线 | ch22 auto/decltype | 52.4%→**65.5%** ✅ | 样板（`dbc1af7`），范式见上 |
| A 机制主线 | ch69 constexpr | 51.2%→**61.0%** ✅ | 第二章程（本次：真机证伪 heavy 递归"指数级"编造 + 反模式/工业案例/易错点/FAQ/性能 字段卡→因果叙述） |
| A 机制主线 | ch119 ranges 深度 | 51.7%→**60.0%** ✅ | 第三章程（本次：最佳实践/㉒.3/㉒.4 字段卡→因果叙述 + ⑭ asm 诚实标注示意） |
| A 机制主线 | ch51 CRTP | 52.0%→**61.9%** ✅ | 第四章程（本次：附录G 20 条工业列表→按动机三类叙述 + 坑/性能/FAQ 字段卡→因果叙述） |
| A 机制主线 | ch52 EBO | 53.8%→**63.7%** ✅ | 第五章程（本次：⑰ FAQ/⑱ 最佳实践/⑲ 性能/㉒.3 踩坑/⑭ WG21/② 前置/③ 后续 七处字段卡→因果叙述） |
| A 机制主线 | ch118 modules | 54.4%→**74.3%** ✅ | 第七章程（本次：根治 ①–⑳ 二十组「`[标准]`/`[经验]`」两行字段卡→因果叙述；0.2 编年/0.4 史料/㉒.1 历史纵深/㉒.3 生产踩坑 转因果；⑲ 补「8.2s/2.1s 为示意、本机实测 1.42×」诚实标注） |
| A 机制主线 | ch71 policy | 55.4%→**64.4%** ✅ | 第八章程（本次：⑪⑫⑬⑭⑯⑰⑱⑲ 八组字段卡→因果叙述；核验 ch43 实为缓存局部性，修正内存池归因；data_sanity 0 ERROR / DRIFT=0 / preflight 11 过） |
| A 机制主线 | ch70 tag dispatch | 56.0%→**65.6%** ✅ | 第九章程（本次：⑨⑪⑫⑬⑭⑯⑰⑱⑲ 九组字段卡→因果叙述；除去死引用 ch57 统一为 ch51；data_sanity 0 ERROR / DRIFT=0 / preflight 11 过） |
| A 机制主线 | ch21 const 家族 | 56.4% | |
| A 机制主线 | ch19 variables | 56.8% | |
| A 机制主线 | ch72 表达式模板 | 57.1% | |
| A 机制主线 | ch50 多继承 | 57.4% | |
| B 性能并发 | ch152 性能模型 | 53.7%→**75.7%** ✅ | 第六章程（本次：附录G 20 条同构工业列表→按"回答哪个问题"三类因果叙述；附录H 数字字段卡→叙述并清编造；㉒.3/⑱/⑰/②/③/0.2/0.4 七处字段卡→因果叙述） |
| B 性能并发 | ch154 缓存优化 | 54.8% | |
| B 性能并发 | ch153 CPU 微架构 | 57.2% | |
| B 性能并发 | ch107 atomic | 59.2% | |
| B 性能并发 | ch110 无锁 | 59.5% | |
| B 性能并发 | ch155 SIMD | 59.6% | |
| B 性能并发 | ch15 profiling | 59.9% | |
| B 性能并发 | ch101 算法理论 | 60.0% | |
| C 源码精读 | ch127 LLVM | 54.6% | |
| C 源码精读 | ch125 libc++ | 54.8% | |
| C 源码精读 | ch133 ClickHouse/Redis | 57.1% | |
| C 源码精读 | ch134 Unreal | 57.3% | |
| C 源码精读 | ch126 MS STL | 59.3% | |
| D 教学骨干 | ch88 optional/variant | 58.5% | |
| D 教学骨干 | ch65 type_traits | 58.6% | |
| D 教学骨干 | ch10 版本矩阵 | 59.3% | |
| D 教学骨干 | ch03 C++98/03 | 57.1% | |
| D 教学骨干 | ch83 map | 60.1% | |

**单章验收标准（全部满足才算过）**：

1. 密度 ≥60%（`python tools/prose_density.py <章文件>`）；
2. 字段卡清零——正文论证位无「定义/历史/编译器行为」式罗列残留；
3. 禁编造——符号/输出/数字真实，或诚实标注（`[实现-推断]`/`[UNVERIFIED]`/示意）；
4. 实证锚定只升不降（`verify_asm_evidence.py` DRIFT=0）；
5. 门禁全过（preflight 11/11 + quality 16/16）。

**节奏红线**：串行或 ≤3 章小批量推进，**每章独立验收、独立提交**——禁止 30 章一波流并行生成
（批量生成即「形式完备主义」复辟，L0 红线）。

---

## 0. 现状基线：已经很强，缺口很明确

### 已经很强的（不是台阶，别重复造轮子）

| 维度 | 现状 |
|---|---|
| 主题覆盖 | 147 章 / 16 part，从 C 历史到 C++26，进阶主题全覆盖（TMP `ch68`、表达式模板 `ch72`、内存序 `ch108`、无锁 `ch110`、RCU `ch112`、SIMD `ch155`） |
| 章节深度 | 进阶章普遍上千行（`ch72` 1191 行 / `ch155` 1182 行 / `ch68` 952 行），非"点到为止" |
| 性能数据 | 已实测化（vptr 布局、同步代价 atomic 3.5ns / mutex 7.6ns、转型代价 dynamic_cast 慢 13.6x） |
| 汇编实证 | 已有 53+ 例 GCC15.3 真机 objdump（B4 / P4-1~5 / P5-1~9 系列，覆盖 RTTI/EBO/协程/内存序/完美转发/NRVO/SSO 等） |
| 配套资产 | UB 反例库 15 例、编译矩阵 466 处、WG21 TRACKER、面试题 30 题、术语/交叉引用/Mermaid 全审计 0 错 |
| 练习体系 | 705 题、章中位 5、零练习章 0（Phase APP 已闭环） |
| 历史 part | part01_history 10 章 8255 行（C 历史 → C++26 版本演进编年 + 特性表） |
| 质量门禁 | quality 16/16、compile 5/5、CI 八 job 真绿 |

**结论：这本书不缺"广度"、不缺"进阶主题"、不缺"证据起点"。它缺的是体系、贯穿性、形态和体温。**

### 真正的六个缺口（台阶就建在这里）

| # | 缺口 | 精确指标 | 本质 |
|---|---|---|---|
| G1 | 汇编实证**不成体系** | 485 个 ASM 块中 **391 未锚定（80%）、314 未验证（65%）** | 做了几十个精品点，但没形成覆盖全书的"实证矩阵" |
| G2 | 缺**贯穿全书**的实战项目 | `Examples/` 只有 `chNN_*_ex.cpp` 孤立示例，零贯穿项目 | 知识点是孤岛，读者学完不会"做东西" |
| G3 | 陷阱/反模式**未成体系** | MISCONCEPTIONS 仅 21 处、UB 库 15 例，散点存在 | 缺一本可背诵的"专家守则/陷阱手册" |
| G4 | 代码块**可执行率偏低** | 7530 cpp 块仅 54.9% 含 `main` | 大量代码是"展示"而非"可跑" |
| G5 | **人文温度缺失** | 历史 part 偏"编年+特性表"；文风无误但冷 | 缺人物、设计哲学、提案故事；缺类比、勾子、翻车实录 |
| G6 | **呈现形态不全** | 表格 1.2 万行但横向对比未体系化；Mermaid 511 块全是结构图（flowchart 452 / pie 56 / classDiagram 41）**零折线图** | 缺逐行语法演示、标准库源码剖析、工业应用摘要、数据图 |

> G4 是"可验证性债"，已拆给弱模型慢慢磨（编译覆盖 55%→80%），本 roadmap 不重复它，但各阶产出须与它协同。

---

## 1. 台阶一：汇编实证矩阵（底层纵深 · 招牌级）

**价值主张**：把"从汇编与对象模型理解 C++"做成**全书主线招牌**——这是市面上绝大多数 C++ 书没有的硬核视角。

**现状**：53+ 例精品实证散落在少数章，但 485 块里 80% 未锚定、65% 未验证。缺口不在"能不能做"，而在"没做成全书的体系中轴线"。

**目标态**：
1. 每个 ASM 块三件套齐全：对应 `_asm_demo/*.cpp` 源 + 真机 objdump 产物 + 正文锚定。
2. 产出「实证矩阵看板」：一个索引，可查"哪个结论有哪些汇编证据、证据状态"。
3. 形成"从机器理解 C++"的显性叙事线（每 part 有 3-5 个标杆实证锚点）。

**分阶段**（按"结论最有冲击力"排序）：
- P1 内存/对象模型：vptr/虚表、多继承、EBO、SSO、`shared_ptr` 原子计数、`make_shared` 单次分配（结论清单已落 `docs/references/P1_memory_object_model_checklist.md`：53 例覆盖大半，4 缺口待补）
- P2 模板零开销：CRTP、表达式模板、`std::forward`、`std::visit`、`std::optional`
- P3 并发：内存序屏障、无锁 CAS、ABA、fence、原子 RMW
- P4 现代特性：移动语义、完美转发、RVO、协程帧、`std::ranges` 零成本
- P5 其余 part：按"是否为反直觉机器真相"筛选

**工作量**：最大的一阶。**核心判断（哪些结论值得实证、怎么讲透）是强模型活；照模板写源、跑 objdump、归档是弱模型可磨的机械活。**

**验收**：未锚定 391→0、未验证 314→0；每块可复现；实证矩阵看板上线并入 INDEX。

---

## 2. 台阶二：贯穿全书实战项目线（应用价值 · 最直观）

**价值主张**：让读者"学完 147 章能写出一个完整的东西"，从"参考书"升级为"教程 + 项目"。

**现状**：`Examples/` 有孤立示例，无跨 part 项目主线。

**候选项目**：

| 候选 | 覆盖 part | 难度 | 特色 |
|---|---|---|---|
| 手写 JSON 库 | 03/05/06/07/08 | 中 | 最经典，递归下降+模板+移动+异常全链路 |
| 泛型矩阵/张量库 | 04/06/07/14 | 高 | 表达式模板+内存池+SIMD+缓存优化串成线 |
| 无锁队列/对象池 | 04/09/06 | 高 | 硬核差异化，受众窄 |
| 迷你日志/配置库 | 03/04/06/10/13 | 低 | 入门友好，演进空间大 |

**推荐**：**手写 JSON 库**为主（读者价值最直接、覆盖 part 最广）；泛型矩阵库作后期进阶案例。

**分阶段**：选题+里程碑切分 → 骨架（接口/测试框架）→ 逐 part 落地模块 + 正文「项目实战」小节 → 全文交叉引用 + 门禁纳入。

**验收**：主项目可编译可测、每 part 有对应模块、正文有「项目实战」回链。

---

## 3. 台阶三：陷阱/反模式体系（专家守则 · 沉淀级）

**价值主张**：从"教程"到"可背诵的专家守则"——类似 Effective C++ 的条目化沉淀。

**现状**：MISCONCEPTIONS 21 处、UB 库 15 例、legacy 审计候选（10 unsafe C / 94 裸 new / 101 reinterpret_cast）都是散点。

**目标态**：成体系「陷阱手册」——按类组织（内存/生命周期/并发/模板/性能/ABI），每条四要素：**现象 / 根因 / 防御 / 实证或反例**，链回正文。

**分阶段**：陷阱清点（从 MISCONCEPTIONS + UB 库 + legacy 候选萃取 50-100 条精选）→ 分类沉淀 → 正文「⚠ 陷阱 N」双向回链。

**验收**：>=5 类、>=50 条精选、每条可复现、双向链接。

---

## 4. 台阶四：人文历史 + 趣味叙事（体温线 · 灵魂级）

**价值主张**：技术书最大通病是"正确而乏味"。这一阶给硬核骨架注入**体温**——有人物、有故事、有钩子，让书既硬又暖、读得下去。

**现状**：part01_history 有版本演进编年，但缺人物与设计哲学；文风严谨但冷，缺类比/钩子/事故故事。

> **红线（与"汇编必须真机"同级）**：人文历史必须**考证真实、附出处**，绝不编人物故事、绝不编提案编年、绝不编造争议。宁缺毋滥。

### 子线 A：人文历史线（内容增量，可考证化）

- **人物志**：Stroustrup（C++ 之父与"语言是表达思想的工具"哲学）、Ritchie/Thompson（C 的诞生）、Stepanov（STL/泛型）、Sutter/Meyers（最佳实践的传统）。
- **设计哲学注**：每个 part 的"为什么这么设计"——背后是权衡、约束、当时的硬件与工程现实。
- **提案斗争史**（真实、有据）：concepts 三度被拒最终进 C++20、modules 近十年历程、coroutine 与 executors 的曲折、Rust 崛起引发的 C++ 自省。
- **争议与骂战**：Linus 炮轰 C++、C vs C++、Rust vs C++——解析争议背后的**真实技术理由**（不站队、讲清双方依据）。

### 子线 B：趣味叙事线（文风改造，强模型主笔）

- **类比钩子库**：为抽象概念建立"搬家 vs 复印""共享单车 vs 私家车"式生动类比，沉淀成统一索引。
- **章首勾子**：每章一句让人想读的开场——真实痛点 / 反直觉事实 / 历史时刻。
- **翻车实录**：真实世界 C++ 事故（缓冲区溢出史上大事件、UB 导致的真实灾难）作为警示故事嵌入相关章节。

**落地方式**：每 part 加「人文引言」；关键特性加「历史注」（哪个提案、谁、当年争什么，附出处）；类比体系统一进 INDEX。

**工作量**：人文历史线中等（弱模型可搜集素材、强模型审定考据）；趣味叙事线最依赖强模型文笔，弱模型易写成套话。

**验收**：每 part 有人文引言、每条历史注有出处、类比索引上线、全书读感"有温度"。

---

## 5. 台阶五：内容形态补全（可读性 · 全面性）

**价值主张**：前四阶解决"深度、应用、守则、温度"，这一阶解决"**讲得清、讲得全、讲得直观**"——用对比表、数据图、逐行演示、源码精读、工业案例五种形态，让知识可读、可查、可循证。

**共同特征**：除"基础语法演示"需可编译外，其余形态**不依赖编译门禁**，是弱模型可大量分解打磨的优质并行线。

**现状（摸底 2026-08-30）**：表格 1.2 万行但横向对比未体系化；Mermaid 511 块全是结构图（flowchart 452 / pie 56 / classDiagram 41 / sequenceDiagram 12）**零折线图**；7530 cpp 块仅 54.9% 含 `main`，逐行注释的教学式演示偏少；标准库源码与工业案例仅散点提及（148 / 151 文件命中关键词，无体系）。

### 子线 A：表格对比体系
- 现状：有表格，缺"横向对比"思维（方案 vs 方案、API vs API、语言 vs 语言）。
- 目标态：按主题产出横向对比表——容器复杂度、智能指针取舍、并发原语适用场景、`vector` vs `deque` vs 自定义、Rust vs C++ 关键语义对照。
- 验收：每 part ≥1 张横向对比表；纳入 INDEX。

### 子线 B：线形图 / 数据图（现为 0）
- 现状：Mermaid 全是结构图，**零折线图/趋势图**，性能-规模、内存-占用等关系无法可视化。
- 目标态：引入 `xychart`（折线/柱状）呈现随规模变化的数据——缓存命中率随矩阵块大小、容器插入耗时随 N、锁竞争随线程数等。
- 验收：≥30 张 xychart；每张数据由示例真实生成，不手编。

### 子线 C：基础语法逐行演示（参照 C 手册范式）
- 现状：大量 cpp 块是"展示"而非"逐行讲透"，缺"每一句为什么这么写"的教学式注释。
- 目标态：参照《C 语言极致详解手册》（用户已备）的逐行注释风格，为高频语法/惯用法建立"演示体"。
- 验收：核心 30 个语法点/惯用法有逐行注释演示；覆盖率可门禁。

### 子线 D：标准库源码逐行剖析
- 现状：仅有散点提及，无逐行精读，缺"从标准库实现看 C++ 工程技艺"的独特视角。
- 目标态：遴选优秀实现精读——`vector` 扩容/移动、`string` SSO、`optional` 存储、`shared_ptr` 控制块、`function` 小对象优化、`sort` introsort、`visit` 派发等（以 libstdc++/libc++ 为参照，逐行标注"为什么这么写"）。
- 筛选红线：只剖析**设计意图明确、可本地验证**的实现；用"精简复刻 + 逐行注"呈现工程思想，绝不整段搬运受许可证约束的源码。

### 子线 E：真实工业应用摘要
- 现状：散点，无 case study 体系。
- 目标态：为关键特性配"工业应用摘要"——谁在用、怎么用、带来什么（LLVM / Chromium / 游戏引擎 / 高频交易 / 嵌入式 RTOS 等），每篇 200-400 字 + 出处。
- 筛选红线：摘要必须真实可溯源，不编造使用场景。

**分工**：全阶弱模型可大量磨（搜集素材、写演示、画图表、跑示例生成数据）；强模型定范式与审定——尤其源码剖析的"工程思想"解读、工业摘要的真实性、对比表的结论口径。

**验收**：五子线各达标，五类形态统一纳入 INDEX 与门禁指标。

---

## 6. 跨阶整合（五阶都要做的收口）

- 更新 `INDEX` / `GLOSSARY` / `MISCONCEPTIONS`，纳入实证索引、项目模块、陷阱手册、人文历史注、类比索引、**对比表/图表/演示/源码剖析/工业摘要**。
- 维护 `build/metrics.json` 与 `STATE.json`，让"实证覆盖率 / 项目模块数 / 陷阱条目数 / 人文注条数 / **对比表数 / xychart 数 / 源码剖析数 / 工业摘要数**"成为可门禁的新指标。
- 红线：**147 章上限不增章**（人文/历史/故事/形态一律嵌入既有章节，不另开新章）、汇编必须真机、不可用特性诚实标注、不注水、人文历史必须考证真实、工业应用必须真实可溯源、源码剖析不整段搬运受许可证约束的源码。

---

## 7. 分阶段顺序与分工建议

| 顺序 | 动作 | 主导 | 弱模型可磨部分 |
|---|---|---|---|
| 1 | 台阶一 P1（内存/对象模型实证矩阵） | 强模型定结论与叙事 | `_asm_demo/*.cpp` + objdump + 归档 |
| 2 | 台阶二选型 + 骨架设计 | 强模型 | — |
| 3 | 台阶四子线 A 素材搜集（人物/提案/争议，附出处） | 强模型考据 | 弱模型搜集原始素材供审定 |
| 4 | 台阶五内容形态补全（表格/图表/演示/源码/工业摘要） | 强模型定范式 | 弱模型大量磨（搜集/演示/图表/生成数据） |
| 5 | 台阶二逐 part 模块实现 | 强模型定骨架/接口 | 模块实现代码 |
| 6 | 台阶一 P2-P5 批量实证 | 强模型选点 | `_asm_demo` 机械产证 |
| 7 | 台阶三陷阱清点与沉淀 | 强模型萃取 | 反例代码 + 格式化 |
| 8 | 台阶四子线 B 趣味叙事（类比/勾子/翻车） | 强模型主笔 | 初稿供润色 |
| 9 | 跨阶整合 + 门禁 + 指标化 | 强模型 | 指标/脚本 |

**并行性**：台阶一与台阶二并行（纵深 vs 广度）；台阶四子线 A（考据）与台阶五（形态补全，不依赖编译）可与其余并行；台阶三与台阶四子线 B 宜在前两者有产出后做（复用实证与反例）。

---

## 8. 一句话决策

> **先攻台阶一 P1（内存实证矩阵）**，复用你已埋下的 485 ASM 资产、最难、最差异化；**台阶二骨架**同步启动；**台阶五内容形态**尽早铺开给弱模型磨（不依赖编译、最快见可视化增量）；**台阶四子线 A 人文考据**独立并行（给书长期供血）；陷阱与趣味叙事等前两者有货再沉淀。工程三座大山维持"弱模型磨 + 你抽空插 U 盘"，不再占主线。

---

## 9. 远期储备（后话，不占当前主线）

- **part0 / 末尾 Rust 章节**：C++ 与 Rust 的对照是极佳的教学切口，但属结构性扩章、工程量与红线影响大，**暂缓**，待五台阶有实质产出后再评估。
- **外部参考范式手册（本机）**：用户用其他 Agent 产出《C 语言极致详解手册》《Linux 内核极致详细手册》，位于本机 `docs/references/external/`（已 `.gitignore`，未随仓库推送）。台阶五子线 C（逐行语法演示）以 C 手册为范式、子线 D（源码剖析）以 Linux 内核手册为范式，不重复造轮子。

---

_落地时本文件由 `NEXT_LLM.md` 引用为「内容深化主线」，与工程路线图（`REPOSITORY_AUDIT_AND_ROADMAP` / `UPGRADE_PLAN`）并列。_