# 安全策略

## 支持范围

本项目为公开技术文档仓库（含可编译示例与工具链）。以下均属支持范围：

- 章节内容的事实性错误（标准/提案/版本/ABI/性能数据）→ [内容勘误模板](.github/ISSUE_TEMPLATE/content_errata.md)
- 工具链 / 门禁 / CI 缺陷 → [Bug 报告](.github/ISSUE_TEMPLATE/bug_report.md)
- 示例代码中的安全缺陷（缓冲区溢出、注入、未定义行为演示不当）→ 按 Bug 报告上报

## 上报方式

- 公开问题：直接使用 GitHub Issues（带对应模板）。
- 疑似**敏感漏洞**（如提交历史中的凭据泄漏、供应链投毒）：请勿公开，改为私信仓库维护者或创建 **private security advisory**。

## 提交前自检（防止机密入库）

- 严禁把任何令牌 / 私钥 / 口令写入仓库，包括临时脚本与 `fix_*.py`。
- 提交前运行 `git diff --check`；若怀疑历史已存在泄漏，先暂停 push 并上报。
- 本仓库未安装 gitleaks 前，以下特征应人工排除：`ghp_`/`github_pat_`/`AKIA`/`-----BEGIN` 等。

## 依赖与供应链

- Python 依赖建议以锁定文件安装；第三方示例（Qt/Boost 等）仅作教学引用，不纳入运行时依赖。
- 若发现依赖投毒或恶作剧包，立即报告并锁定受影响版本。

## 安全更新

- 安全相关修复走常规 PR，并在 CHANGELOG 的「安全」小节标注 CVE / 影响范围 / 修复版本。