feat: 版式统一战役收官（Phase 7）+ 编译门禁 REAL 桶收尾批次

一、编译门禁收尾（承接 09-03 REAL 桶分诊）
- ch70 标签分发整章骨架代码修复：12 个 template-at-block-scope/语句散
  命名空间失败全清（骨架体 { /* */ } 闭合、跨块引用自包含化、顶层调用
  main 化），48 blocks 0 fail；真机复验三处演示输出全对
- ch71/ch81 小推进 4 块（SafeQueue::push / Guarded::op / string_view
  print、rev 闭合），真机输出复核通过
- PROGRESS 新增 Phase 6（编译门禁实测状态）：全绿章台账、残留失败
  分类（判定非缺陷）、真截断形态学与修复法、SKIP 规则台账

二、版式统一战役 Phase 7（2026-09-04 收官）
- 新工具 tools/codeblock_style.py：围栏标签别名归一（c++/cc/cxx→cpp、
  output/console→text、sh→bash）+ 裸围栏推断（绝不自动判 cpp，防指标
  漂移、防源码摘录误入门禁）+ 行尾注释显示宽度对齐（CJK=2，>76 列
  跳过）+ --titles 题注（默认关闭）
- 全 16 part 147 章铺开：标签 303 处（含 40 处代码性裸围栏人工定点
  text）+ 对齐 4639 处；裸围栏 S3 全书 329→0 清零（--list-bare 实测 0）
- 修复工具 CRLF bug：split 残留 \r 污染 30 文件（ch50 行数翻倍）→
  splitlines() + newline.join 字节级保留行尾，从 HEAD 还原重做后门禁
  全恢复；cpp_blocks 7534→7527（README 同步），d5_coverage 127 不变

三、围栏解析器硬化（题注迁移前置，防假全绿）
- 6 个按 ^```cpp$ 精确匹配的工具改为接受围栏 info 串并跳过 info 行：
  chapter_compile_check / chapter_lint / compile_run_sanitize_pipeline /
  run_cpp_assertions / verify_exercises / exercise_dup_guard
- example_exercise_audit / normalize_comments 的 fence_lang 改取首
  token（带 title 的块不再漏计漏处理）

四、围栏题注迁移试点（TEACHING §8.4，3 章 184 块）
- ch22/ch24/ch70：围栏加 title="示例 N · ★…☆"，正文题注行保留
  （badge/主题/版本标注不搬进 title）；命中率 184/188
- 连带修复 ch24 块 #33 真截断（struct Packet 缺 }; 闭合），57/0

五、门禁状态
- quality 全绿：whitespace 0 / consistency 147 章 100/100 /
  gen_metrics 事实源对账 ✅；编译对照组（ch126/ch125/ch127/152~155/
  157/158）0 回归
- .gitignore 增补 _rw_tmp/（分诊临时目录，triage2/dumpblocks 等）
- 台账：PROGRESS Phase 7 新增（7.1 工具护栏 / 7.2 铺开表 / 7.3 收官 /
  7.4 题注迁移待逐章铺开）；CHANGELOG 增 2026-09-03 / 09-04 节
