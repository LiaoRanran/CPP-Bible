# 369 放权地基接线 · 第一批验收报告

> 日期：2026-09-12 · 执行者：CodeBuddy Agent（ds v4.1 flash）
> 依据：`References/architecture_架构演进/369_执行提示词_架构收口与放权地基接线.md`
> 纪律：全部数字以**实跑**为准（不抄任务书）；不 push（push 权在维护者）。

## 零、第〇步 状态对齐（基线）

| 项 | 实测值 |
|---|---|
| 起点 commit | `e427d37`（ahead 13 条未推送） |
| replay Windows | confirm=54 refute=0 infra_error=0（EXIT=0） |
| replay WSL | confirm=54 refute=0 infra_error=0 |
| gate | 规则 36 条 · block=0 warn=8；开 `--advice` 命中 19（**全为** PED-MISCONCEPTION 误报） |
| golden | sync 后 atoms=26 evidence=54 verified=26（human=23 / red_team=3 / machine=0）replay_confirm=54 |
| pytest | 97 passed（`.venv/Scripts/python.exe -m pytest tests/ -q`） |
| 盘上资产 | atoms 26 · evidence 54 · misconceptions 78 · `_atom_*.cpp` 夹具 50 |

**与任务书的差异（以实测为准）**：未推送提交 13 条（任务书写 10，轮间用户已入库 327–370 等三个提交）；
replay 三分类 + `run_match_file` 支持已在 `35f1c94` 落地；pytest 基线 97（非旧记录的 27）；
`run_match_file` 形态卡 **6 张**（任务书写 4 张）。

## 一、任务1 文档实现状态对账（P0-1）→ `73fb8a6`

- 文件：`References/architecture_架构演进/353_...v1.0....md`、`357_...v1.1....md`、`364_....md`
- 353：新增「实现状态对账」节（21 项三栏：存在/不存在-未采纳/已被取代）；§7.1 权限表改"流程约定，未技术强制"
  并列出仅有的 3 处技术锚；§7.3 安全模型按实况重写（无命令白名单/无编译沙箱）；§9.1 迁移脚本脚注
- 357：新增 14 条修订落地状态对账表；修订 1/2/3/8/10/12 就地加状态块（CLAIM-STRENGTH 明确**拒绝实现**）
- 364：顶部状态声明 + §8.3 检查清单后置 + 目录 `schemas/` 标注未建
- 证据：`--list` 无 SCHEMA-VALIDATION/CLAIM-STRENGTH/CONTENT-SAFETY/dal_proposed；无 `schemas/`；无 `.git/hooks/pre-commit`

## 二、任务2 PED-MISCONCEPTION 兼容复数（P1-4）→ `9bafb1c`

- 文件：`tools/gate_engine.py`、`tests/test_gate_engine.py`
- 改动：新增 `_misconception_gap()`——`pedagogy.misconception` / `pedagogy.misconceptions` / 顶层
  `misconceptions` 三处任一非空即过；三处皆空才报
- **规则数：36 → 36**（改实现，不增规则）
- 毒样例阴阳：pytest +4（阳：三处皆空→报；阴 ×3：单数 / 复数 / 顶层→不报）
- **advice：19 → 0**（PED-MISCONCEPTION 归零）；pytest 97 → 101

## 三、任务3 原子身份唯一（P1-5）→ `1641ab7`

- 文件：`tools/gate_engine.py`、`tools/poison_drill.py`、`tests/test_gate_engine.py`
- **规则数：36 → 37**（新增 block 规则 `ATOM-ID-UNIQUE`：stem==id + id 全库唯一）
- 毒样例：**P8**（ATOM-ZZ-TMP-001 stem≠id + 同 id 第二份）→ 拦截者 `ATOM-ID-UNIQUE` ✅；阴性对照放行 ✅
- pytest 101 → 102；存量 26 颗预检 stem==id 全一致、id 唯一 → block=0 不变

## 四、任务4 poison 覆盖率口径统一 + 豁免台账（P1-2）→ `7a04416` + `311a123`

- 文件：`tools/poison_drill.py`、`tools/poison_exemptions.yaml`（新增，27 条豁免）
- 覆盖率分母：源码正则去重数（32）→ **注册规则数**（`gate_engine.RULES`）
- fail-closed：未覆盖且未登记豁免 → 退出码非 0（经 cppbible quality 使 CI 红）；台账**零依赖解析**（不引 PyYAML）
- 毒样例：+**P9**（verified 绑 verdict=refute → S2）、+**P10**（期望值硬编码进夹具字面量 → S3）
  → **11/11 全拦截 + 阴性对照放行**
- 覆盖率实测：10/37 覆盖 + 27 豁免 = **0 欠账**（EXIT=0）
- 357 §16.3 勘误（原"12 条 / 36 规则 33%"与实跑不符）

## 五、任务5 工作树清洁校验（P1-3）→ `456344d`

- 文件：`.github/workflows/ci.yml`（Evidence Replay 步**之后**新增 Worktree Cleanliness 步）、`tools/prepush_check.py`
- `ci_local_precheck.py` 无需改动：它按 ci.yml 解析步骤执行，新步自动入列（实测 `--list` 含该步）
- **范围收窄（实测依据，非擅改）**：任务书写 `Examples/`，实测 Examples/ 根下有 437 个 Book 示例
  `.cpp` 存在历史行尾漂移（工作树 CRLF vs 索引 LF）——全目录检查会让 WSL 本地预检**恒红**；
  收窄为 `Examples/atoms/`（replay 等工具的唯一写入范围），理由写入代码注释与提交信息
- 验证：WSL 实跑该步 rc=0；prepush 正例绿 / 反例红；ci.yml YAML 合法（quality 38 步）

## 六、任务6 红队报告锚必填 + KPI 未启用标注（P1-6）→ `4f87c0d`

- 文件：`365B_....md`、`367_....md`、`353_....md`、`357_....md`
- 365B（红队模板）纪律节：**🔴 阻断必填锚**（可复现命令/输出、工件符号或行号，且**当次可复核**）；
  无锚条目只计 🟡 建议、**不计强证伪**
- 367 §三：对立 KPI 表加"未启用指标"裁定 + "强证伪"计数口径（防"把弱问题说成方向级"博弈）
- 353 §8.1：指标表新增"采集状态"列，14 项逐条实测标注（**已启用 5 / 未启用 9**）
- 357 修订9：⏳/❌ 各行统一称"未启用指标"；361 检索无百分比 KPI（0 命中），无需标注

## 七、任务7 数字止漂移 + golden 机器勾稽（P1-8）→ `21be9db` + `09c65cd`

- 文件：`tools/golden_lock.py`、`tests/test_s1_s6.py`、`tools/golden_state.json`、`353`、`365`
- `commit` 字段**首次填实**（原恒为空）：新增 `_git_provenance()` 机器填写 (HEAD 短哈希, 工作树是否脏)
- `check --accept`：**接受即同步基线**（消除"接受了但基线仍旧、下轮重复报同一恶化"）+ 审计记录新增
  `metrics_after`（机器勾稽，与当期测量对账）
- 353 对账页去硬编码（"36 条"→"以 `--list` 实跑为准，引用前必须先实跑"）；365 加时点口径
- 实测：sync 后 `commit=4f87c0d dirty=True`；pytest 102 → 103

## 八、任务8 S3 覆盖 run_match_file 形态（P1-12）→ `d808db1` + `4bc5f27`

- 文件：`tools/gate_engine.py`、`tests/test_gate_engine.py`、`353`
- 新增 `_s3_expected_segments()`：覆盖 literal 与 `run_match_file + run_match_keys` 两种 actual 形态；
  文件形态**只取 value 段**（不拿 key 名比对——key 合法出现在 printf 格式串里）、**只查本卡声明的 keys**
- 规则数：37 → 37（改实现，不增规则）
- 毒样例阴阳：pytest +2（阴：格式串+变量不误报；阳：字面量硬编码 → block 且标注来源）
- 存量 **6 张** run_match_file 卡**零误报**（block=0 不变）；pytest 103 → 105

## 九、第一批收尾全量门禁（2026-09-12 实跑）

| 门禁 | 结果 |
|---|---|
| replay Windows | confirm=54 refute=0 infra_error=0（EXIT=0） |
| replay WSL | confirm=54 refute=0 infra_error=0；工件已还原（`.asm` sha 不变） |
| gate --check | **37 条** · block=0 warn=8（EXIT=0） |
| gate --run --advice | advice=**0**（19→0） |
| poison_drill | **11/11**（EXIT=0）；覆盖率 10/37 + 豁免 27 |
| pytest | **105 passed** |
| golden_lock check | 恶化 0 · 改善 0 |
| consistency | 100/100 |
| gen_metrics --check | ✅ 全部文档数字与事实源一致 |
| cppbible quality | **23 passed / 0 failed** |
| WSL ci_local_precheck | **32 步 · 失败 0**（含新增 Worktree Cleanliness） |

## 十、交付清单（本批 12 条提交）

| # | commit | 说明 |
|---|---|---|
| 1 | `5e51622` | 第〇步 golden sync（25/52 → 26/54） |
| 2 | `73fb8a6` | 任务1 353/357/364 实现状态对账 |
| 3 | `9bafb1c` | 任务2 PED-MISCONCEPTION 兼容复数 |
| 4 | `1641ab7` | 任务3 原子身份唯一 block 规则 |
| 5 | `7a04416` | 任务4 poison 口径统一 + 豁免台账 + S2/S3 毒样例 |
| 6 | `311a123` | 任务4 357 §16.3 勘误 |
| 7 | `456344d` | 任务5 工作树清洁校验（CI + prepush） |
| 8 | `4f87c0d` | 任务6 红队锚必填 + KPI 未启用标注 |
| 9 | `21be9db` | 任务7 golden accept 机器勾稽 + commit 填实 |
| 10 | `09c65cd` | 任务7 数字止漂移（353/365） |
| 11 | `d808db1` | 任务8 S3 覆盖 run_match_file 形态 |
| 12 | `4bc5f27` | 任务8 353 对账页同步 |

## 十一、未完成项与悬而未决（**需人裁，未自行拍板**）

1. **push**：本批结束后本地领先 origin **25** 条提交；按铁律 7，push 权在维护者。
2. **437 个 `Examples/*.cpp` 行尾漂移（本批新增发现）**：工作树 CRLF vs 索引 LF；Windows git 因 stat
   缓存不可见、WSL/CI checkout 可见。是否 `git add --renormalize` 统一入库需裁决（非本批产物，本批未动）。
3. **4 颗 `pedagogy: >-`（折叠字符串）原子**（ALLOC-002 / LEAK-002 / PERF-003 / PERF-004）：
   `isinstance(ped, dict)` 为假 → PED-MOTIVATION / PED-SOCRATIC / PED-PREDICT-FIRST 三条 advice
   规则对它们**静默跳过**（已知盲区，本批未扩权，仅在代码注释与报告中留痕）。
4. **`tools/book_atom_sync.py` 未跟踪**且未登记于 `cppbible.py`（非本批产物）——入库或删除需裁决。
5. **第二批 M 项未启动**（任务书要求"维护者另开窗口授权"）：CI git 作者白名单 + CODEOWNERS +
   fixture-diff ⇒ redteam_review 联动；tools/ quality 拆组 + 退役审计。
6. **MCC 首次真实落地未启动**：任务书排在第一批之后，选"下一颗尚未生产的新原子"（CONC 已满 3 颗）。

## 十二、口径修正记录（任务书 vs 实测）

| 任务书表述 | 实测 | 处理 |
|---|---|---|
| 未推送 10 条 | 13 条 | 以实测为准 |
| `run_match_file` 形态 4 张卡 | 6 张（CONC-001..006） | 按 6 张实现与验证 |
| poison 覆盖率分母 36 | 源码正则 32 / 注册规则 37 | 分母统一为**注册规则数** |
| 工作树校验含 `Examples/` | 437 文件历史漂移 | 收窄为 `Examples/atoms/`（理由入注释） |
| （未写基线）pytest | 97 | 记录为基线，收尾 105 |
