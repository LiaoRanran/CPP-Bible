# tool_assumptions_draft.md（草稿，建设者验收后收编为 docs/kernel/tool_assumptions.md）

> 每条隐性假设：ID | 假设内容 | 被哪个攻击违反 | 当前是否有检查 | 建议。
> 来源：阶段一盲测 + 阶段二校准（371 W 系列直接并入）。

| ID | 假设内容（工具的隐性前提） | 被哪个攻击违反 | 当前检查 | 建议 |
|---|---|---|---|---|
| T-A01 | artifact_assert 的断言语义由攻击者按"与 claim 相关的结构证据"填写 | B2-R1（contains "main"） | 无（只查断言命中，不查断言对象相关性） | 断言文本必须交验 fixture 源码标识符；通用符号禁锚 |
| T-A02 | `actual`/`.out` 中呈现的数字即"观测"，卡散文引用的读数即观测的子集 | B3-R2（未声明键+散文编造） | 部分（S3 只扫声明键值域；run_match 只核声明键） | `expected`/`falsification` 数字须能在声明键中找到；否则 warn→block |
| T-A03 | "编译器零诊断/无警告"由 `compile_rc==0` 即可证明 | B6-R3（-Wall 警告 rc=0） | 无（compile_rc 不看 stderr） | "零诊断"字样 ⟹ 命令必须含 `-Werror` 的判据规则 |
| T-A04 | `human:`/`machine:`/`redteam:` 前缀之后是"真实可信的署名者" | B9-G2a/b/g（空名/空格名） | 无（`startswith` 前缀） | 前缀后必须非空 + 匹配维护者白名单（git 作者/文件） |
| T-A05 | `artifact` 与 `artifact_sha256` 描述的是"本卡命令亲自编译生成的那个产物" | N4-R5（borrow 他卡工件） | 弱（只要求命令行文本包含 artifact 路径，任意 argv 可造假） | 生成命令须为编译器调用且输出=artifact 路径；或 artifact 生成轨迹哈希 |
| T-A06 | multi-TU 夹具的结论由"被锚的唯一 artifact + .out"完整承载 | B7（R2+R5 组合） | 无（单 artifact 字段） | 371 W1：`artifacts[]` 或多产物留痕字段（建设者三选一决策中） |
| T-A07 | relations 只有一种合法写法（dict 形态） | N1-G3（mapping-form 环静默） | 无（规则只读 `type`/`target` 键） | 解析器/规则统一双写法；mapping-form 也进 DAG/前置校验 |
| T-A08 | EV id 全局唯一（与 ATOM 同级别身份） | N2-N1b（同 id 双卡遮蔽） | 无（EV 无唯一性规则） | EV id 唯一 + stem==id（对齐 ATOM-ID-UNIQUE） |
| T-A09 | 行为不随环境（核数/用户名/日期/路径/CWD）漂移 | B5-R6（nproc/__DATE__/USERNAME） | 无（run_match 全等比较） | 环境依赖值禁入 run_match_keys；或要求"本机+CI 双绿"契约 |
| T-A10 | `matrix.compiler` 声明的多编译器有真实双平台/CI 工件支撑 | N3-M11（P7 锚自满足） | 弱（锚正则含命令自证项） | 去命令自证锚；多编译器声明须附 Examples/...out 双份或 CI run 号 |
| T-A11 | P5 falsification "有量化值"即"可判别" | B8-G5（同源自比含数字） | 部分（只查 `\d`） | 量化值须来自两个不同观察对象；同源自比 warn |
| T-A12 | pipeline 假定 `status_history`/`by` 是人真打的 | B9/G2（前缀伪造） | 前缀级 | 结合 T-A04 白名单 + git author 钩子（CI 侧） |
| T-A13 | 卡的文件编码为 UTF-8 LF、无 BOM | M21/M22（CRLF/BOM 容忍） | 容忍（不报） | 规范化检查（369 A1 已列行尾债，非本洞新增） |
| T-A14 | 折叠块 `|`/`>-` 的内容会被解析为结构化字段 | N5-supp1（pedagogy 折叠→字符串） | 无（dict 检查静默跳过） | 解析器对 `>-`/`|` 后的对象字段仍做 dict 归一，或规则显式处理非 dict |
| T-A15 | replay `command` 只含编译器/构建类可执行程序 | N4-R5（python/cp 任意 argv） | 无（白名单不存在——353 已如实重写 §7.3） | 至少限制：非编译器程序（python/cp/…）须登记 allowlist 或禁用于 artifact 生成路径 |

> 注：T-A03（W3）、T-A06（W1）与 371 建设者自述一致，本表给出**已实证的 payload** 而非仅理论。