# 攻击面分类学（两套口径并列与对齐状态）

> 版本：v1.0（2026-09-14，479 任务 5）
> 背景：472 收尾时发现**同名异义**——`tools/poison_drill.py` 的 A1–A11 与
> `References/architecture_架构演进/409_*.md` 原表的 A1–A10 使用同一编号但语义不同。
> 本文件是**对齐状态的单一记录点**：任何引用「攻击面 A*」的文档，必须注明用的是哪套口径。

---

## 1. 为什么会有两套

| 口径 | 出处 | 用途 | 现状 |
|---|---|---|---|
| **代码口径（A1–A11）** | `tools/poison_drill.py` 的 `ATTACK_TYPES` / `ATTACK_TYPE_LABELS` | **实际执行**：毒样例归类、`--by-type` 覆盖率、`tools/poison_surface_map.json` | 在用（472 起含 A11） |
| **409 原表（A1–A10）** | `References/architecture_架构演进/409_阙疑对抗防御体系化_*.md` | 调研期分类学（描述当时的攻击面清单与处置状态） | 历史文档，**未随代码演进更新** |

409 原表当时是「对某时刻存量风险的快照」，随规则演化，编号被代码侧沿用时语义发生了迁移
——这不是 bug，但**同名异义**会让跨文档引用失真（例：410 提示词按 409 口径写的 P→A 映射表）。

---

## 2. 代码口径（A1–A11，实际执行）

标签为**成员样例归纳**（非 409 逐字）；计数以 `tools/poison_surface_map.json` 为唯一真相源
（`python tools/poison_drill.py --by-type` 打印该台账快照）。

| 编号 | 标签（归纳） | 成员样例举例 |
|---|---|---|
| A1 | 记录层伪造：状态/身份/绑定不实 | P1 假论断（verified 无证据）、P13 空名签收、P17 证据 id 重复、P58 签收与 git 作者不符 |
| A2 | 声明-实现脱钩：工件/证据与声明不符 | P2 过期工件、P16 借工件（cp）、P20 `-o` 目标≠artifact、P57 cat 式证据 |
| A3 | 断言无判别力：恒真/自证/通用符号/全样板 | P3 缺反例、P4 自证断言、P15 通用符号、P47 全样板 `contains_any` |
| A4 | 时序穿链：编译后覆写、留痕比夹具旧 | P33/P34 编译后覆写、P38 `.out` 比夹具旧 |
| A5 | 环境量污染：机器/时钟量进读数键或断言键 | P42 `nproc` 进读数键、P44 环境量进断言键 |
| A6 | 解析走私：缩进/重复键/全角键绕过 YAML 语义 | P36 重复 verdict 键、P37 全角键、P43 缩进走私 |
| A7 | 关系图失配：环、矛盾、未知关系类型 | P18 mapping-form 环、P29/P30 矛盾、P55 `refutes`、P56 未知类型 |
| A8 | 间接注入：注释/知识库内容伪造出处 | P21 注释伪造出处、P39 注释伪造符号 |
| A9 | 编译器配置逃逸：零诊断判据未配 `-Werror` | P11 零诊断缺 `-Werror` |
| A10 | 供应链与工件完整性：工具冒充、非编译器产出、快照幂等 | P40 工具冒充、P41 非编译器产出、P51 快照幂等 |
| A11 | 并发与可用性：僵尸锁 DoS、活锁误接管 | P45 僵尸锁、P46 活锁 |

## 3. 409 原表（A1–A10，历史快照）

| 编号 | 409 定义 | 409 当时的处置 |
|---|---|---|
| A1 | 声明-实现脱钩（漂亮声明+假工件） | 已修（EV-ARTIFACT-PRODUCER + P15/P16） |
| A2 | 断言恒真（写死的字面量/自比） | 已修（`EV-ASSERT-DISCRIMINATOR` + P4/P6） <!-- doc-lint:ignore 409 原表逐字引用 --> |
| A3 | 工件借用/覆盖（cp/mv 其他工件） | 核实中（P1a 任务 0） |
| A4 | 注释伪造（夹具注释里写符号名） | 已修（`_strip_comments` + P15） |
| A5 | 通用符号滥用（main/call/ret 哪都命中） | 已修（EV-ASSERT-SYMBOL-MAPPED + P15） |
| A6 | 未声明键（`.out` 有断言没声明的键） | warn 级（EV-OUT-UNDECLARED-KEY + P14） |
| A7 | 符号映射缺失（断言符号不在工件里） | 部分可判（EV-ASSERT-SYMBOL-MAPPED） |
| A8 | **间接注入**（知识库内容被污染） | 未覆盖 |
| A9 | 规则逃逸（找到规则边界条件） | 持续风险（373 的 13/17 绕过） |
| A10 | 门禁假阳性（sha 路径覆盖不到 artifact_assert） | 已修 |

## 4. 同名异义对照（**引用前必看**）

| 编号 | 409 原表 | 代码口径 | 一致？ |
|---|---|---|---|
| A1 | 声明-实现脱钩 | 记录层伪造 | ✗ |
| A2 | 断言恒真 | 声明-实现脱钩 | ✗ |
| A3 | 工件借用/覆盖 | 断言无判别力 | ✗ |
| A4 | 注释伪造 | 时序穿链 | ✗ |
| A5 | 通用符号滥用 | 环境量污染 | ✗ |
| A6 | 未声明键 | 解析走私 | ✗ |
| A7 | 符号映射缺失 | 关系图失配 | ✗ |
| A8 | 间接注入 | 间接注入 | ✅ |
| A9 | 规则逃逸 | 编译器配置逃逸 | ✗（近义，语义面不同） |
| A10 | 门禁假阳性 | 供应链与工件完整性 | ✗ |
| A11 | —（原表无此号） | 并发与可用性 | 新增（409 原文授权「全新攻击面则更新分类学 A11…」） |

**结论：仅 A8 完全一致**。跨文档引用「A4/A10」等编号时，必须写明口径。

---

## 5. 对齐方案与裁决状态（**未决，需人审**）

479 任务 5 的提案与理由存在张力，故按「不确定就停下记录」处理，**本轮不擅自改代码分类名**：

* **方案 A（改代码对齐 409）**：把 `ATTACK_TYPES` 的语义回改为 409 原义。
  影响面：`tools/poison_drill.py`（映射 + 标签）、`tests/test_poison_attack_type.py` 的
  A4/A8/A10 覆盖断言、`tools/poison_surface_map.json` 需重生成、本文件与 `_worklog_472.md` 同步。
  代价：约 1 小时；收益：与历史调研文档编号统一。
* **方案 B（改文档对齐代码）**：保留代码口径，更新 409/410 的 A 表并标注「已被代码口径取代」。
  代价：改历史调研文档（违背「历史快照不追改」惯例）；收益：不动已自洽的代码与测试。
* **已落地的中立措施（本文件）**：两套口径并列 + 同名异义对照 + 引用规范，
  **消除误用风险而不预设方向**；代码侧标签已在 `ATTACK_TYPE_LABELS` 显式化。

**裁决所需信息**：若后续调研文档（409/410 系）仍被当作活引用，选 A；若它们已是历史快照，
选 B。此判断需文档所有者（用户）作出。

---

## 6. 引用规范（立即可用）

1. 写「攻击面 A7」时，附口径：`A7（代码口径，472+）` 或 `A7（409 原表）`。
2. 覆盖率数字一律引用 `tools/poison_surface_map.json`（由 `--write-surface-map` 实测生成），
   不手抄。
3. 新增攻击面 ⇒ 只扩**代码口径**（下一编号），并在本文件 §2 登记；409 原表作为历史快照冻结。
