# 章节映射权威表（空号 → 现章）

> 依据：逐条核对 `Book/**` 章节 H1 标题与引用上下文语义建立，**非机械按数字猜测**。
> 每条引用在正文出现的位置见 `content_debt_tasklist.md` §1.2 文件分布。

## 1. 映射规则（删除后合并章节）

| 空号 | 原计划主题 | 现章 | 依据（现章 H1） |
|------|-----------|------|-----------------|
| ch33 | 悬垂 / 生命周期 | **ch28** | 第28章 对象生命周期与未定义行为（生存期、悬垂、UB） |
| ch34 | 异常安全 / valueless | **ch40** | 第40章 异常安全（Exception Safety） |
| ch54 / ch56 | 虚继承内存布局 | **ch49** | 第49章 虚继承与菱形继承：共享虚基类 |
| ch59 | 模板推导 / 模板基础 | **ch60** | 第60章 模板基础与实例化 |
| ch73 | CRTP 进阶 | **ch51** | 第51章 CRTP（奇异递归模板模式） |
| ch75 | 模板报错可读性 | **ch67** | 第67章 Concepts 与 requires（改善报错可读性） |

## 2. 改写规则（原主题已删除，无对应现章）

| 空号 | 原计划主题 | 处理 | 依据 |
|------|-----------|------|------|
| ch74 | 反射 | **删去/改写**：C++26 反射未成章，正文改为不指向特定章 | 无对应章 |
| ch102 | 并发 / static 初始化 | **改写**：静态局部线程安全见 ch19 自身；若指线程/原子见 ch93 / ch107 | 无独立并发原语章 |
| ch103 | mutex | **改写** → ch93（thread/async 含锁）或 ch110（lock-free 对比） | 无独立 mutex 章 |
| ch104 | condition_variable / mutex | **改写** → ch93 或 ch107 | 无独立 cv 章 |
| ch105 | 信号量 / 同步原语 | **改写** → ch93 / ch107 | 无独立章 |
| ch114 | 异步执行器 / std::execution | **改写** → ch93（future/async）或 ch113（协程） | std::execution 未入标准 |

## 3. 执行原则

1. 每条引用先读上下文确认语义，再套用映射/改写；
2. `ch93/ch94` 等"未改动 INDEX"元注记行（作者说明文字）若含空号，一并修正；
3. 全部修改后跑 `quality`，确认 dangling=0、topology=0、无新 WARN；
4. 完成后把 `dangling_ref_linter` / `prereq_topo_check` 从软门禁转硬（CI `continue-on-error: false`）。