# WORKLIST v4 — 质量收尾阶段（2026-07-14 建立）

> **前置状态**（2026-07-14 终检）：
> - 密度 v3：avg combined=24.9/30, shallow=0, min=23, max=30, A-J 十维全非零 → 竣工
> - I 维度：62/62 全收口（13 批 / ~1,700 行纯散文）→ 竣工
> - 方向 1 汇编实证：核心机制全满 8 例 → 竣工
> - 编译门禁：112/147 通过（35 豁免），5 真实 bug 已修复
> - 一致性：30 次连续 100/100

---

## Phase A：质量加固（P0 — 排雷优先）

### A1. 交叉引用审计

**目标**：147 章所有 `[chNN](...)` 交叉引用不指向死链接。

| 子任务 | 内容 | 状态 |
|--------|------|------|
| A1.1 | 写脚本扫描所有 `[ch数字]` 引用，验证目标 .md 文件存在 | ⬜ |
| A1.2 | 写脚本扫描所有 `[#锚点](#...)` 内部锚点，验证目标标题存在 | ⬜ |
| A1.3 | 修复断裂引用（预估 5-15 处） | ⬜ |
| A1.4 | 补充缺失的交叉引用（核心依赖链：如 ch26 lambda → ch69 constexpr 应互引）| ⬜ |

### A2. CI 豁免消化

**目标**：减少 `compile_exempt.json` 中的可修项，提升真实门禁覆盖。

| 子任务 | 内容 | 状态 |
|--------|------|------|
| A2.1 | 审计 35 项豁免，分类为「可修」「需重构」「真豁免」| ⬜ |
| A2.2 | 修复可修项（预估 5-10 块：缺头文件/简单语法错误）| ⬜ |
| A2.3 | 标注真豁免项（平台/外部库/MSVC 专有）为永久豁免 | ⬜ |
| A2.4 | 更新豁免清单，目标 110+/147 通过 | ⬜ |

### A3. 术语一致性

**目标**：全卷统一核心术语（中英混用 → 首次出现标原文，后续用中文）。

| 子任务 | 内容 | 状态 |
|--------|------|------|
| A3.1 | 建立术语映射表（约 50 个核心术语） | ⬜ |
| A3.2 | grep 扫描高频混用（如 "移动语义" vs "move semantics"）| ⬜ |
| A3.3 | 逐章统一（先改高频章，其余逐批） | ⬜ |

---

## Phase B：发布提升（P1 — 可见成果）

### B1. 方向 1 扩展（非核心机制章）

**目标**：在已有 8 例基础上，给余下有价值的章追加汇编实证。

| 优先级 | 候选章 | 价值 |
|--------|--------|------|
| ★★★ | ch117 copy_elision | NRVO 与 RVO 的区别：NRVO 编译器拒绝时的汇编差异 |
| ★★★ | ch42 strict_aliasing | `-fstrict-aliasing` 对 load/store 指令的实际影响 |
| ★★ | ch48 rtti | `typeid` vs `dynamic_cast` 的真实指令对比 |
| ★★ | ch52 ebo | 空基类优化在 sizeof 中的实际汇编体现 |
| ★ | ch28 lifetime | use-after-free 的编译器优化"许可"→UB 可见后果 |

### B2. 发布 v1.1.0

| 子任务 | 内容 | 状态 |
|--------|------|------|
| B2.1 | 更新 CHANGELOG.md（I 收口 + 8 例汇编实证 + 门禁里程碑）| ⬜ |
| B2.2 | 打 tag `v1.1.0` | ⬜ |
| B2.3 | 创建 GitHub Release（描述 + CHANGELOG 链接）| ⬜ |

### B3. PDF/EPUB 全量构建

| 子任务 | 内容 | 状态 |
|--------|------|------|
| B3.1 | 修复 Mermaid 图表→静态图片渲染（epub/PDF 不支持动态 SVG）| ⬜ |
| B3.2 | 跑 `generate_pdf.py` 全量构建 | ⬜ |
| B3.3 | PDF 质量检查（目录/页码/图表/代码块/中文字体）| ⬜ |

---

## Phase C：体验优化（P2 — 锦上添花）

### C1. Mermaid 图表审计

| 子任务 | 内容 | 状态 |
|--------|------|------|
| C1.1 | 全量 Mermaid 语法验证（`_mermaid_validate.mjs` 已有基础设施）| ⬜ |
| C1.2 | 修复渲染失败的图表（预估 3-8 处） | ⬜ |

### C2. 章编号一致性

| 子任务 | 内容 | 状态 |
|--------|------|------|
| C2.1 | 扫描所有 `第N章` 声明与文件名 chN 的一致性 | ⬜ |
| C2.2 | 修复编号不一致（预估 0-3 处） | ⬜ |

### C3. 代码块质量

| 子任务 | 内容 | 状态 |
|--------|------|------|
| C3.1 | 审计 `// 编译: g++ -std=c++20` 注释与各块实际需求的匹配度 | ⬜ |
| C3.2 | 修复无法独立编译的块标注为「片段」| ⬜ |

---

## 执行约定

1. 所有改动后跑 `consistency_check.py`（必须 0/0）
2. 涉及 .cpp 块变化后跑编译门禁
3. Book/*.md 正文保持最简洁（写作风格红线）
4. 每完成一个 Phase 子项 → commit + push + 记忆日志
5. ROADMAP_v3.md 跟着每批同步回填

---

## 进度总览

| Phase | 子项数 | 完成 | 进度 |
|-------|--------|------|------|
| A 质量加固 | 10 | 0 | 0% |
| B 发布提升 | 8 | 0 | 0% |
| C 体验优化 | 6 | 0 | 0% |
| **合计** | **24** | **0** | **0%** |

---

_本文件与 ROADMAP_v3.md 互补：ROADMAP 记录历史，WORKLIST 指导未来。_
