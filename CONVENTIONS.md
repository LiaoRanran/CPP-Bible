# 编写规范与约定 · CONVENTIONS

> 本书所有章节强制遵循本规范。任何偏离都需在章末「偏离说明」标注。

## 0. 总纲（SUPREME ENGINEERING SPECIFICATION 落地）

本书以 `docs/SUPREME_ENGINEERING_SPECIFICATION.md` 为最高质量宪章（即用户下达的「C++ ULTIMATE BIBLE — SUPREME ENGINEERING SPECIFICATION」）。所有写作、扩写、修订活动优先遵循其优先级与反幻觉/验证协议：

- **优先级铁律**：正确性 > 完整性 > 深度 > 广度 > 篇幅。宁可少写，也绝不允许错误、幻觉、伪深度、未经验证的专业结论进入正文。
- **五层真相模型**：任何涉及标准/实现/ABI/平台/微架构的内容，必须显式标注所属层级（见 §1）。禁止把 `[实现]`/`[ABI]`/`[平台]`/`[微架构]` 伪装成 `[标准]`。
- **验证优先**（对应宪章 §6）：任何具有教学意义的重要代码与实现/性能结论，必须可验证（编译/运行/UB sanitizer/反汇编/跨编译器/跨标准版本），并在正文用 `[VERIFIED]`/`[UNVERIFIED]`/`[NEEDS-VERIFY]` 标注（见 §10）。无法实际验证的内容一律标 `[UNVERIFIED]`，绝不伪装成已验证。
- **反幻觉**（对应宪章 §7/§8）：标准条款、proposal 编号、ABI 布局、内存模型、并发/lock-free、优化、汇编、CPU 行为、benchmark 数字等高风险断言，禁止伪造；不确定时标注「需核验标准来源」或 `[UNVERIFIED]`。
- **最高价值缺陷优先**（对应宪章 §22）：每轮优先修复高风险错误 > 知识断层 > 标准错误 > 实现误导 > 大量重复 > 失效链接 > 未经验证代码 > 严重浅层内容；禁止一次性大规模重写整个仓库，禁止平均用力。

## 1. 立场分层标签（必须区分，禁止混淆）

| 标签 | 含义 | 用途 |
|---|---|---|
| `[标准]` | ISO C++ 语言/库规定 | 引用标准条款（如 `[expr]`、`[class.virtual]`） |
| `[实现]` | 编译器/标准库实现细节 | GCC/Clang/MSVC、libstdc++/libc++/MS STL 行为 |
| `[ABI]` | 二进制接口稳定性 | 名称修饰、对象布局、vtable/RTTI 布局、调用约定、异常 ABI |
| `[平台]` | OS / 硬件平台相关 | Itanium C++ ABI、Windows x64、Linux ELF、x86-64 / ARM64 |
| `[微架构]` | CPU 硬件层行为 | cache 层级、分支预测、TLB、流水线、SIMD、memory hierarchy |
| `[经验]` | 工程共识 / 个人判断 | “通常建议…”“实践中…” |

**规则**：
- 凡是 `[实现]`/`[ABI]`/`[平台]`/`[微架构]` 内容，必须标注具体实现/架构（如 `[实现·GCC15]`、`[ABI·Itanium C++ ABI]`、`[平台·x86-64]`、`[微架构·Skylake]`、`[微架构·ARM Cortex-A76]`），不得伪装成标准规定。
- `[标准]` 必须是 ISO C++ 真正确保的行为；凡属「通常如此」的实现/平台/微架构现象，必须降级到对应标签，并说明「标准保证什么 / 实现通常做什么 / 某平台目前怎么做 / 为何不能把后者当成前者」。
- 任一非 `[标准]` 的高风险断言（ABI 布局、内存模型、并发/lock-free、优化、汇编、CPU 行为、benchmark 数字），若未经实际验证，须追加 `[UNVERIFIED]`（见 §10）。

## 2. 章节统一模板（20 元素）

每章开头固定元数据块，正文按以下顺序组织（可合并小节，但不得遗漏）：

```
# 第NN章 <标题>

> 元数据：标准基 / 预计阅读 / 前置 / 后续 / 难度

## ① 学习目标
## ② 前置知识  ⟶ 链接
## ③ 后续依赖  ⟶ 链接
## ④ 知识图谱（ASCII）
## ⑤ Mermaid 流程图（适用时）
## ⑥ UML 类图（适用时）
## ⑦ ASCII 内存图 / 对象布局
## ⑧ 生命周期图
## ⑨ 调用栈 / 时序图
## ⑩ 汇编分析（Compiler Explorer 风格，标注 -O2）
## ⑪ STL 联系
## ⑫ 工业案例（服务器/引擎/DB… 禁止 Hello World）
## ⑬ 源码分析（libstdc++/libc++/MS STL 对比）
## ⑭ WG21 提案（编号 + 标题 + 动机）
## ⑮ 面试题
## ⑯ 易错点
## ⑰ FAQ
## ⑱ 最佳实践
## ⑲ 性能分析（含复杂度/缓存/ABI）
## ⑳ 推荐阅读
## 附录：练习题 / 思考题 / 源码阅读建议
```

## 3. 知识点统一模板（每节至少覆盖）

```
### <知识点>

【定义】一句话精确定义
【历史】起源与演进
【为什么设计】解决什么痛点、权衡是什么
【标准规定】[标准] 条款与约束
【编译器行为】模板实例化/重载/名字查找规则
【GCC实现】/【LLVM实现】/【MSVC实现】 差异点
【libstdc++实现】/【libc++实现】/【MS STL实现】 差异点
【内存模型】对象在内存中的布局
【汇编】关键路径的 -O2 汇编（AT&T 或 Intel 语法，标注）
【性能】基准数据（注明来源/示意）
【复杂度】时间/空间
【异常安全】noexcept? 强/基本/不抛出保证
【线程安全】data race?
【缓存友好性】false sharing? 局部性?
【CPU影响】分支预测/流水线
【ABI】跨版本/跨编译器稳定性
【工程应用】真实项目用法
【真实源码】取自开源项目的片段
【错误示例】// ❌ 注释说明错在哪
【正确示例】// ✅ 注释说明为什么对
【至少10个例子】编号例 1..N
```

## 4. 图示约定

- **ASCII 图**：用于内存布局、对象模型、调用栈、依赖图、时序图。使用等宽框线（`┌─┐│└┘├─┤`），节点标明地址/偏移/大小。
- **Mermaid**：流程图 `flowchart`、时序图 `sequenceDiagram`、类图 `classDiagram`、状态图 `stateDiagram-v2`、思维导图 `mindmap`。保存源码于 `Mermaid/`，正文嵌入 ` ```mermaid `。
- **UML 类图**：用 Mermaid `classDiagram` 表达继承/组合/依赖/聚合。
- **汇编图**：用 ` ```asm ` 或 ` ```x86asm `，标注编译器/标志/语法。
- **内存图**：用 ASCII 或 Mermaid `graph` 表达段/堆/栈。

## 5. 代码规范

- 所有代码可编译（C++23，`-std=c++23 -O2 -Wall -Wextra`）。
- 禁止 `using namespace std;` 在头文件；示例正文可局部使用以便阅读，但工业案例必须显式 `std::`。
- 工业案例禁用 Hello World；采用服务器/引擎/DB/网络/渲染/交易等真实场景。
- 示例代码放入 `Examples/<章号>_xxx.cpp`，正文用 ` ```cpp ` 引用关键片段并标注文件路径。
- 错误示例统一前缀 `// ❌`，正确示例 `// ✅`，并说明原因。

## 6. 交叉引用与索引维护

- 章节内引用：`⟶ Book/part03_language/ch20_reference_pointer.md`
- 术语首次出现加粗并链 `Index/GLOSSARY.md#odr`。
- 每章写完：① 在 `INDEX.md §1` 登记；② 在 `INDEX.md §2` 依赖图补边；③ 新术语入 `GLOSSARY.md`；④ 相关知识点在 `CROSSREF.md` 建双向链；⑤ 面试题/误区/FAQ 入对应库。

## 7. 术语统一表（强制）

| 术语 | 统一写法 | 禁用写法 |
|---|---|---|
| 右值引用 | 右值引用（rvalue reference） | 右值引用（不用「右值参考」） |
| 完美转发 | 完美转发（perfect forwarding） | — |
| 对象切片 | 对象切片（object slicing） | — |
| 未定义行为 | 未定义行为（UB） | 不用「未定义」单说 |
| 求值顺序 | 求值顺序（order of evaluation） | — |
| 名称查找 | 名字查找 / 名称查找（name lookup） | — |
| 实参/形参 | 实参（argument）/ 形参（parameter） | 不混用 |
| 模板实参 | 模板实参（template argument） | — |

## 8. 自检清单（每章收尾必过）

- [ ] 1. 无事实错误、无标准版本混淆（如把 C++20 概念写成 C++11）。
- [ ] 2. 有内存图或汇编（核心节至少其一）。
- [ ] 3. 适用时含 GCC/Clang/MSVC 三者或 libstdc++/libc++/MS STL 三者对比。
- [ ] 4. 有工业案例（非 Hello World）。
- [ ] 5. 有面试题与易错点。
- [ ] 6. 交叉引用已建立（≥3 条链接）。
- [ ] 7. 新术语已入 GLOSSARY.md。
- [ ] 8. 有性能分析（复杂度/缓存/ABI 至少一项）。
- [ ] 9. 有 WG21 提案或 ISO 标准背景（标准相关章）。
- [ ] 10. 有练习题/思考题/源码阅读建议。
- [ ] 11. 无「知道怎么用但不知为何这样设计」的留白（补动机与权衡）。
- [ ] 12. 无重复讲解（与已写章节重叠处改为交叉引用）。

## 9. 风格

- 中文为主，保留必要英文术语（首次出现给中英文）。
- 不用应试八股，重工程直觉与「为什么」。
- 表格丰富、图示齐全、示例可运行。
- 性能数据注明来源或标注「示意」。

## 10. 验证状态标记（必须）

对应宪章 §6「代码必须进入验证体系」。任何具有教学意义、且属于高风险层级的断言（实现/ABI/平台/微架构/性能/benchmark/并发/优化/汇编），必须在其附近标注验证状态：

| 标记 | 含义 | 何时用 |
|---|---|---|
| `[VERIFIED]` | 已实际验证 | 已编译/运行/UB sanitizer/反汇编/跨编译器或跨标准版本验证过，且结论与输出一致 |
| `[UNVERIFIED]` | 未验证 / 无法验证 | 来自常见经验、文档转述、或本环境无法实跑（如特定 CPU、特定 OS），**禁止伪装成已验证** |
| `[NEEDS-VERIFY]` | 待核验 | 标准条款/proposal 编号/ABI 细节等需进一步溯源确认，避免伪造 |

- 验证手段优先级：实跑（编译+运行+输出）> sanitizer（UB/ASan/TSan）> 反汇编（`g++ -S -masm=intel`）> 跨编译器/跨版本对比 > 文献转述（必须标 `[UNVERIFIED]`）。
- `Examples/*.cpp` 与 `_bench_d5_*.cpp` 是可复现证据源；凡引用其结论，优先 `[VERIFIED]` 并注明编译命令与标志。
- 工具 `tools/verification_audit.py` 统计各章标记覆盖度，并标出「高风险章零标记」 backlog，作为本维度（实现细节可靠性）的可追踪指标。

## 11. 标准依据与反幻觉红线

对应宪章 §7/§8：涉及标准规则时尽可能给出 C++ 标准版本、相关 clause/section、feature-test macro（`__cpp_*`）、相关 WG21 proposal；**不得凭记忆捏造 proposal 编号**。不确定时写「需核验标准来源」并标 `[NEEDS-VERIFY]`。禁止：伪造标准条款、伪造 proposal、伪造 ABI 保证、把常见实现写成标准保证。
