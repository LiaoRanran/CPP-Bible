# 行尾与文本属性（2026-09-12 引入 · 修 A1「行尾不统一」）
#
# 原则（三条，各有理由）：
#   1. 手写文本统一 LF：跨平台一致（Windows/WSL 双环境同仓工作，CRLF 漂移曾导致
#      WSL 侧 437 个 Examples/*.cpp 假 M + gate 检查恒红的隐患）。
#   2. Windows 脚本保留 CRLF：cmd.exe / PowerShell 对批处理的 CRLF 有依赖。
#   3. 证据工件（.asm/.out 等）**声明为 binary，永久禁转换**：字节即身份——
#      replay 的 artifact_sha256 强校验与 run_match 逐字比对都基于工作树字节，
#      任何行尾转换都会让全库 refute。（2026-09-12 盘点：316 个索引侧 CRLF 中
#      含 64 个 .asm，若不加本条，一次 renormalize 即毁掉全部工件证据。）

* text=auto eol=lf

# Windows 脚本：行为依赖 CRLF
*.bat  text eol=crlf
*.cmd  text eol=crlf
*.ps1  text eol=crlf

# 证据工件：字节即身份（本条必须晚于上面的通配，且以具体模式覆盖）
*.asm  -text
*.out  -text
*.o    -text
*.obj  -text
*.exe  -text

# ── 已知漂移与迁移策略（2026-09-13，监工裁决「暂不 renormalize」）────────────
# 现状：437 个 Examples/*.cpp 的**工作树是 CRLF、索引里是 LF**（d647849 行尾统一
# 之后未做批量 renormalize 留下的漂移）。Windows 侧 git stat 缓存让它不可见，
# WSL checkout 则显示为一堆假 M。
#
# 裁决：不做一次性 renormalize。理由：①437 个文件的假 M 会淹没真实改动，等于给
# 下一次真实回归提供藏身处；②历史上它曾让工作树清洁检查恒红，使门禁沦为"狼来了"。
#
# 策略 = **碰到即转**（逐步迁移）：谁修改某个 .cpp，谁顺手把它转成 LF 一并提交；
# 新文件一律 LF（由上面的 `* text=auto eol=lf` 保证）。漂移清理进度由
# `tools/debt_ledger.json` 的 DEBT-002 跟踪（票到期即停线，避免无限期挂着）。

# ── 611 A3 UTF-16 捕获产物：字节即身份，禁止行尾转换 ──────────────────────────
# data/build_reproducibility_report.md 是 PowerShell 重定向捕获的 UTF-16LE 文件，
# 不是 UTF-8 文本。`* text=auto` 会把它当文本做 CRLF 归一，checkout 时改坏成奇数长度、
# UTF-16 解码失败 ⇒ test_603_capture_untouched_by_this_batch 在本地与 CI 都红。
# 按 611「字节即身份、全仓无人引用、只登记不改」的设计，标 -text 让它像 .asm/.out 一样
# 原样进出仓库（修复 632 A2 #4 的根因，CI 方能绿）。
data/build_reproducibility_report.md -text
