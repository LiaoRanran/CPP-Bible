# 646 验收报告 · 清 645 债 + 继续补强薄弱部分

> 任务书：`_auto/inbox/646.md`。完成报告：`_auto/outbox/646.md`。状态：`_auto/status.json`。
> **本轮不 push、不代签、不 golden accept、未改 CORE_TOOLS 判决逻辑、未改现有 67 规则、受控目录零污染。**

## 一、完成总览（对照 646 §三/§四/§五）

| 阶段 | 任务 | 交付 | 状态 |
|---|---|---|---|
| 0.1 | 开工基线 | `data/646_baseline.md` | ✅ |
| 0.2 | 645 债务核对 | 基线 §二 | ✅（含口径修正，§三） |
| A1 | 规则→卡显式映射（清债 1） | `rule_card_mapper_646` + `646_rule_card_mapping.json` | ✅ 67×27 |
| A2 | 三层耦合真正打通（清债 1 续） | `three_layer_orchestrator_646` + `646_coupling_report.md` | ✅ 归因 0.1→**1.0** |
| A3 | 性能优化·工具速度（清债 2） | `performance_optimizer_646` + `perf_646` + `646_performance_report.md` | ✅ 4/4 ≥30% |
| A4 | 性能优化·证据检索（清债 2 续） | `evidence_index_646` + `646_evidence_index.json` | ✅ **99.9%** |
| A5 | 账本规则字段补全（清债 3） | `authority_rule_annotator_646` + `646_authority_rule_annotation.jsonl` | ✅ 452 条 |
| A6 | R5 扩样本验证（清债 6） | `loop_r5_runner_646` + `646_r5_report.md` | ✅（结论见 §三.3） |
| B1 | 标准获取补全 | `standard_fetcher_646` + `646_standard_fetch_report.md` | ✅ **18/18** |
| B2 | 反例语义级补全 | `counterexample_searcher_646` + `646_counterexample_report.md` | ✅ **27/27** |
| B3 | 充分性不足卡补强 | `evidence_sufficiency_646` + `646_sufficiency_report.md` | ✅ **27/27**（幻影卡修正） |
| B4 | 工具进一步精简 | `tool_consolidation_646` + `646_consolidation_report.md` | ⚠️ **只分析未执行**（§三.5） |
| B5 | 注释质量提升 | `docstring_quality_646` + `646_docstring_quality_report.md` | ✅ 4.25→**4.5/5** |
| C1 | 收工门禁 | `run_646_gate.py` | ✅ **PASS** |
| C2 | 两阶段 pytest 终验 | 门禁日志 | ✅ |
| C3 | 验收报告 + status + outbox | 本文件 + `status.json` + `outbox/646.md` | ✅ |

## 二、关键实测（数字真实、可复算）

### 1. 清债 1：三层耦合真正打通（A1+A2）

- **A1 映射**：67 规则 × **27 张真实卡**（见 §三.1 幻影卡修正），映射强度分布
  `{high: 195, medium: 664, low: 729}`，**无孤儿规则**（67/67 有映射）；已知关联必命中
  （`ATOM-GRAY-ZONE`→唯一 UB 卡 high、`ATOM-MISCONCEPTION-*`→8 张有误解的卡）。
- **A2 打通**：**10 条链**，**证据≠0 的链 10/10**，**问题归因可复核率 1.0**（目标 ≥0.5）——
  对比 645 的 0.1（token 猜测匹配）**显著提升**。

### 2. 清债 2：性能（A3+A4）

- **A3 工具速度**：4 个重负载全部 **≥30%** 提速——`orchestrate` 重复调用 **99.5%**、
  `build_mapping` 冷→热 **99.6%**、`discover_blind_spots` 重复 gate→复用计数 **100%**、
  证据读盘裸读→缓存 **100%**。手段=`perf_646` 记忆化 + 一次 gate 扫描多方共享。
- **A4 证据检索**：倒排 + 等级索引，**提速 99.9%**（线性 285 命中 vs 索引 285 命中，**一致**）。

### 3. 清债 3：账本规则字段（A5）

- **452 条** Authority 判决事件全部注释 `rule_ids`（新文件，**原账本 sha256 前后一致 = 一字未改**），
  **67/67 规则**每条至少 1 条事件关联。

### 4. 清债 6：R5 扩样本（A6）

- 窄样本（645，3 草案）1.0 → 扩样本（452 事件）**0.9851**（MODIFY 口径）/ **1.0**（独立审查口径）。
- **根因（比数字重要）**：A1 映射很宽 + 452 事件覆盖全部 27 卡 ⇒ 几乎每条规则都被事件覆盖 ⇒
  **R5 定义饱和、缺乏区分度**。结论：645 的 1.0 不是「虚高」，而是**该口径本身无区分力**（§三.3）。

### 5. 头部层补强（B1-B3）

- **B1**：真获取 **18/18**（`memory`/`cpp` 长超时重试 attempt=1 成功）。
- **B2**：反例候选覆盖 **27/27 卡**（166 条候选，扩展语义表 + 域兜底，只搜不判）。
- **B3**：充分性重判 **27/27 充分、0 不足**（真实卡域）。

### 6. 优雅代码（B5）

- docstring 质量抽查 20 关键工具，平均分 **4.5/5**（改进前 4.25），无低于 4 分者。

## 三、诚实登记（防自欺，对照 646 §九）

1. **口径修正：卡片总数是 27 不是 28（重要）**。645 报告的「28 卡 / 1 张充分性不足」中的第 28 张
   `ATOM-MEM-MOVE-001` 是**幻影卡**——只存在于 `.pytest_tmp/**/atoms/` 测试沙箱，从未被 git 跟踪。
   真实仓库 = `atoms/**/*.md` 28 个文件 - `README.md` = **27 张卡**。故 645 的「1 张不足」是幻影；
   646 B3 以真实 27 卡重判 ⇒ **27/27 全充分**。根因假设=645 的卡片域被测试沙箱证据污染，
   646 收口为「卡片域 = 真实 `list_atoms()`」。
2. **B1 重试成功但仍是同一网络**：18/18 靠长超时（60s）+ 退避达成，非换源；网络抖动下可能再失败。
3. **A6 R5 扩样本未显著下降（0.985/1.0）**：不是「稳定」而是**口径饱和无区分度**（§二.4）。
   让 R5 有区分度需更窄的「建议→采纳」定义，留 647。
4. **A2 归因 1.0 部分来自映射宽度**：A1 的属性映射很宽（67 规则几乎都映射到全部 27 卡），而 27 卡
   都有 L1 证据 ⇒ 证据≠0 容易达成。**真实增益是「证据流首次打通」（0.1→1.0）**，但**区分度有限**，
   与 A6 同源。不夸大为「耦合已完美」。
5. **B4 只分析未执行**（诚实）：给出 3 组合并方案（15→10），但**未删除任何工具**——因为 15 个工具各自被
   `tests/test_*_645.py` import，贸然删除会让 645 测试套件整体红，违反「功能不丢失 / 单测全绿」。
   执行合并（含同步改测试）留 647。
6. **A3/A4 的提速是「重复调用/缓存」场景**：冷启动单次的绝对耗时仍由 gate 全库扫描主导（~2s），
   本批未做算法级压缩；不夸大。
7. **A5 注释文件不是正式 schema**：是否写入 Authority 正式 schema 留交人（§五.5）。
8. **工作区遗留**：645 之前遗留的 `data/*.md|json` 修改（629/630/631 基线等）非本批产生，本批不动。

## 四、硬边界遵守

- **未 push**（ahead 从 84 继续增加，交人统一 push）。
- **未代签**、**未 golden accept**、**未开 delegation**。
- **未改 CORE_TOOLS 判决逻辑**、**未改现有 67 规则**。
- **未修改历史账本**：A5 只新建注释文件，原 `decision_event_v2_ledger.jsonl` sha256 前后一致。
- **受控目录零污染**：`atoms/` `evidence/` `Examples/` `Book/` git status 净变更 0（门禁核对）。
- 全程中文；每个 646 工具 `--check` 只读幂等；纯标准库优先。

## 五、交人裁决项（对照 646 §八）

1. **是否 push**（ahead=84+，625-646 全区间）。
2. **A3（645）三条规则草案是否上线**（`DRAFT-PEDAGOGY-001`/`DRAFT-CLAIM-BOUND-001`/`DRAFT-REL-KNOWN-001`，
   沙箱涟漪 0）。
3. **R5 口径**：扩样本仍饱和 → 建议**不扩白名单**，先改窄口径再跑一轮（647）。
4. **647 重点**：信任根独立（fail-open 修复）/ 保护器真上岗 / 仓库拆分执行 / 继续补强。
5. **A5 账本规则字段**：是否正式写入 Authority schema（不只是注释文件）。
6. **性能优化**：是否推广到全部工具（不只是 Top 负载）；A3/A4 的缓存是否上生产路径。

## 六、附：产物清单

- **工具 12**（`tools/*_646.py`）+ 门禁 `run_646_gate.py`。
- **测试**：`tests/test_*_646.py`（fast 37 例）。
- **报告**：`data/646_*`（基线/映射/耦合/性能/索引/账本注释/R5/标准/反例/充分性/合并/注释质量）。
- **状态/交接**：本文件 + `_auto/status.json` + `_auto/outbox/646.md`。
