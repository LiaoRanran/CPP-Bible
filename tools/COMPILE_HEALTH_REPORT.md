# 全书编译健康报告 — GCC 15.3.0 全量门禁审计

> 生成时间：2026-07-19　工具链：`compile_all.py --main-only` + `compile_gate.py`

## 1. 方法论

- **编译器**：`C:\Qt\Tools\mingw1530_64\bin\g++.EXE`（MinGW-W64 GCC **15.3.0**）
- **编译标志**：`-std=c++23 -O0 -fsyntax-only`（`-fsyntax-only` 仅做语法/语义检查，不产出目标码）
- **范围**：`--main-only`，仅编译含 `int main` 的完整程序块（插图片段预期不可独立编译，已排除）
- **扫描**：`Book/` 下全部非 `_legacy` `.md` 章节
- **比对**：真实失败块集合 vs `tools/compile_exempt.json` 65 条显式豁免（含 5 类 AUTO_PATTERNS 正则兜底）

## 2. 审计结果

| 指标 | 值 |
|---|---|
| 扫描章节 | 147 |
| 全过章节 | 113 |
| 含失败块章节 | 34 |
| 编译块总数（含 int main） | 3720 |
| 失败块数 | 65（占比 1.75%） |
| 报告完整性 | 完整（partial=False） |

## 3. 门禁结论（真 bit-rot 判定）

- 报告失败块：**65**
- 已知豁免命中：**65**（显式 65 / 模式 0）
- **新增回归（真 bit-rot）：0**
- 清单冗余（豁免未命中任何失败）：0


✅ **65 个失败块与 65 条豁免 1:1 精确匹配，新增回归 = 0，清单冗余 = 0。**
   与 2026-07-18 旧基线（147 章 / 3720 块 / 65 失败）完全一致，
   证明全书在 GCC 15.3.0 下无编译器演进导致的 bit-rot。

## 4. 豁免原因分布（65 条）

| 原因 | 条数 | 含义 |
|---|---|---|
| CROSS_BLOCK | 22 | 跨代码块依赖（单块摘出不可编译，需同章其他块） |
| MULTI_FILE | 17 | 多文件程序（单 .md 块无法独立链接） |
| MODULE | 6 | C++20 模块（需预编译模块单元，本工具链无对应模块） |
| EXT_LIB | 6 | 依赖外部第三方库（Boost/Qt 等，未随书提供） |
| INTENTIONAL_UB | 4 | 故意展示未定义行为（教学用负例） |
| MSVC | 4 | 仅 MSVC 可编译（书明示 Windows 专属） |
| INTENTIONAL_ERROR | 2 | 故意展示编译错误（教学用负例） |
| POSIX | 2 | POSIX 专属 API（非 MinGW 默认提供） |
| GUARDED_COMPILE | 1 | 带条件编译保护，独立摘出不触发该分支 |
| LIBSTDCPP | 1 | 依赖 libstdc++ 内部实现细节 |

## 5. 与 asm 实证库复验的协同结论

- **asm 实证库**（72 源 / 69 真实证据 + 3 预期特例）：69 个 GCC 15.3.0 反汇编证据 100% 对齐，0 DRIFT / 0 伪造。
- **编译门禁**（147 章 / 3720 块）：65 失败全豁免，0 真 bit-rot。
- 两条独立证据链共同确认：**本书在 GCC 15.3.0 工具链下实证完整、编译诚实、无版本漂移。**

