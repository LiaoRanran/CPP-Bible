# _worklog_571 · 夜间大包：L3/L4 攒数据 + 收口

> 任务书：`References/architecture_架构演进/571_夜间大包_L3L4攒数据_ruff分批_命题签署.md`
> 承接：`82c89ce`（570）｜分支 master（本地，未 push）｜本文件按惯例**不入库**。

## 0 · 交付（**停在任务边界**：任务 1、4 完成；任务 2、3 未开工，如实登记）

| 任务 | 状态 | commit |
|---|---|---|
| 1 · L3 攒数据（M3 补样 + v2 全量基线） | ✅ 完成（可判 7→**65**，目标 ≥59 达成） | `b969360` |
| 2 · L4 实体（overturned 事件通道 + 三曲线） | ⛔ **未开工**（见 §5 停点说明） | — |
| 3 · ruff 914 分批清 | ⛔ **未开工**（同上） | — |
| 4 · 命题级 signed_by 最小骨架 | ✅ 完成 | `6c1b3fb` |
| 附 · 测试重冻结（扩面的必然后果） | ✅ | `403bb16` |

## 1 · 任务 1：M3 可判样本 7 → 65（先量后动，两个根因）

**先量的结论**（83 卡逐卡量门禁读取面形状）：`contains_in` 3 卡 / `absent_in` 3 / `-Werror` 1 /
`count:` **0** / `contains_any` 21 / `run_match_keys` **0**（← 这个 0 就是 bug 的信号）。

**根因 ①（真 bug）：`GATE_READ_KEYS` 漏了 `actual`。** 门禁/复算**真读** `actual.run_match_keys`
（B3 `EV-OUT-UNDECLARED-KEY`）与 `actual.run_match_file`（replay），而这两键**嵌套在 `actual:` 下**，
只列顶层键名永远匹配不到 ⇒ 整块 `actual` 被当成"门禁不读" ⇒ 把"**没问**"记成了"**不适用**"
（实测修复前 0 卡命中 `run_match_keys`）。

**根因 ②：M3 变体面太窄**（只对 `_in`/`-Werror`/`count:`），而绝大多数卡的读取面只有普通断言条目
⇒ 无变体可产 ⇒ 全落 `n_a(out_of_scope)`。补三条**可证弱化**的新变体：
* 删掉一条 flow 式断言条目（卡少查一项）；
* 给 `contains` 追加**恒真样板候选**（`.file` 这类 ⇒ 平凡输出即可满足）；
* 删掉一条 `run_match_keys` 声明（**既有 B3 规则看着** ⇒ 预期 blocked）。
  *踩过的两个写法坑*：① 列表项**带引号**（`- "a"`）→ 正则漏了引号；② **flow 式内联**列表
  （`run_match_keys: [a, b, c]`，无 `- item` 行）→ 只找列表项会全漏。两种写法现在都覆盖。

**v1 → v2 逐算子对比**（`data/mutation/full_baseline_v2.json`，649s / 1181 变体 / 83 卡；
口径沿 565b：可判分母 = blocked+escaped，warn_only 不入严格分子）：

| 算子 | v1 b/e/n_a | v2 b/e/n_a | v2 可判 | 逃逸率（95% C-P） |
|---|---|---|---|---|
| M1 | 64/1/27 | 64/1/27 | 65 | 1/65 = 1.5% [0.04%, 8.3%] |
| **M2** | 0/207/14 | **168/0/27** | 168 | **0/168 = 0% [0, 2.2%]** |
| **M3** | 2/11/75 | **13/52/42** | **65** | 52/65 = 80% [68.2%, 88.9%] |
| M4 | 200/0/33 | 200/0/33 | 200 | 0/200 = 0% [0, 1.8%] |
| M5 | 0/0/83 | 0/0/83 | **0** | **None（insufficient，不填 0）** |
| M6 | 324/8/0 | 324/8/0 | 332 | 8/332 = 2.4% [1.0%, 4.7%] |
| M7 | 139/0/0 | 139/0/0 | 139 | 0/139 = 0% [0, 2.6%] |

* **M3 目标达成**：可判 7 → **65 ≥ 59**；`strict` 5/65 = 7.7% [2.5%, 17.0%]。
* **M2 是意外收获**：v1 的 207 条"逃逸"在 v2 归零（168 blocked / 27 n_a）——那批大多是**假逃逸**
  （正文/非门禁字段里的路径变形），`actual` 修好后判据面才对。
* **M3 的 52 条逃逸逐条定性（全是真洞，不是无效变异）**：样板候选 35 + 删断言条目 11 +
  `run_match_keys` 6 —— 即"卡可以悄悄少查一项/接受平凡输出，门禁不响"。这条**正是 L3 攒数据要的产物**
  （发现器不是 CI 红灯），留给后续批次按"先量后动、warn 起步"处理。

## 2 · 任务 4：命题级 `signed_by` 最小骨架

* **schema**：`claim_structured[*].signed_by`（可选，`human:<实名>`）；缺省不签 ⇒ 仍由卡级
  `verified_by` 兜底。`prop_graph` 侧**无需改动**——`signed_by` 列与
  `prop_signed/card_signed/unsigned` 的 signoff_state 判定在 565 就已就位（当前命题级署名 0 条）。
* **门禁校验**：扩 **S1-GIT-AUTHOR-BINDING**（不新增规则 id ⇒ 不增 RULE-COVERAGE 债）：命题级署名
  若存在，其名须与该文件最后一次 git 提交作者匹配，否则 **warn** ——刻意与卡级同款（观察期口径 +
  571 硬纪律"warn 起步"），**不做 block**。**偏差**：提示词的用例措辞是"冒名 block"，本批按硬纪律
  落 warn 并在用例里锁 warn（升级 block 的前置与卡级相同：观察期零误伤 + 规范写明"签收必须本人"）。
* **不自动签**：现有 3 条未签命题（`ATOM-LANG-INLINE-001` 的 prop-1/2/3）本批一行没碰。
* **回归锁**（`tests/test_prop_signed_by.py`，4 例）：合法通过 / 冒名 warn（文案点明"命题级"与卡级兜底）/
  缺省安静且**卡级判断不丢** / **存量 79 命题不因新字段变红**。

## 3 · 收工验收（fresh）

| 项 | 实测 |
|---|---|
| gate（**存量零误伤硬约束**） | `规则 62 条 · 命中 141 (block=0 warn=136 advice=5)` —— 与 570 结尾**逐字相同** |
| poison | **110/110**（制衡层有效） |
| replay | `confirm=56 refute=0 infra_error=0` |
| pytest fast `-n auto` | **exit 0** |
| pytest slow `-n0` | **exit 0**（修完重冻结后复跑；**首跑 exit=1 已查清并修复**，见 §4） |
| ruff | `All checks passed!`（select 已钉） |
| `tool_integrity.py --check` | exit 0（改 gate ⇒ 同 commit 重钉） |
| 受控目录 | `git diff --quiet -- evidence/ atoms/` exit 0 |
| 任务 1 机器判据 | M3 可判 **65 ≥ 59** + `full_baseline_v2.json` 落盘 + C-P 区间（§1 表） |

## 4 · 过程记录：slow 首跑红（已修，如实留痕）

扩面必然改结论 ⇒ `tests/test_mutation_fuzz.py` 两处红：① `FROZEN_CONCLUSIONS` 少了新增的第三条
M3 变体（**预期**，按实测重冻结并注明"是扩面的预期后果、非回归"）；②
`test_558_out_of_scope_when_only_prose_mentions` 的夹具里**本来就有一条** `artifact_assert`
⇒ 571 起它确实是可弱化点，原断言（整表只剩 out_of_scope 单例）**本身失效** ⇒ 改为只锁该用例原意
（"正文里的 `contains_in` 不算门禁读取面"）。修完 slow **exit 0**。

## 5 · 停点说明（任务 2、3 未开工，不留半成品）

本批四任务的**任务 1 体量远超预期**（v2 全量基线 649s/次，且 M3 扩面过程中连踩三个实现坑：
`actual` 键漏判、引号列表项、flow 式内联列表，每次都需重跑验证），加上任务 4 的 schema/校验/回归锁，
本批次预算已耗尽 ⇒ 按任务书"**做不完停在任务边界、不留半成品**"停在**任务 1/4 完成的边界**，
任务 2（overturned 事件通道 + 三曲线）与任务 3（ruff 914 分批）**一行未动**，移交下一批：
* 任务 2 的现状/做法已在任务书里写明，无新增侦察负担；
* 任务 3 的前置（钉 `select`）570 已完成，可按"逐族 `--fix` + 逐族 commit"直接开工。

## 6 · 偏差表

1. **命题级冒名严重度**：提示词写"冒名 block"，本批落 **warn**（571 硬纪律"warn 起步"+ 与卡级同款
   观察期口径）；已在用例与本节声明。
2. **M3 可判面扩张的两条新变体属"探索性弱化"**（不是把判据口径改好看）——新增变体大多**逃逸**
   （真洞），严格拦截率因此**下降**（7.7%）；数字没被美化。
3. **任务 1 的"改前"数据**取自 570 的实跑（可判 7），未复跑旧工具；v1 对照用仓库既有的
   `full_baseline_v1.json`（1188 变体）。
4. **`tests/test_mutation_fuzz.py` 的两处重冻结**是本批**有意**改结论（扩面），已在 commit message
   与 §4 说明"非回归"。
5. 任务 2/3 **未开工**（见 §5）。
