# MIS-UB-005（misconception）

## 正面

【误解】MIS-UB-005 把临时对象绑定到引用，生命周期就一定被延长
触发说法：const T& r = f(); 临时对象活到 r 作用域结束

## 背面

为什么错：生命周期延长只适用于**直接绑定到引用变量**这一条路径；例外见标准 [class.temporary]
反例 1：绑定到结构体成员引用、数组元素、函数返回引用、initializer_list 元素均**不**延长
