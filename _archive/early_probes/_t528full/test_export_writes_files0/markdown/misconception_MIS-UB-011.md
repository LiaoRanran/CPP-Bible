# MIS-UB-011（misconception）

## 正面

【误解】MIS-UB-011 依赖跨 TU 全局变量的初始化顺序是可靠的
触发说法：另一个 cpp 里的全局对象在我用之前肯定初始化好了

## 背面

为什么错：跨翻译单元的**动态初始化顺序未指定**（静态初始化顺序问题，SIOF）
反例 1：后果随链接顺序偶发；解法是函数内 static（首次调用时初始化）或显式初始化函数
