# MIS-UB-015（misconception）

## 正面

【误解】MIS-UB-015 多个编译器/版本结果一致，说明标准规定了顺序
触发说法：GCC 和 Clang 都一样，所以行为是确定的

## 背面

为什么错：实测 GCC 输出 h/g（右→左）、Clang 输出 g/h（左→右）——**两个编译器选了相反顺序**，这恰恰证明顺序未被规定，而不是被规定
反例 1：可移植性依赖的是标准给出的**合法结果集合**，不是抽样一致；实现一致随时可能随版本变化（本项目的取样一致只是当前实现的巧合）
关联原子：ATOM-UB-GRAY-001
