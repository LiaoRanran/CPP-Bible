# ATOM-CONC-RACE-001（atom_claim）

## 正面

【conc】ATOM-CONC-RACE-001
以下论断是否成立？依据是什么？
数据竞争（无同步的并发冲突访问，且至少一方为写）是 C++ 未定义行为，不是"结果偶尔算错"—— 编译器可据此做激进优化，行为完全不可预测。TSan 能检测数据竞争，但"没被 TSan 报 ≠ 没有数据竞争"： 其漏报边界（插桩盲区 / 必须全量插桩 / 时序偶发）使工具沉默不能当作安全证明。

## 背面

论断：数据竞争（无同步的并发冲突访问，且至少一方为写）是 C++ 未定义行为，不是"结果偶尔算错"—— 编译器可据此做激进优化，行为完全不可预测。TSan 能检测数据竞争，但"没被 TSan 报 ≠ 没有数据竞争"： 其漏报边界（插桩盲区 / 必须全量插桩 / 时序偶发）使工具沉默不能当作安全证明。
边界：{'standard': ['C++11', 'C++14', 'C++17', 'C++20', 'C++23'], 'compilers': ['GCC 15.3.0 (MinGW-w64)', 'GCC 14.2.0 (WSL)', 'GCC 13.3.0 (WSL)'], 'opt': ['-O2'], 'platform': ['x86-64']}
关键证据：
  - EV-CONC-005: confirm
  - EV-CONC-006: confirm
常见误解：
  - 误以为数据竞争只是结果偶尔算错，不是 bug
  - 误以为没被 TSan 报就是没有数据竞争（忽略漏报边界）
  - MIS-CONC-003
