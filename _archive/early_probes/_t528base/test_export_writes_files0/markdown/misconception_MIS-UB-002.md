# MIS-UB-002（misconception）

## 正面

【误解】MIS-UB-002 有符号整数溢出会像无符号一样回绕
触发说法：int 加到最大就变成负数，循环自然退出

## 背面

为什么错：有符号溢出是 UB，无符号才是定义好的模运算——两者规则不同，不能类推
反例 1：实测后果：GCC -O2 依此把 for (int i=0; i>=0; ++i) 编译成无条件 jmp 死循环
关联原子：ATOM-UB-GRAY-001
