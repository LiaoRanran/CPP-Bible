# MIS-UB-007（misconception）

## 正面

【误解】MIS-UB-007 读未初始化的变量只是拿到一个垃圾值，不算 UB
触发说法：未初始化的 int 读出来是个随机数而已

## 背面

为什么错：读取不确定值是 UB（陷阱表示亦在此列），优化器可假设它取任意值并据此删分支
反例 1：结构体 padding 读取同理：即使用 memset 比较也会因 padding 不确定而失败
