# 00 总览 · 调研：每改一版到绿 回路还能再压吗（且不削弱判决）

- 检索日期：2026-09-18
- 解释器：`.venv\Scripts\python.exe`（Python 3.13.13）｜ 仓库：`C:\CodeLearnling\note\note\C++\CPP-Bible`
- 立场：只读调研员，未改任何正式代码；所有产出落 `_arch_v8/`
- 开工 / 收尾各跑 `git status --short` 自证（见 01 末尾 + 本文件"副作用"段）

---

## 一句话核心判断

**回路现在根本不是绿的，先谈"压到绿"没有意义；而在"不削弱判决"的前提下，能安全砍掉的时间极少——两个历史方向里 B4（网关索引化）ROI 仅 ~1.3s 且带缓存失效风险，应归 W；B3（受影响测试选择）是唯一能砍测试时间的杠杆，但"跳过判决测试"=削弱可信度，必须归 W（先攒依赖图与漏捕率数据，证明完备性后再谈）。**

---

## 最该先动的一件事

**先把 580 重构的"未完工"收尾**：把 `poison_drill.py`、`tests/test_p0g_lock.py`、`tests/test_replay_lock_serial.py`、`tests/test_mutation_parallel_580.py`、`tests/test_mutation_isolation_579.py` 里残留的 `_REPLAY_LOCK` 引用改到已提交的 `_replay_lock_path()`（HEAD `f574b76` 已改 `atom_evidence_replay.py`，但依赖方没跟上）。否则 fast / poison 继续红 / 崩，"压到绿"无从谈起。

> 这是给建设者的建议。本调研员按纪律不改正式代码、不 golden accept、不 push。

---

## 实测数字（2026-09-18，32 核，`.venv`）

| 阶段 | 命令 | 实测墙钟 | 现状 |
|---|---|---|---|
| fast | `pytest -m "not slow" -n auto` | **70.2s** | 🔴 RED：7 失败 |
| slow | `pytest -m slow -n0` | **203.4s** | 🟡 1 失败（`test_golden_lock_json`） |
| gate | `tools/gate_engine.py --check` | **3.8s**（warm 4.06s，无跨进程缓存） | ✅ pass（block=0 warn=186 advice=5） |
| poison | `tools/poison_drill.py` | **崩溃** | `AttributeError: module 'atom_evidence_replay' has no attribute '_REPLAY_LOCK'` |
| 最大单测 | `tests/test_task_queue_stateful.py::TestTQ::runTest` | **79.4s** | 占 slow 总时 39% |

> 与你给的假设对照：
> - fast 你估 35–42s、`pyproject.toml` 注释写 ~10s → 实测 **70s**（两者都错）。
> - slow 你未给数、`pyproject.toml` 注释写 ~140s → 实测 **203s**（注释也错）。
> - full serial 你估 589s → **未实测**；单测两阶段并行才是实际回路，全量串行非优先路径，故不花 10 分钟去量（缺数据，不填数）。

---

## ⚠️ 实测副作用（必须声明，不掩盖）

跑测试套件时，其"还原真实仓库"逻辑把两个**被跟踪但未提交**的文件 `git checkout` 回了 HEAD：

- `tools/atom_evidence_replay.py`、 `tests/test_p0g_lock.py` 的**未提交增量编辑被销毁**。
- dangling blob 检索无果（它们从未进 index/commit），**git 内不可恢复**。建议用编辑器本地历史救。
- 根目录被测试生成 7 个临时文件（`_cmp580.py` `_j1_580.out` `_p580a.txt` `_p580b.txt` `_recon580g.py` `_recon580h.py` `_worklog_580.md`），已移到仓库外 `c:\Users\ASUS\CodeBuddy\20260918085412\_arch_v8_rescue\` 保留，未销毁。

**关键陷阱**：这两个文件现在 `git status --short` 不再出现（已 == HEAD），所以"git 显示零改动"是**假象**——真实损失是未提交工作。收尾 `git status --short` 验证见 01 末尾；正式目录（atoms/evidence/Examples/tools/tests）除这两处已提交态外**无新增修改**。

---

## 文件导航

- `01_现状解剖.md`：回路组成、各阶段耗时拆解、为什么 fast 不是 10s、为什么现在 RED、B3/B4 不落地的真原因、副作用细节。
- `02_外部做法.md`：2025-26 业界按变更选测试 / gate·lint 增量缓存，每条带来源链接 + 检索日期 + 已查证/一方称。
- `03_NDW三档.md`：N（现在能做、零 Oracle 风险、机器验收）/ D（攒数据）/ W（等条件）三档；N 每条给改文件 + 验收 + 回退。
