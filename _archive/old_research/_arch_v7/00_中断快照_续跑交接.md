# 564 中断快照与续跑交接（积分中断，非完整交付）

> 状态：trae 于 2026-09-17 下午执行 564 时积分不足中断。**本文件是抢救性快照**，把已实锤但只存在于会话上下文、尚未成文的发现固化，供明天续跑（建议仍由 trae 异族续，保持独立性）。已完成项不必重跑。
> 标注：【trae实跑】= trae 会话报告；【豆包亲验】= 监工独立复核。

## 一、已完成、已落盘、可复跑（不用重做）

### A0 本仓弱 Oracle 解剖 —— 核心发现已成立
- 【trae实跑+豆包亲验】`misconceptions/` 目录**正好 80 个文件**；抽样卡 frontmatter **无** `run_commands / compile / artifact_sha256 / run_match_file / actual`，仅个别有 `relations`。
  → **80 条 MIS 误解库是本仓真正"零编译 Oracle"的内容**：gate 只查结构合规、关联只查 ID 是否存在，**没有任何机器对"这条误解的纠偏结论是否为真"做判决**。字段可以填满、机器没真判。这是元系统跨域的第一个真实样本，也是 A2/A3 最硬的输入。
- 【trae实跑】`evidence/hist/`、`evidence/ub/`、多数 `evidence/lang/` **并非零编译**：历史结论卡靠"c++14 能编 / c++17 移除"这类**编译器版本可用性证据**挂真编译（Oracle 是"某特性在某版本可否编译"，不是结论本身被证明）。续跑时要把这类"弱但非零"的 Oracle 与 MIS 的"纯结构"Oracle 分档，别混为一谈。
- 续跑待补：把这三类各抽 2-3 张，列表写明 replay 实际走到哪一步、三分类/P0-A/artifact_sha 在它们身上退化成什么。

### C0 meta 层攻击 —— 两个 PoC 已实锤、可复跑（只读，monkeypatch/TEMP 副本，不写正式目录）
脚本：`_arch_v7/probes/poc1_rule_supply_chain.py`、`poc2_poison_self_attest.py`。
复跑（避开苦力跑门禁的时段；ge.run 为只读扫描但仍建议错峰）：
`.venv\Scripts\python.exe _arch_v7\probes\poc1_rule_supply_chain.py`
`.venv\Scripts\python.exe _arch_v7\probes\poc2_poison_self_attest.py`

**PoC1 规则供给链（三件）**：
- A. 把当前命中最多的规则其 `check` 在进程内换成 `return []`（= 攻击者改 gate_engine.py 一行的运行等价物），**77 条 finding 静默消失、gate 自身无任何完整性告警**（当前库 block=0 全 warn，攻击对 warn/block 同构；对 block 规则消失的就是拦截）。
- B. `tools/tool_integrity.py` 的 sha256 基准**能**检出文件被改（防线存在），但它是**独立命令，gate_engine 单跑时不调用它** → 日常 `--check` 路径沉默。
- C. 基准用 `--update` **无认证重签**：改完规则顺手重算即自洽（TEMP 副本演示），信任根最终只落在 git 留痕 + 人审 commit，**无加密签名**。

**PoC2 毒样例/覆盖率供给链（两件）**：
- A. RULE-COVERAGE 的 covered 集合是用正则在 poison_drill **自己源码**里 grep `"RULE-ID" in who` 文本得到的——**注入一行这种字符串、0 个真载荷，covered 即 +1**（历史"覆盖正则只认字面量"工程坑，被实证为可谎报覆盖的自证漏洞）。
- B. `poison_exemptions.yaml` 豁免由工具自解析、**无签名**：自写一条 id/reason/date 即可把任意未覆盖规则永久移出分母（load_exemptions 原样接受），CI 因此转绿。

→ 这 5 点是 C1 最小加固的直接输入（方向：gate 启动内联 tool_integrity 自检或 CI 强制串接；覆盖率必须要求"载荷真实触发且断言 severity"，不认裸字符串；豁免台账需异方/人签 + 不可自批；关键规则人锁）。续跑时把每条写成"现状→攻击→最小加固→现在能做/等L3→存量零误伤核验"。

## 二、中断时正在做（半成品，续跑接着走）
- **B0 人审解剖**：刚开始（只跑了 1 条命令去摸 `principal_ok` 与 `golden_lock accept` 实际流程）。续跑：逐环节指出 136 warn 人审、golden 单签 accept、principal_ok、inference 人签在什么结构条件下允许 rubber-stamp。
- **D 推理引擎数据盘点**：刚开始。续跑先数命题边 / concept_edges / relations / 攻击支持边的真实规模，再驱动 Datalog vs ASP(clingo) vs Soufflé vs DuckDB/SQLite 递归 vs 手搓 Python 的选型；推荐 clingo/Soufflé 必须核 Windows 安装与轻依赖可行性。

## 三、完全未做（按 564 任务书补齐）
- A1 Oracle 类型学表（可计算强度维度：可复现性/错误率能否给上界/单次成本/对人依赖/能证什么不能证什么）；
- A2 内核迁移清单：逐模块标【编译器强耦合 / 弱 Oracle 也能跑 / 完全领域无关】，产"抽走 g++ 后当场能跑 vs 立刻归零"两张清单；
- A3 新领域 Oracle 发现 SOP + 多 Oracle 冲突仲裁 + 本质无强 Oracle 领域的处置；
- B1 ≥3 条"让人审无法走过场"护栏（带机器判据）+ 人审错误率测量方案（盲埋雷/复核一致性/抽检）与单用户极限；联网取 automation bias / rubber-stamp / calibrated trust / 连续审计 2025-26 实证（要数字要链接）；
- C1 上表 5 点的最小加固成文；
- E 开放激励终局（篇幅最小，默认冻结，写解冻的可判定信号）；
- `00_总览_现在做_攒数据_等模型.md`：每条"现在做"过三关（存量零误伤、弱模型可机械执行、不依赖模型变强）。

## 四、续跑硬约束（沿用 564）
- 只读 + 产物隔离 `_arch_v7/`；探针不改正式文件；git status 自证，甄别并行苦力会话 WIP。
- 文献为判断服务、每块精选、不许堆数量；每个论断挂链接(+检索日期)/文件行号/探针输出；区分已实现/设计/未实现/理论未实证。
- 统计口径若引用 563：strict=615/956、treated=729/956，warn_only 永不算严格拦截（563 N9 已是误报，勿再咬基线）。
- 不重复 560-563 已覆盖项（见 564 反重复清单）。
- 注意：执行期间苦力 559 可能在跑 pytest/golden/poison 收工门禁，**勿并发跑 replay/poison/真编译**；ge.run() 只读扫描可错峰短跑。

## 五、给监工(豆包)的验收要点
- 两个 PoC 续跑后应仍稳定复现 5 点；若苦力随后改了 tool_integrity/poison，结论需重核。
- A0 的"80 条 MIS 无真判决"是跨域论断的基石，要求 A2 明确回答：MIS 这类内容在当前架构下是否应允许出现 verified 状态（预期：不应，只能到结构合规/证据链组织层）。
