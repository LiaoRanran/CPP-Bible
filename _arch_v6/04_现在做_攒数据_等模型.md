# 563-04 · 现在做 / 攒数据 / 等模型（每条挂机器验收）

> 本轮纪律：只出规格不落地；"等模型"必须写触发条件，不给万能解。数字依据全在 [02](file:///C:/CodeLearnling/note/note/C++/CPP-Bible/_arch_v6/02_可计算判据集.md)。

## 【现在做】纯机械脚本/字段，苦力 <1 天，零模型依赖

| # | 事项 | 喂什么→吐什么 | 机器验收（可钉） |
|---|---|---|---|
| N1 | **统计上界工具** `tools/stat_bounds.py`（probe_bounds.py 正规化）：cp_upper/interval/wilson/zero_failure_n | n,k,conf→上界/区间/样本量 | 与 scipy.stats.beta 分位对拍（若环境有 scipy）±1e-6；k=0 解析解 n=59→U≈0.0500；内置进 metrics_collector |
| N2 | **每批复测自动带置信区间**：poison/mutation 报告强制分子+分母+区间，修掉基线文件 strict_rate 0.6433 与重算 0.6136 的口径漂移 | full_baseline json | 报告含 blocked/escaped/n_a 三原始计数；比率禁止无分母单独存在；M2 逃逸区间必须随报告输出 |
| N3 | **grounded 论证视图**（03-A）：命题/攻击边→三值标注+级联最小改变 | claim_structured+relations+当批复 replay | §A.3 五个 pytest（空集/自攻/互攻/单 refute/幂等）+3 真实案例人工对照一致 |
| N4 | **分权元数据**：规则记 drafted_by/reviewed_by model_family；评审记录 vendor family | gate registry/红队记录 | 同族起草+审查的规则 gate warn 拒绝生效；同族一致标 correlated |
| N5 | **三条红线计数进 metrics**：same_family_draft_review（应0）、single_signer_accepts（趋0）、llm_veto_over_oracle（恒0） | task/golden 事件 | 三指标在 metrics.jsonl 有字段；llm_veto_over_oracle 非 0 即 CI 红 |
| N6 | **复证分类器**（03/组5）：按 claim_boundary 分工具链敏感/不敏感命题 | frontmatter 聚合 | 输出两清单+计数；与人工抽查 5 张卡分类一致 |
| N7 | **Red Gate 覆盖度**：有阴面且实测翻转的 observation 占比 | negative_controls 字段 | 数字可复算；无 Red Gate 的 observation 卡名单可列出（先 warn，升 block 需另批） |
| N8 | **三方编译器不一致率**（组1） | matrix 逐单元结果 | `toolchain_disagreement_rate` 入 metrics；分歧单元清单可列 |
| N9 | **修正 mutation 基线文件口径**（02 §3 发现的不自洽） | full_baseline_v1.json | 重新生成后 strict_rate=blocked/variants 与计数自洽；README_v1 标注两个口径定义 |

## 【攒数据】机制/字段先有，数据够了才允许声称/开门

| # | 事项 | 开门数据（可计算） | 不达标时的结论 |
|---|---|---|---|
| D1 | **G-iso/G-llm-screen 的 EIR 盲测**：≥59 条**异族独立构造**命题（0 误判→误判率上界≤5%@95%）；要 2% 精度需 149 | 盲测集 k=0 且命题来源不与建设卡同分布 | 不足 59 或题目自建→只能写"56 卡 0 误伤（同分布观察上界 5.21%）"，禁止写 EIR≤5% |
| D2 | **mutation 逃逸率连续曲线**：每批同口径跑，≥3 批同算子序列 | 逃逸率 C-P 区间随批单调收窄或稳定 | 区间在扩大→拦截在退化，禁止收窄红队规模 |
| D3 | **单调收敛三曲线**（562 §3.3 落地）：逃逸生存时间、Oracle 升级推翻数、误伤率时间序列 | ≥3 个批次时点 | 只有 1 个时点（现状）→"单调收敛"不可声称 |
| D4 | **异族漏报率上界**：红队带锚攻击被评审漏报的二项监控 | 累积 n≥29（上界 10%）起报告 | 样本不足只列计数不算率 |
| D5 | **M3 算子补样本**：当前 13 可判/75 N/A，区间 [54.6%,98.1%] 无法定性 | 可判 n≥59 | 不足前 M3 在报告标"insufficient evidence" |
| D6 | **golden 双签流程数据**：异族分类 vs 人分类分歧率基线 | ≥4 个完整批次 | 无分歧基线不单人签的制度只先 warn |
| D7 | **等价变异体甄别**：escaped/n_a 中语义等价项人工标注比例 | 抽样 ≥100 变体 | 未甄别前逃逸率标"含等价噪声，可能高估" |

## 【等模型/等条件】触发条件写死，禁止"等 AGI"式立项

| # | 事项 | 具体触发条件（可机器/事实判定） | 为什么现在不行 |
|---|---|---|---|
| W1 | LLM 评审获得**放行权**（从建议升否决以外的正向权） | 异族陪审团在 D1 盲测集误判率 C-P 上界 ≤1%@95%（需 n≈460 零误判量级）且 reconstruction 类攻击有可复现封堵证据 | 2026 证据：异族只去相关误差、仍有未堵盲区；不可判定性定理（562）封顶 |
| W2 | 命题自动入图/自动连边解禁 | embedding/抽取边在人盲标注集精确率 C-P 上界≥99% 且 529 冻结项由人正式解冻 | 自动 KG 入库是战略冻结项；语义边必须带 provenance |
| W3 | 编译器 TCB 换已验证编译器 | 出现支持现代 C++（≥C++17）的、有机器核验正确性证明且性能可接受（≥GCC -O1 的 90%）的编译器 | CompCert 仅 C；C++ 无对等物（2026-02 事实） |
| W4 | 命题级 Lean/SMT 自动证明进闸 | claim 语言域出现命题体系（当前是经验事实"asm 里有没有 lock"，非定理）+ 561 路线的 SMT 通道对 UB/溢出类性质证伪率有实测上界 | 形式化对象尚不存在；561 已划可证边界 |
| W5 | LLM 生成 Red Gate/性质不变量免人审 | LLM 生成性质的误报/漏报在独立集有 ≤1% 上界（SolidCoder/VeriSpecGen 2026 数据是"有帮助"非"可信"，86.6% 规格合成都不够） | 生成端义务可前移，但义务的正确性仍需机验 |
| W6 | 系统自起 worker / 自动修宪 | 永不（结构禁令，非能力问题）：G-supervisor OFF、人持立法与终审 | 562：接管与制宪权归人是底座不存在下的唯一自洽选择 |

## 批次顺序建议（给苦力的依赖链）

```
N1 统计工具 ──► N2 报告口径 ──► D1 盲测（异族任务，非苦力）
N3 grounded视图 ──► 03-A.4 级联划界
N4 分权元数据 ──► N5 三红线 ──► D6 双签
N6/N7/N8 三个覆盖率字段（可并行，都是 frontmatter 聚合）
N9 基线修正（独立小批，先做，否则后续比率全带偏）
```
建议苦力**第一批只做 N9+N1+N2**（半天量级）——先让所有后续比率建立在自洽口径上；其余按周排。

## 一页总结（本轮回答的那个数字）

**本周就能变成真数字的形容词是"零误伤"：56 卡 0 block 的精确含义是"单卡误伤率 95% 上界 5.21%（rule3 近似 5.36%），不是零"；它附带一个立刻可用的行动数——要对异族独立命题宣称"误判率≤5%@95%"，盲测集至少 59 条且 0 误判（2% 精度需 149 条）。** 同一份数据还顺手暴露了两个被平均分掩盖的硬事实：M2 算子 207/207 全逃逸（区间 98.2%–100%）、基线报告 strict_rate 0.6433 与重算 0.6136 口径漂移。