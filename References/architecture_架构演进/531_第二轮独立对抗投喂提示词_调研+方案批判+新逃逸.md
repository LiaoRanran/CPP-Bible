# 531 · 第二轮独立对抗 + 调研 + 方案批判（投喂独立模型）

> **你的身份**：独立红队 + 外部调研员。与建设者**不是同一实例**，不接受"建设者说有问题/没问题"作为证据。
> **仓库**：`C:\CodeLearnling\note\note\C++\CPP-Bible`，Windows + PowerShell，门禁用 `.venv\Scripts\python.exe`。
> **你的产出**：全部落在 `_adv_round2/`（批判报告.md、探针脚本、复现说明），**正式目录 atoms/ evidence/ tools/ Book/ 零改动、零 commit、零 push、零 golden accept**。
> **本会话已第 6 轮同型审计**（368→373→402→452→470→上轮批判）。前 5 轮的两阶段盲读/沙箱 monkeypatch/数字抽验/可复现探针/零污染自证模式你都熟悉。**本轮增量要求见下，不许把上一轮换个数字重交。**

---

## 一、先做这件事（10 分钟定调）：自己实测基线，别信任何人的自报

```powershell
.venv\Scripts\python.exe tools\gate_engine.py --check
.venv\Scripts\python.exe tools\poison_drill.py
.venv\Scripts\python.exe tools\atom_evidence_replay.py --check --no-sanitizer
.venv\Scripts\python.exe -m pytest tests/ -m fast -q
.venv\Scripts\python.exe tools\knowledge_graph.py stats
git rev-parse --short HEAD
```
把你**实测**的规则数/block/warn/poison 断言数/replay confirm 数/pytest 点数/kg 节点边数写进报告开头。**任何与建设者口径不符处，以你实测为准并标红**——上一轮就抓到过"自报41实际157、自报152实际155"的失真。

---

## 二、三选一强制任务（三件都要交，不是选一件）

### A. 外部调研：找我们没吸收的新攻击面/新方法（不抄已饱和项）
- 已知参考（**不许只复述**）：AutoVerifier (arXiv 2604.02617)、Tool Receipts (2603.10060)、PROClaim (2603.28488)、NIST agentic eval probes、Argos (MSR)、TTHE harness evolution、DSE silent failures、property-based test workflow。
- **你要做**：判断这些方法里，**哪一条还没被本系统吸收、且现在就能落地**；或去找 2025-2026 更新的 agentic verification / reward hacking / verifier-gaming 新论文或工程实践，指出本系统"验证器博弈"还没防住的形态。
- 每条必须说清：来源、它针对的攻击形态、本系统当前是否已防、建议怎么补。不要泛泛而谈"可以引入LLM"——Oracle 铁律：LLM 永远不能当最终闸。

### B. 对 530 施工方案做设计批判（重点，别只打现状）
下一批要落地的方案是 `References/architecture_架构演进/530_批次H施工提示词_让已建结构兑现承诺_最终施工版.md`。**它还没执行**。你要攻击的是：
1. **这个方案落地后会引入什么新洞**？例如：
   - T1 把 .out 纳入指纹——攻击者改 .out 内容让指纹变但断言仍过，是否绕过？
   - T2 把 `.`/`%` 开头符号判为无判别力——会不会误伤"夹具特有伪指令"的合法断言？有没有**第三种汇编前缀**（如 `@` 符号、`#` 行号、`$` 局部标签）漏网仍恒真？
   - T3 拆 CONTRASTS/CONTRADICTS 后，关系写法本身还有没有第 8 种同构变换能静默丢边（上一轮 E07 缩进走私只修了5变体）？
   - T4 OBSERVATION-LIVENESS 三条活性条件——能否构造"三条都机械满足、但命题仍是无观测含义的推断"？这正是它要堵的绕过。
   - T5 强制 --classify 四分类——分类本身会不会变成新的自我合理化逃生舱（real/false_positive 由谁判）？
   - T7 SQLite 队列——`BEGIN IMMEDIATE` 在 Windows SQLite 的并发语义、deps JSON 注入、worker 冒名，有没有洞？
2. **530 提示词本身的漏洞**：哪些任务差模型一定做歪？哪些验收判据能被"假通过"？（参考上一轮你抓出的：含降级分支不断言 severity、毒样例 who 字段正则盲区。）
- 每个设计批判必须落到"改哪个文件/哪段逻辑"，不要停在"这里可能有风险"。

### C. 对当前磁盘系统跑可复现探针（只报新逃逸）
- **已饱和，不要重复打**（5轮都打不出新洞，再打就是凑数）：P0-A 重编译不变量、人签名册 principal_ok、cl 免检链、YAML 缩进硬化、recompile tampered。
- 重点试**还没打透的面**：增量 replay 指纹、contains_in 跨编译器漂移、warn 整体 accept 的逃生路径、claim_structured 自标绕过、概念图边语义、并发 replay/golden 互斥。
- 每个逃逸必须：①给出可复现探针（落在 `_adv_round2/probes/`，能跑出 verdict）②说明它为什么绕过现有规则③定级（block 可直推 verified / warn 可见 / 设计观察）。**找不到新逃逸就明说"本轮饱和"，不许编。**

---

## 三、纪律（违反则报告作废）

1. **数字必须实测**：自报/文档/提示词里的数字一律重测，实测不符就标红，不替建设者圆。
2. **攻击按数据流/时序穿链，不按字符串单点**：上一轮的核心教训——"每点都合规"的组合才是真逃逸。
3. **零污染**：交报告前 `git status --porcelain` 证明 atoms/ evidence/ tools/ Book/ 无改动；探针全在 `_adv_round2/`。
4. **不替建设者改代码、不 commit、不 push、不 accept**。
5. **饱和即停**：你判断某面已无新洞，明说理由，不要为凑数硬报"观察"。
6. **上一轮你的误判要自查**：上轮有一处你断言"IFNDR 不可能是 16.2 子条"被原文核伪——本轮推断级结论先核原文再下。

## 交付
- `_adv_round2/批判报告.md`：A 调研结论 / B 530方案设计批判（按 T1-T7 逐条）/ C 新逃逸清单（探针路径+verdict+定级）/ 与上轮对比的增量说明 / 饱和声明。
- `_adv_round2/probes/`：每个 C 逃逸一个可复现脚本 + 期望输出。
- 一句话总结放报告开头：本轮最大发现是哪条、它为什么是前 5 轮没抓到的。
