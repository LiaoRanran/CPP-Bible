# 592 全库审视 · 状态盘点与问题清单

> 审视时间：2026-09-19 · HEAD=6346b26（589）· 苦力 591 并行执行中
> 审视方式：只读勘察 + fresh 门禁实跑 + 基线/数据文件回读，不凭记忆
> 纪律：所有数字标注来源与口径，问题附证据，建议附前提

---

## 一、执行摘要

**一句话核心判断**：系统的"机械判决层"已经接近单用户结构上限（契约逃逸 1/1379，C-P95 上界 0.40%），但"数据一致性层"和"人审闭环层"存在明显缺口——metrics 曲线停在 v1、overturned 通道未建、golden 持续预期红、50 条命题缺活性锚、332 commit 未 push。机械层越强，这些"非机械缺口"越成为系统可信度的短板。

**关键数字（fresh 实跑或文件回读）**：

| 维度 | 数字 | 来源 |
|---|---|---|
| 门禁 | gate exit=0 · 63 规则 · 191 命中 (block=0 warn=186 advice=5) | fresh 实跑 |
| 复算 | replay confirm=56 refute=0 infra_error=0 | fresh 实跑 |
| 完整性 | tool_integrity OK（5 CORE 一致） | fresh 实跑 |
| 毒样例 | 124/124 · RULE-COVERAGE 39/63 · 诚实 95.2% | 589 收工（本轮未跑，避 591 冲突） |
| 基线 v6 | 1593 变体 / blocked 1378 / escaped 1 / n_a 179 / equivalent 35 | 文件回读 |
| 唯一逃逸 | M1 TCE（EV-CONC-001，删 negative_controls） | v6 by_operator |
| 命题 | 79 条 / 27 卡 · inference 29 / observation 50 · 命题级签署 0 | prop_graph stats |
| 测试 | fast ≈85-90s (464 passed) · slow ≈645s (334 passed / 1 预期红) | pyproject 注释（589 实测） |
| 未 push | 332 commit | git rev-list |
| 工具数 | 130+ tools/*.py · 72 tests/test_*.py | 文件枚举 |

---

## 二、仓库基础状态

### 2.1 Git 状态
- **分支**：master · remote `git@github.com:LiaoRanran/CPP-Bible.git`
- **未 push**：332 commit（ssh exit141，需用户切热点手推，agent 不自推）
- **工作树 M**：2 条 CRLF 行尾假脏（`data/mutation/full_baseline_v4.json`、`evidence/conc/EV-CONC-001.md`），内容 diff 为空，开工前即存在，勿提交勿还原
- **untracked**：大量历史临时件（详见 §9 卫生债）

### 2.2 目录结构（核心）
```
atoms/          27 张原子卡（命题载体）
evidence/       56 张证据卡（编译/运行/sha256 复算）
misconceptions/ 79 条 MIS 误解库（590 核实，非 80）
Examples/       13.4MB / 1552 文件（真实编译工件）
tools/          130+ .py（gate_engine 209KB 最大）
tests/          72 test_*.py + conftest.py（140 行）
data/           mutation 基线 v1-v6 + metrics + oracle_registry + prop_liveness_todo
build/          编译缓存 + replay_manifest + .replay_lock（gitignore）
References/architecture_架构演进/  530-591 投喂词 + 调研归档（60+ 文件）
_arch_v2-v11/   历轮只读调研原始产出（untracked）
_worklog_*.md   历轮工作日志（untracked，403-589）
```

---

## 三、门禁与判决层

### 3.1 现状
- **gate_engine.py**（209KB，CORE）：63 条规则，191 命中，block=0 / warn=186 / advice=5
- **三判决入口**（gate / replay / poison）启动首句强制 `enforce()`，fail-closed
- **tool_integrity.py**：5 CORE 工具 sha256 基准，改后必须同 commit `--update` 重钉
- **warn 186 的构成**：50 条 OBSERVATION-LIVENESS（命题缺活性锚，575 新增）+ 136 条存量（575 之前）

### 3.2 已收口的逃逸（历轮建设成果）
| 批次 | 收口内容 | 逃逸变化 |
|---|---|---|
| 570 | -Werror 逐诊断编译行绑定 | M3 去 -Werror 逃逸 1→0 |
| 572 | 断言计数基线 + any-of 通用候选检测 | M3 真洞 52→0 |
| 574 | M5 尺子 bug 修复（claim_structured 块内定位） | M5 0/0/83 → 29/0/56（29 条活雷登记） |
| 575 | 命题级活性锚（OBSERVATION-LIVENESS warn） | M5 报告口径 29 逃逸→规则命中（578 翻盘） |
| 587 | matrix 取值校验（warn 起步，逐元素） | M6 非法值逃逸 204→0 |
| 588 | M6/M4 行尾注释漏网 + GATE_READ_KEYS 补 matrix + CRLF 健壮性 | 发现器形态完备化 |
| 589 | M2 注释净化（字段块内注释路径不再造伪变异） | M2 27 条等价伪变异→un-mask 出 27 条真实 blocked |

### 3.3 未收口的逃逸
- **M1 TCE escaped=1**（EV-CONC-001，删 negative_controls）：最难，需工件/TCE 比对，已冻结为单独攻坚包
- **M5 29 条活雷**：inference 改标 observation 后 OBSERVATION-LIVENESS warn（观察期，不 block）

### 3.4 问题
- **P1（中）**：gate 输出的 warn 详情在终端被截断，完整的"规则数/命中数汇总行"不在最后 5 行——建议 gate 末尾固定打印一行 `[SUMMARY] rules=N hits=M block=B warn=W advice=A`，便于自动化采集
- **P2（低）**：warn 186 中 50 条是 575 新增的"待补锚"观察期告警，与 136 条存量告警混在一起——建议报告层区分"观察期新增"与"存量"，避免 golden 恶化判断被新观察期告警淹没

---

## 四、变异发现器与基线

### 4.1 基线谱系
| 版本 | 变体 | blocked | escaped | n_a | equivalent | 关键变化 |
|---|---|---|---|---|---|---|
| v1 | 1188 | 729 | 227 | 232 | — | 初始（M2 207 假逃逸） |
| v2 | 1181 | — | — | — | — | M3 扩面（actual 键修复） |
| v3 | — | — | — | — | — | M5 尺子修复（29 活雷） |
| v4 | — | — | — | — | — | 命题级活性锚 |
| v5 | 1183? | 989 | 9 | 185 | — | 工件层根隔离（579） |
| v6 | 1593 | 1378 | 1 | 179 | 35 | 发现器形态完备化（588）+ v6 重冻结 |
| v7 | 1593 | 1405 | 1 | 179 | 8 | 待 591 冻结（M2 un-mask 27 条真实 blocked） |

### 4.2 v6 by_operator（文件回读）
| 算子 | blocked | escaped | n_a | equivalent |
|---|---|---|---|---|
| M1 | 64 | 1 | 27 | 0 |
| M2 | 141 | 0 | 27 | 27 |
| M3 | 65 | 0 | 42 | 0 |
| M4 | 224 | 0 | 27 | 0 |
| M5 | 29 | 0 | 56 | 0 |
| M6 | 716 | 0 | 0 | 8 |
| M7 | 139 | 0 | 0 | 0 |

### 4.3 问题
- **P3（高）**：v6 在 589 T2 修复（d36d5c8）后已 27 条不匹配——v6 的 M2=141/27eq 是修复前的数字，修复后真值是 M2=168/0eq。v6 已不是权威基线，但文件仍在且 metrics 可能引用它。v7 冻结（591 任务1）是当前最高优先级
- **P4（中）**：v1-v5 基线文件全部保留（600-700KB each），但没有"哪个版本是当前权威"的显式标记——建议 `data/mutation/CURRENT_BASELINE` 指针文件或在每个基线 JSON 顶层加 `authority: true/false`
- **P5（低）**：equivalent 字段在 v6 顶层叫 `equivalent`，by_operator 里叫 `equivalent_invalid`——命名不一致，建议统一

---

## 五、数据与度量层

### 5.1 metrics.jsonl
- **仅 3 条记录**：2026-09-14 ×2 + 2026-09-17 ×1
- **最后一条**（2026-09-17T17:40:45，565b 恢复采集）：
  - gate: 61 规则 / warn 136（已过时，当前 63/186）
  - poison/replay: null（`--no-heavy` 跳过）
  - curves.mutation_escape_rate: **v1 的 0.2374（227/956）**——严重过时
  - curves.overturned: 0（真值，无产生路径）
  - curves.escape_survival: null（无数据）
  - monotone_convergence: "不可声称（只有 1 个时点）"

### 5.2 其他数据文件
| 文件 | 状态 | 问题 |
|---|---|---|
| `data/oracle_registry.json` | 存在，3 oracle，放权全 OFF | 83 卡 0 张填 verified_by_oracle |
| `data/overturned_events.jsonl` | **不存在** | 573 设计了 schema 和 CLI，但文件从未创建——0 事件时应该有空文件或显式"未启用"标记 |
| `data/prop_liveness_todo.md` | 存在，50 条 | 人审旧账，机器不代填 |
| `data/mutation/selfcheck_cards.json` | 存在（589 新增） | 8 卡固定小卡集，确定性自检用 |
| `data/governance_docs_manifest.json` | 待 591 创建 | A1 防御 |
| `data/governance_weakening_scan.json` | 待 591 创建 | A1 防御 |
| `tools/golden_state.json` | 存在（590 核实，在 tools/ 非 data/） | golden 状态快照 |

### 5.3 问题
- **P6（高）**：metrics curves 停在 v1（0.2374），与当前 v6（1/1379≈0.0007）相差 340 倍——任何读取 metrics 的报告/仪表盘都会显示错误的逃逸率。565b 恢复采集时只跑了 `--no-heavy`，没更新 curves 字段。建议下一批 metrics 采集时强制更新 curves 到当前权威基线
- **P7（中）**：overturned_events.jsonl 不存在——573 设计了 `--log-overturned` CLI 和 schema，但从未有人调用过。0 事件时文件不存在会让"检查是否有推翻事件"的代码需要处理 FileNotFoundError，且无法区分"通道未启用"与"0 事件"。建议创建空文件 + 首行注释说明
- **P8（中）**：metrics 最后一条的 gate 数字（61/136）已过时（当前 63/186）——采集间隔太长（9-14 到 9-17 隔 3 天，之后再没采集过）。建议 metrics 采集进 CI 或每批收工时自动跑一次
- **P9（低）**：golden_state.json 在 tools/ 不在 data/——590 已指出，位置不一致

---

## 六、命题与论证层

### 6.1 现状（prop_graph stats fresh 实跑）
- **79 命题 / 27 卡**
- **类型**：inference 29 / observation 50
- **签署**：card_signed 76 / unsigned 3（3 条全部来自 ATOM-LANG-INLINE-001 prop-1/2/3）
- **命题级 signed_by**：0 条（574 加了可选字段，无人填）
- **机验**：79 条全部有机器锚点
- **anchor_source**：atom 卡 27/27 自身无锚，全部靠证据卡（evidence）

### 6.2 命题级活性锚（575 新增）
- 50 条 observation 命题缺 `liveness` 字段 → OBSERVATION-LIVENESS warn
- 清单：`data/prop_liveness_todo.md`（50 条，人审旧账）
- 机器只检查"锚是否真实存在于引用卡 artifact_assert + 非通用 + 非散文"，不代填

### 6.3 论证层（grounded / Dung）
- **当前状态**：无"全局可接受命题集"答案——563/564 调研设计了 grounded 三值标注方案，590 路线图 R4 列为关键路径，但**未落地**
- **知识图谱**：`tools/knowledge_graph.py`（36KB）存在，但 7 类旧边集，582 调研指出 KG 边仍是旧集（E1/E2/E3 未落地）
- **prop_closure.py**：583 设计了命题级闭包双实现（Py vs WITH RECURSIVE），但**未创建**（582 调研列为 N2，583 只做了 N5/N1/N3）

### 6.4 问题
- **P10（高）**：论证层是 590 调研指出的"差距最大三处"之首——79 命题之间的攻击/支持关系没有全局可接受集计算，命题网络只是"有向图"而非"论证框架"。这是从"可用"到"令人信服"的关键缺口
- **P11（中）**：3 条 unsigned 命题（ATOM-LANG-INLINE-001 prop-1/2/3）持续未人签——566 已列出，590 再次指出，人审旧账
- **P12（中）**：prop_closure.py 未创建——582 N2 设计了命题闭包双实现对账，583 没做，591 也没做。命题网络的"可达性/闭包"计算缺失
- **P13（低）**：知识图谱 7 类旧边集，582 E1/E2/E3（external_leads/EV-LEAD-CITATION/memory_query）未落地

---

## 七、测试与覆盖层

### 7.1 现状
- **72 个 test_*.py**
- **fast**（`-m "not slow" -n auto`）：≈85-90s，464 passed / 2 skipped / 5 snapshots
- **slow**（`-m slow -n0`）：≈645s，334 passed / 1 failed
- **唯一预期红**：`test_golden_lock_json`（golden warn 136→186 待人审 accept，长期预期红）
- **conftest.py**：140 行，SLOW_MODULES + SERIAL_EXTRA + replay_serial 机制

### 7.2 毒样例覆盖（589 收工，本轮未跑）
- poison 124/124
- RULE-COVERAGE 39/63（行为级 covered，581 修复后）
- 表观覆盖率 100% / 诚实 95.2%（60/63，3 条 machine-untriggerable 单列）
- legacy 豁免 27 条（581 标红）
- 双指标（V-iso）100%/100%

### 7.3 问题
- **P14（中）**：fast 85-90s / slow 645s——560 调研指出 fast 有压缩空间（B3 受影响测试选择），但归 W 档（跳过判决测试=削弱可信度）。当前最大单测 `test_task_queue_stateful` 79.4s 占 slow 39%，可剖析最小样例预算（560 D2）
- **P15（中）**：RULE-COVERAGE 39/63——24 条规则无行为级毒样例覆盖。其中 3 条 machine-untriggerable（人审象限），剩余 21 条是真覆盖债。581 修复了"文本 grep 伪造覆盖"后，真实覆盖率从 37→39，但仍有 21 条裸奔
- **P16（低）**：test_golden_lock_json 持续预期红——从 575 到现在已 15+ 批，golden accept 人审旧账未清。建议在 CI 里显式标记"预期红"并附 issue 链接，避免新人误以为是 bug

---

## 八、信任根与攻击面

### 8.1 已实锤的攻击（564 PoC + 590 调研）
| 攻击 | 实锤状态 | 防御状态 |
|---|---|---|
| 规则供给链（进程内置空 check，77 finding 静默消失） | 564 PoC 实锤 | tool_integrity 可检出，但 gate 不自我调用、`--update` 无认证 |
| 毒样例自证（注释谎报覆盖+1） | 564 PoC 实锤 | 581 修复（行为级 covered） |
| 人签文本自证（`human:liaoranran` 即 principal_ok） | 564 PoC 实锤 | 名册挡拼写不挡身份，单用户阶段无解 |
| **A1 治理文档零防护** | 590 新发现 | 591 任务2 防御中（governance_doc_guard） |
| **A2 conftest 自篡改** | 590 新发现 | 591 任务3 防御中（TEST_CONFIG_TOOLS） |
| A3 legacy 回溯口子 | 590 新发现 | 未防御（27 条 legacy 豁免需人复核） |
| A4 stale 不回传 | 590 新发现 | 未防御（oracle 换代后 stale 标黄但不回传判决） |
| A5 采集文件写假数 | 590 新发现 | 未防御（metrics.jsonl 可直改） |

### 8.2 信任根现状
- **CORE_TOOLS**：5 个（gate_engine / atom_evidence_replay / poison_drill / toolchain / cppbible），sha256 基准在 `tools/.tool_checksums`
- **三判决入口 enforce()**：fail-closed，缺基准/文件缺失/校验异常一律拒绝运行
- **`.tool_checksums` 自身不纳入校验**（567 已声明：谁改基准仍可自签 = PoC#3，本批不碰）
- **oracle_registry**：只写不读，判决层一律不读 verified_by_oracle
- **放权开关**：G-iso / oracle_auto_accept / llm_as_judge 全 OFF

### 8.3 问题
- **P17（高）**：A1 治理文档零防护是 590 最意外发现——代码有五层防护（tool_integrity / enforce / poison / replay / golden），但投喂词（治理文档）零防护。权力与防护倒挂：改投喂词比改代码更容易影响系统行为。591 正在防御，但防御范围（哪些 .md 算"治理文档"）需要明确
- **P18（中）**：`.tool_checksums` 自签问题——`--update` 无需身份，任何人都可以改代码后重签。单用户阶段这是"git 留痕+人眼"兜底，但 590 指出最小伪造链（置空规则+改证据+--update+rebase 抹痕）零留痕。建议加"人审带外清单"或 OpenTimestamps 链锚（590 R5）
- **P19（中）**：A3-A5 未防御——legacy 回溯口子、stale 不回传、采集文件写假数，这三个攻击面 590 已指出但 591 只防了 A1/A2。建议下一批逐个评估防御成本

---

## 九、文档与卫生

### 9.1 投喂词/调研归档
- `References/architecture_架构演进/`：530-591 共 60+ 文件（投喂词 + 调研归档）
- 历轮只读调研原始产出：`_arch_v2/` 到 `_arch_v11/`（10 个目录，untracked）
- 历轮工作日志：`_worklog_403.md` 到 `_worklog_589.md`（40+ 文件，untracked）

### 9.2 卫生债（需非 agent 终端/用户确认）
| 类别 | 规模 | 说明 |
|---|---|---|
| `data/logs/` | ~596MB | 观测日志，可归档/压缩 |
| `tools_old558/` | 1 目录 | 558 旧版备份，可删 |
| `.pytest_tmp/run-*` | 44 目录，单目录可超 800 文件 | pytest 临时目录，safe-delete 批量阈值 500 拦截，建议写进 CI 收尾 |
| 根目录 `_*.py/_*.txt/_*.ps1/_t*/` | 100+ 文件 | 历轮探针/临时件，可批量删 |
| `data/mutation/_*.json` | 30+ 文件 | 历轮变异临时基线，可删 |
| `_probe_ch132_blk*.exe` | 若干 | 编译产物，可删 |
| `eval_pack*/` | 1 目录+zip | 评估包，可归档 |
| `_adv_*/` | 6 目录 | 红队批判临时件，可归档 |

### 9.3 问题
- **P20（中）**：卫生债累积严重——根目录 100+ 临时件、data/mutation 30+ 临时 JSON、.pytest_tmp 44 目录。这些不影响系统功能，但影响 `git status` 可读性和新 agent 的环境判断。建议用户在非 agent 终端跑一次批量清理，或写进 CI 收尾脚本
- **P21（低）**：_worklog 从 403 到 589 共 40+ 文件，全部 untracked——这些是历轮施工记录，有历史价值但不应入正式库。建议归档到 `References/worklog_archive/` 或单独 repo
- **P22（低）**：投喂词 530-591 共 60+ 文件，命名不统一（有的带"只读"有的带"建设包"有的带"投喂词"）——建议建立索引文件 `References/architecture_架构演进/README.md` 列出所有投喂词的批次/类型/状态

---

## 十、问题清单（按严重度排序）

### 🔴 高（影响系统可信度或数据正确性）
| # | 问题 | 证据 | 建议 |
|---|---|---|---|
| P3 | v6 基线已失效（M2 27 条不匹配），v7 待冻结 | 589 D1，590 核实 | 591 任务1 正在做，完成后 v6 标"已取代" |
| P6 | metrics curves 停在 v1（0.2374），与当前差 340 倍 | metrics.jsonl 最后一条 | 下一批 metrics 采集强制更新 curves 到权威基线 |
| P10 | 论证层无全局可接受集（grounded 未落地） | 590 R4，563/564 设计 | 单独规格包：KG 真语义边→grounded 引擎→AGM 标注 |
| P17 | A1 治理文档零防护（权力与防护倒挂） | 590 新发现 | 591 任务2 防御中，需明确防御范围 |

### 🟡 中（影响可维护性或人审闭环）
| # | 问题 | 证据 | 建议 |
|---|---|---|---|
| P7 | overturned_events.jsonl 不存在（0 事件无文件） | 文件系统检查 | 创建空文件+首行注释，区分"未启用"与"0 事件" |
| P8 | metrics 采集间隔太长（最后一条 9-17，之后无采集） | metrics.jsonl | 进 CI 或每批收工自动跑 |
| P11 | 3 条 unsigned 命题持续未人签 | prop_graph stats | 人审旧账，按 566 三步流程 |
| P12 | prop_closure.py 未创建（命题闭包缺失） | 582 N2，583 未做 | 下一批 N 档，纯新增只读工具 |
| P14 | fast 85-90s / slow 645s，最大单测 79s | pyproject 注释 | 560 D2 剖析最小样例预算（不跳过测试） |
| P15 | RULE-COVERAGE 39/63，21 条真覆盖债 | 581 修复后数字 | 分批补毒样例，每批 3-5 条 |
| P18 | .tool_checksums 自签问题（--update 无认证） | 567 声明，590 伪造链 | OpenTimestamps 链锚（590 R5）或人审带外清单 |
| P19 | A3-A5 未防御（legacy/stale/采集文件） | 590 新发现 | 下一批逐个评估防御成本 |
| P20 | 卫生债累积严重（100+ 临时件） | git status | 用户非 agent 终端批量清理或 CI 收尾 |

### 🟢 低（优化/整洁）
| # | 问题 | 建议 |
|---|---|---|
| P1 | gate 末尾无固定 SUMMARY 行 | 加一行汇总便于自动化采集 |
| P2 | warn 186 不区分"观察期新增"与"存量" | 报告层分类 |
| P4 | 基线无"当前权威"显式标记 | CURRENT_BASELINE 指针或 authority 字段 |
| P5 | equivalent 命名不一致（顶层 equivalent / by_operator equivalent_invalid） | 统一 |
| P9 | golden_state.json 在 tools/ 非 data/ | 位置一致化 |
| P13 | 知识图谱 7 类旧边集，E1/E2/E3 未落地 | 下一批 N 档 |
| P16 | test_golden_lock_json 持续预期红无显式标记 | CI 标记预期红+issue 链接 |
| P21 | _worklog 40+ 文件 untracked | 归档到单独目录或 repo |
| P22 | 投喂词 60+ 文件无索引 | 建 README 索引 |

---

## 十一、优先级建议

### 立即（591 完成后，下一批 592）
1. **v7 基线验收**：591 任务1 完成后，独立复跑 v7 全量对账（1593/1405/1/179/8/1406，M2=168），确认后 v6 标"已取代"
2. **metrics curves 更新**：跑一次完整 metrics 采集（含 poison/replay，不用 --no-heavy），把 curves 更新到 v7 数字
3. **overturned_events.jsonl 创建**：空文件+首行注释，573 的 `--log-overturned` CLI 验证可用
4. **591 验收**：治理文档防护（A1）+ conftest 入哈希（A2）独立复跑验收

### 短期（593-595，2-3 批）
5. **prop_closure.py**（N2）：命题级闭包双实现，纯新增只读工具
6. **RULE-COVERAGE 补债**：分批补 21 条裸奔规则的毒样例，每批 3-5 条
7. **A3-A5 防御评估**：legacy 回溯口子、stale 不回传、采集文件写假数，逐个评估防御成本
8. **人审旧账清理**：3 条 unsigned 命题 + golden accept（warn 136→186）

### 中期（596+，单独规格包）
9. **R4 grounded 论证层**：KG 真语义边→grounded 引擎→AGM 标注，从"可用"到"令人信服"的关键一步
10. **R5 OpenTimestamps 链锚**：590 修正 585 Q6 后可落地，解决 .tool_checksums 自签问题
11. **M1 TCE 攻坚**：唯一逃逸，需工件/TCE 比对，最难
12. **push 332 commit**：用户切热点手推

### 卫生（用户非 agent 终端）
13. 批量清理根目录临时件 + data/mutation 临时 JSON + .pytest_tmp
14. 归档 _worklog 到单独目录
15. 建投喂词索引 README

---

## 十二、人审交人项（agent/苦力不得代签）

| 项 | 状态 | 操作 |
|---|---|---|
| golden warn 136→186 accept | 待办（test_golden_lock_json 持续预期红） | `golden_lock check --accept --classify` |
| 3 条 unsigned 命题（ATOM-LANG-INLINE-001 prop-1/2/3） | 待办 | 改卡面 verified_by → build → 复核 |
| 50 条 observation 命题补 liveness 锚 | 待办（清单 data/prop_liveness_todo.md） | 人审内容后填卡面 |
| 83 卡填 verified_by_oracle | 待办（0 张填） | 人审后逐卡填 |
| push 332 commit | 待办（ssh exit141） | 用户切热点手推 |
| compiler 是否强制版本数字（EV-UB-001 唯一反例） | 待裁决 | 强制则 +1 warn 破坏 191 |
| machine-untriggerable 是否计入诚实分子 | 待裁决（当前按严口径不计入，95.2%） | 改 coverage_report 一行可翻回 100% |
| M1 TCE 逃逸是否继续冻结 | 待裁决 | 单独攻坚包或接受 1/1406 |

---

## 附录：审视方法与覆盖范围

- **门禁实跑**：tool_integrity --check / gate_engine --check / atom_evidence_replay --check（fresh，exit 0）
- **数据回读**：v6 基线顶层+by_operator / metrics.jsonl 全 3 条 / oracle_registry / prop_graph stats
- **文件枚举**：tools/*.py（130+）/ tests/test_*.py（72）/ data/mutation/full_baseline_v*.json（v1-v6）
- **Git 状态**：log/status/rev-list/diff（两条 M 假脏内容 diff 为空）
- **未跑**：poison_drill（避 591 冲突，用 589 收工数字）/ pytest fast/slow（用 pyproject 注释的 589 实测数字）/ 全量 mutation（v6 已落盘，回读即可）
- **未读**：历轮 _worklog 全文（用 commit message + 投喂词 + 历史对话摘要交叉验证）/ _arch_v* 调研原文（用 590 整理归档 + 历史对话摘要）
