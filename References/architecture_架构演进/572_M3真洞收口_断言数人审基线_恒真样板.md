# 572 · 收 M3 的 52 条真洞：断言数人审基线 + 恒真样板收口

承接 `403bb16`（571，监工已亲验 GATE_READ_KEYS 漏 actual 是真 bug、M2 真 0 逃逸、M3 浮出 52 条真洞、严格拦截率降到 7.7%）。解释器只用 `.venv\Scripts\python.exe`；一任务一 commit；不 push、不 golden accept。**核心纪律：这些洞的本质是"gate 不知道这张卡本该有几条断言"，所以正解是人审基线对比，不是再补第 N 个黑名单符号。** warn 起步、存量零误伤（541 撞过存量，先量后动）。

## 背景：52 条真洞的三类形状（571 v2 基线定性）
- 35 条 **样板候选**：`contains_any`/`contains` 里被追加一个恒真符号（`.file`/`.text` 之类），断言变平凡；
- 11 条 **删条目**：flow 式 artifact_assert 列表少一条，剩下的照样成立；
- 6 条 **删 run_match_keys**：少声明一个键，复算少查一项。
共同点：现存断言仍满足 ⇒ gate 看不见"断言被偷偷减了"。

## 任务 0（先逐一定性，别急着写规则）
把 52 条逐条打印：卡 id / 哪个算子子情形 / 变异前断言数 vs 变异后断言数。分三类：
- **(a) 条目变少可对基线**：删条目/删 run_match_keys——变异后条目数 < 人审基线；
- **(b) 恒真样板可识别**：追加的符号是通用符号（复用 553 的 `_is_universal_symbol`）；
- **(c) 结构性堵不住**：现存断言本身就是平凡的（不是变异造成的），这类登记不硬堵。
先量：(a)(b)(c) 各多少条。

## 任务 1（堵 (a)）：人审基线对比
1. 从 golden_state / 人审过的 56 卡，记录每张卡的**断言条目数基线**（artifact_assert 条数 + run_match_keys 条数）——这是人审时认定"这张卡就该有这些断言"；
2. gate 每次 check：当前卡的这些计数 **< 人审基线** ⇒ 出 **warn**（文案："断言/键数少于人审基线，可能被悄悄删项"）；
3. **基线只增不减、warn 起步**：基线就从当前人审卡取，所以存量当前不 < 基线 ⇒ 存量零误伤（先量确认）；
4. 新增断言（>基线）不算违例（卡可以补强）。

## 任务 2（堵 (b)）：恒真样板收口
1. 复跑 553 的 `_is_universal_symbol`：35 条样板候选里，追加的符号是否都被判为通用符号？
2. 若已判 ⇒ 说明这条硬化路径没覆盖到"**追加**到 contains（不是 contains_any 注入）"这个新形状，把它接进同一路径（单点、不新造规则）；
3. 若有追加的是**非通用但仍恒真**的符号（如某卡工件里必有的固定串），这类登记为 (c)——不硬凑黑名单。

## 任务 3（(c) 登记 + 跑 mutation 验证）
1. (c) 结构性堵不住的，明确写："这类洞只有 V-iso 真阴面能堵，等阴面覆盖扩到更多卡"；
2. 复跑全量 M3：52 条里被 (a)(b) 收了多少条、剩多少 (c)；逐数贴出；
3. 若新 warn 命中存量卡 ⇒ 立即回退、按 541 教训只登记不上。

---
## 收工验收（fresh）
- 任务 0 的三分类计数表（a/b/c 各多少）；
- gate 仍 **block=0**、warn 增量逐条列卡 id、存量零误伤（当前计数基线对比不命中存量）；
- M3 复跑：52 条真洞收掉多少、剩多少 (c)，逐数；
- poison 110/110（加毒样例则递增）· replay confirm=56 · pytest fast/slow 全绿 · `--check` exit 0 · 受控目录零残留；
- 改了 CORE_TOOLS 就同 commit `--update` 重钉；
- 写 `_worklog_572.md`：三分类表、基线怎么取、哪些 (c) 结构性堵不住、偏差表。

## 硬纪律
- 不补符号黑名单（恒真符号用 553 既有 `_is_universal_symbol`，不新列）；
- 不扩成 block、不新造规则 id 覆盖债；
- 护栏不许裸 except；不编数字；做不完停任务边界。
## 本批不做
任务 2 overturned / 任务 3 ruff 914（等下批）、V-iso 阴面扩覆盖、自动 KG、PoC#3/#4/#5。
