# 369_执行提示词_架构收口与放权地基接线（晚上 CodeBuddy ds v4.1 flash）

> 投喂对象：CodeBuddy Agent（ds v4.1 flash）。自包含任务书，不依赖对话上下文。
> 前置：独立红队报告 368 已经监工逐条实跑核验，本任务书第零节是核验后的裁决口径，**以本节为准，不要照抄 368 的原始数字**（368 跑测之后工程线又推进了）。
> 总原则：架构已冻结（v3.0 五原理轻内核成立，v2.0 九层不建）。本批不生产新知识原子，只做"放权地基接线"——全部是被真实失败/真实缺口买单的小修，不允许借机新建任何 v2.0 机构。

---

## 第零节　红队 368 收口裁决（监工已实跑核验，作为事实基线）

**已独立核验成立、本批要修的：**

| 编号 | 问题（核验后的准确表述） | 核验证据 | 本批动作 |
|---|---|---|---|
| P0-1 | 353/357/364 声称的一批强制机制在代码中不存在：SCHEMA-VALIDATION / CLAIM-STRENGTH / CONTENT-SAFETY / dal_proposed 零命中、无 schemas/ 目录；353 §7.3 称 replay 有"命令白名单+沙箱"，实际 `subprocess.run(argv)` 无沙箱、`_COMPILER_BASENAMES` 仅用于失败分类、可执行卡内任意 argv | `gate_engine.py --list` 36 条无上述 ID；tools/ grep 0；schemas/ glob 0；atom_evidence_replay.py:336-376 | 任务1（最高，S） |
| P1-2 | poison 覆盖率只打印 "must register exemption" 但豁免台账不存在；覆盖率分母（Finding 正则）与注册规则数（36）互不认账；S2/S3 两条 block 规则无毒样例 | poison_drill.py:313-328，exempt 仅命中打印行 | 任务4（S） |
| P1-3 | 全 CI/本地预检无工作树清洁性校验（replay 重编译可能改写 Examples/*.asm 而无兜底） | 先自行 `grep -n "status --porcelain" .github/workflows/*.yml tools/ci_local_precheck.py tools/prepush_check.py` 确认，勿凭报告 | 任务5（S） |
| P1-4 | PED-MISCONCEPTION 只查 `pedagogy.misconception`（单数），G5 卡用 `pedagogy.misconceptions:[ID]`（复数）；`run(include_advice=False)` 默认不跑所以表面 advice=0，开 advice 即批量误报 | gate_engine.py:1003-1007 注册单数；RAII-001:51 用复数；run():1032 默认关 advice | 任务2（S） |
| P1-5 | check_atom_id_format 不校验 文件stem==frontmatter.id，也无跨文件 id 唯一性；复制卡不改 id 可产生双份 verified（dict by id 静默覆盖） | gate_engine.py:193-217、800-801 | 任务3（S） |
| P1-6 | 红队 KPI（命中率/强证伪率）无采集、靠散文自报，367 对立 KPI 落地会被博弈 | docs/redteam_metrics.md 不存在 | 任务6（S，模板） |
| P1-8 | 架构文档存量数字漂移；golden accepted 台账 worse 勾稽不上；**且最新 commit 054ee95 后 golden_state.json 未 sync（停在 25/52，盘上 26/54）** | golden_state.json metrics vs 盘上文件计数 | 任务7+第〇步 |
| P1-12 | S3 只扫 actual dict 字面量，run_match_file 形态（文件引用+keys）不查 .out 内字面量 | gate_engine.py:820-844 | 任务8（S） |

**核验后降级 / 改口径的（不要当致命问题修）：**

- **P0-2（签字仅 `human:` 前缀）机制成立**（gate_engine.py:790 确无 git 身份校验），但红队的例证事实有误：LOCK-001 并非"未 git add 已人签"，它已 tracked、工作树干净、在 commit 01e3873 入库。当前所有 commit author 均为维护者本人、且 Agent 不持有 push 权——**此威胁模型下"CI 作者白名单"不增加实际防护**（白名单挡不住本人账号本地提交），真正现行防线是"人持 push 权、Agent 不 push"。故：文档口径必须改（任务1，把"机器已强制"改为"流程约定"）；CI 身份白名单 + fixture-diff 联动（P1-1）列为**第二批 M 项（放权前置）**，不假装它现在能堵住自签。
- **P1-7（预言冻结可被 amend 破）**：MCC 试点时冻结一律降级为"红队 diff 实验前后矩阵文本"的 warn 纪律，不建 block 门。
- **P1-9（87 脚本/35 CI 步维护税、拆 kernel/book、tools 退役）**：成立但属 M，列第二批。

**红队裁决与监工一致：架构冻结。** 三轮自我批判 + 异质独立红队后，新发现全部是接线级，无新范式问题；v3.0 五原理轻内核成立，v2.0 九层整体不建。下一次架构复盘的触发权交给一个真实失败，不接受新的架构文档提案。

**工程线现状（已超前，先对齐再动手）：** CONC-002(LOCK-001, 01e3873)、CONC-003(RACE-001, 054ee95)、第五批 MEM 三颗(ALLOC-002/LEAK-002/PERF-004, c947128) 均已 verified 入库；盘上 **26 原子 / 54 卡 / 78 误解**；本地 **领先 origin 若干 commit 未 push（撰写时为 12，以第〇步 `git log origin/master..master` 实跑为准）**。c947128 是工具+内容混合提交（违反铁律3，已发生，记录不追溯）。

**工作树遗留项（本批不碰，显式声明）：** `tools/book_atom_sync.py` 为 untracked 新工具，未登记 `cppbible.py` 的 cmd_check 元组（入库即触发 META-MANIFEST warn）。本批任务是放权地基接线，不使用该工具，**保持 untracked 不入库、不修改**；若后续要入库，必须先登记到 cppbible.py cmd_check 并单独提交。`tools/golden_state.json` 为已修改未提交（滞后盘上实际），由第〇步 sync 处理。

---

## 第〇步　状态对齐（只读+基线，先做，不改规则）

1. `git status -sb`、`git log --oneline -12`，记录 ahead 数与未跟踪文件。
2. 跑全量 `atom_evidence_replay`（Windows）确认 54/54 confirm、0 infra_error；再 WSL 全量复算（**两平台严禁并行**，同写 Examples/*.asm 会出假 refute）。
3. `gate_engine.py --check` 记录 block/warn（当前基线 block=0 warn=8）；再显式带 advice flag 跑一次（先 `--help` 确认 flag 名），记录 PED 误报条数作为任务2的修复前基线。
4. `golden_lock.py show` 后 `sync`，把基线从 25/52 对齐到实测（sync 在 Windows 若被 safe-delete shim 拦，改用 WSL python3；sync 会删 Examples/ 产物，跑完核 `git status Examples/`）。
5. 产出一份"状态对齐结论"：10 个未 push commit 各自归类（工具/内容/文档）、当前门禁全量结果、有无混合提交残留。**不 push**（push 权在维护者）。

## 第一批　放权地基接线（全 S，纯工具/文档，逐项独立提交）

> 纪律：每项一个 commit、工具改动单独提交绝不混原子/证据卡（铁律3，反面教材 c947128）；每条新增/修改的 block|warn 规则必须配 pytest 回归（tests/test_gate_engine.py，正反例）与阴阳毒样例；改完即跑 gate+poison+pytest 并贴结果；命令语法以 `--help`/`--list` 实跑为准，不照抄本任务书或 368 的数字。

1. **文档实现状态对账（P0-1，最高，MCC 试点前必须完成）**：在 353/357 顶部加"实现状态对账页"（存在 / 不存在-未采纳 / 已被取代 三栏，逐项以 `--list` 与代码为准）；353 §7.3 安全模型改写为"**当前无命令白名单、无沙箱；command 作者可信是当前威胁模型前提；未来引入外部卡/不可信来源前必须先建白名单**"；357 修订 1/2/3/8/10/12 标注"未实现/未采纳+理由"（CLAIM-STRENGTH 禁词明确**拒绝实现**——会误杀 LOCK-001 这类正确的不确定主张）；364 §8.3 删除/后置 schemas 承诺；353 §7.1 权限表把"❌/✅ 机器强制"改为"流程约定，未技术强制"。
2. **PED-MISCONCEPTION 兼容复数（P1-4）**：`_pedagogy_gap` 对 misconception 字段同时认可 `pedagogy.misconceptions`（非空 list 即过）；配 1 个阴性毒样例（有复数 ID list 不应报）；验收：开 advice 跑，PED-MISCONCEPTION 归零且其余 PED 正常。
3. **ID 唯一性 + stem==id（P1-5）**：新增 block 规则——文件 stem 必须等于 frontmatter.id、id 全库唯一（重复给明确报错）；配毒样例（复制 RAII-001 改名为 ATOM-ZZ-TMP-001 必须 block）+ pytest。
4. **poison 豁免台账与口径统一（P1-2）**：新增 tools/poison_exemptions.yaml（规则 ID+原因+日期）；覆盖率分母统一取注册规则数；未覆盖且未登记豁免 → CI 红；给 S2、S3 各补 1 条毒样例；勘误 357 §16.3 的覆盖率数字。
5. **工作树清洁校验（P1-3）**：先 grep 确认现状；在 ci_local_precheck 末尾、CI replay 步后、prepush 加 `git status --porcelain -- Examples/ atoms/ evidence/ tools/golden_state.json`（白名单豁免 untracked build/），非空即红。
6. **红队报告锚字段（P1-6）**：红队报告模板（288/后续提示词模板）把"阻断条目必须引用可复现命令或工件符号/行号、且该锚当次可复核"设为必填；无锚条目只能计"建议"不计强证伪；353/357/361/367 的百分比 KPI 在采集落地前统一标注"未启用指标"。
7. **数字止漂移（P1-8）**：353/357/365 存量数字统一加"截至 2026-09-12，以 --list/实跑为准"；golden accept 的 worse 改为机器勾稽（与当期测量对账）或自动填写、不让人手输；commit 字段填实。
8. **S3 补 run_match_file 形态（P1-12）**：让硬编码字面量检测覆盖 run_match_file 引用的 .out 内容（4 张该形态卡），注意别误伤合法格式串，配阴样例。

第一批收尾：全量门禁（replay 双平台 / gate 含 advice / poison / pytest / golden sync / WSL ci_local_precheck / consistency / metrics / whitespace）+ 一份"地基接线验收报告"（每项：改了什么文件、规则数前后、毒样例阴阳结果、pytest 数）。

## 第二批（M，需判断，第一批全绿且维护者另开窗口授权后再做，本批不擅自启动）

- CI git 作者白名单 + CODEOWNERS（atoms/** 人级字段变更限维护者 author）与 **fixture-diff ⇒ redteam_review 字段联动**（同提交 Examples/atoms/*.cpp 有 diff 且原子 status 晋升，缺红队签即 block；机器只判"有没有审"，不判"审得好不好"）。诚实记录：前者在"本人账号本地提交"工作流下防护有限，是为 Agent 独立凭证/自动提交的放权日做准备。
- tools/ quality 拆 atoms-kernel / book-legacy 两组，本地预检默认只跑 kernel 组（replay/gate/golden/poison/manifest），CI 全量；tools/ 首次退役审计（90 天零引用且非 CI 依赖 → legacy/）。

## MCC 首次真实落地（第一批完成后，在"下一颗新原子"上跑一次）

- CONC 三颗已收口，故 MCC 试点选**下一颗尚未生产的新原子**（域内选题，先 rglob 确认编号/目录/误解 ID 不冲突）。
- 完整留痕 MCC 六步：①异常附证据排工具假象 ②≥2 假设含≥1 反直觉 ③预言表实验前写死（**冻结=红队 diff 实验前后文本，warn 纪律，不建 block**）④判别实验唯一变量+活性对照+有界+断言候选已在 Linux 工件 grep 实测 ⑤盲裁决锚读数+≥3 替代解释 ⑥存活走质量门/被否入误解库。
- 认知预算（P4）：按 DAL 购买验证强度，低 DAL 单平台单档，高 DAL（错用即 UB/难复现）才全套；红队只对会改变 claim 方向的发现判阻断；每颗成型即报，不留半成品批次。
- CONC 已满 3 颗，**PAT 跨域 checklist 触发条件满足**：顺带产出一个 docs/kernel 单 md（把 MEM 域 P3 自证断言等模式做成跨域核对清单），不建目录/schema/规则。

## 明令禁止（不得把 v2.0 九层建回来，YAGNI）

schemas/ 全套 JSON-Schema 校验；CLAIM-STRENGTH 禁词 block；CONTENT-SAFETY 关键词升 DAL；chaos_engine；范式健康度/crisis 阈值/研究纲领状态机；8 常驻认知角色；自动 DAL 定级/dal_proposed；schema 迁移框架；L3/L4 理解度仪表盘；DAL D/E 两级（25+ 颗零使用）；多 Writer 并行流水线；异质多模型常驻配置。
规模触发、不到不做：L2 预测题 3–5 颗试点、MEM"局部模型"实验（E2，预言须 push 冻结）、增量 replay/ccache（卡量 80–100，现 54）。

## 提交与交付

- 中文提交 `git commit -F`（UTF-8）；只挑本批文件，不混 References/ 与其他 untracked；**不 push**（由维护者执行/授权）。
- 每批结束报：提交哈希与文件清单、全门禁结果（规则数/block/warn/advice、replay confirm/infra、poison 覆盖、pytest 数）、未完成项与悬而未决（需人裁的单列，不自行拍板）。
