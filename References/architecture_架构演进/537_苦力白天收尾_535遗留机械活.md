# 537 · 苦力白天收尾：535 遗留机械活（每条独立 commit，零误伤，不 push）

> 你是建设苦力。好模型已把 L2 调度（C1–C7）和 V-iso（V1–V3）落地，本批做它**明确留下的机械收尾**。不需要你做架构设计，照本规格逐条做、每条独立 commit + 正反例 pytest、存量零误伤。
>
> **仓库**：`C:\CodeLearnling\note\note\C++\CPP-Bible`，HEAD≈ee1d7c8。**只用 `.venv\Scripts\python.exe` 跑门禁/测试**；编译器 `C:\Qt\Tools\mingw1530_64\bin\g++.exe`。**动手前先 Read 目标源码确认现状（行号会漂移），规格与磁盘冲突以磁盘为准并在 worklog 记录。**

## 开工基线（亲手量，不许照抄）
gate（约 60 规则 / block=0 / warn≈136）、poison（约 90/90）、replay（confirm=56）、`pytest -m fast -q`（约 274 passed）。记下来，收工对比。

## T1 · V-iso 毒样例 N1–N7 进 poison（V4）
- 参考 `_arch_v2_round2/probe_fence_iso/` 的 6 个攻击阴面与 `tools/viso_diff.py` 的判据（delete-only / 单 hunk / 删 1–3 行 / ≤40 token / ≤2% / anchor 覆盖 / remove 命中 / retain 逐字 / Itanium 同源 / 实测翻转，全 AND）。
- 在 tests/poison 下为每条判据被违反造一个**应被 block/refute 的毒样例**：冒名程序（删 95 行）、42→43 替换、删错位置、注释走私、连循环带 fence 一起删、retain 被注释、符号换 static/inline。注意 poison 覆盖正则只认字面量 `"RULE-ID" in who`，参数化比较会"样例过但规则算未覆盖"——照既有写法补 RULE-COVERAGE。
- 验收：poison 全过、RULE-COVERAGE 无新增 uncovered、`pytest -m fast` 全绿、存量 0 新 block。独立 commit。

## T2 · 补增量指纹的 .out 缺口（529 B.8，已确认是真缺口）
- Read `tools/atom_evidence_replay.py` 的 `card_fingerprint`（约 1710 行）与 `select_incremental`（约 1756 行）：确认指纹输入清单是否包含该卡 `.out`（现在 .out 的 run_match_file 比对有，但**指纹可能不含 .out**，导致只改 .out 读数时 `--incremental` 误判无变化而跳过）。
- 若确实不含：把 .out 的内容/mtime 纳入指纹；造一个 pytest——改某卡 .out 一个键值后 `--incremental` 必须重跑该卡而非 skip。若已含则在 worklog 写"实测已覆盖，不做"。
- 验收：增量无改动仍 0.3s 级全命中；改 .out 必触发；全量 replay 仍 confirm=56。独立 commit。

## T3 · 核实 observation 活性判据是否拦"自标绕过"（529 B.3）
- Read `tools/gate_engine.py` 的 `check_evidence_trivial_observation`（约 940 行）。构造探针：一条本是 inference 的论断，仅靠把 `claim_type` 写成 `observation` 就走全自动机验通道——现有函数拦不拦？
- 拦得住：写 pytest 钉死、worklog 记录"已覆盖"。拦不住：加一条**先 warn 观察期**的判据（不直接 block，先量存量命中数报告），配正反例毒样例。独立 commit。

## T4 · complete touch 审计的沙箱正式豁免
- 现状：task_queue complete 的 touch 审计会把既有未跟踪沙箱文件（`_arch_*/_adv_*/_adv_v90/_worklog_*/_t*/_po*/_rp*` 等）也列出，造成告警疲劳。
- 做一份**显式沙箱忽略清单**（集中常量，如 `tools/_sandbox_ignore.py` 或 task_queue 内 SANDBOX_GLOBS），只豁免这些**前缀明确的临时/沙箱目录**；正式目录（atoms/evidence/tools/tests/Book/Examples 正式部分）**一律不豁免**。配 pytest：沙箱文件不报、正式目录未声明文件仍被抓。独立 commit。

## T5 · warn 136 条四桶分类（只记录与建议，不 accept）
- 用 golden_lock 既有四桶机制把 136 条 warn 逐条归类，产出 `docs/kernel/warn_136_分类建议.md`：每条 = 规则 / 卡 / 桶 / 建议（良性存量债 or 真问题 or 规则误报）。**你不执行 --accept**（认可权唯人），只把分类和建议理由备好，交人一键审。独立 commit（文档）。

## T6 · CI ruff 存量 15 项
- 用 CI 同口径 ruff（0.6.9）列出 15 项，逐个最小修（不改逻辑语义，只清 lint；若某项修了会动行为，跳过并在 worklog 标注交人）。验收：`ruff check tools/ tests/` 剩余项逐条有交代。独立 commit。

## T7 · V-iso 56 卡迁移 backlog（只排队，不改卡）
- 现状：57 张证据卡仅 1 张带 negative_controls（FENCE 样板）。按 533 的 B0→B1(asm 11 命题/13 卡)→B2(双路径 14)→B3(24) 分批，产出 `docs/kernel/viso_migration_backlog.md`：每张卡标注候选通道（artifact 计数 / run 键 / 做不出阴面的原因）、anchor 函数候选、优先级。**不实际改任何卡、不自动降级**。独立 commit（文档）。

## 收工
- fresh 全量门禁，报实测终值（gate block/warn、poison、replay confirm/耗时、pytest 点数、kg）。
- 写 `_worklog_537.md`：每条 T 的 commit、前后对比、"规格说 X 实测 Y"偏差表、做不了/交人项。
- **不 push、不 --no-verify、不 golden accept、不改 56 卡信任结论；新规则先 warn 后 block。做不完停在 T 边界，不留半成品在正式代码。**
