# 567 · 供给链加固第一批：完整性自检强制化（堵"规则被静默改仍全绿"）

承接 `76fc84a`。解释器只用 `.venv\Scripts\python.exe`；一任务一 commit；不 push、不 golden accept。这是**安全加固批**，改的是判定核心入口，必须配正反例 + 全量回归 + 改完重钉。

## 背景（监工侦察已实锤，别再当新发现）
- `tools/tool_integrity.py` 的 `CORE_TOOLS` = `gate_engine.py / atom_evidence_replay.py / poison_drill.py / toolchain.py / cppbible.py` 五个，有 sha256 `.tool_checksums`；
- **但**：① 它只有 `--update`、**没有 `--check`**（钉完无法独立验证）；② `gate_engine.py` 源码里 grep 不到任何 integrity 调用 ⇒ 改这五个核心文件的任何字节，门禁仍全绿。这就是 564 PoC#1/#2 的实锤根因：**改 `gate_engine.py` 的规则返回 `[]` 静默消失 77 条，因为 gate 跑时不验自己。**

## 任务 0（先确认基线绿，再动工）
用临时脚本对 `.tool_checksums` 与磁盘上五个 CORE_TOOLS 现算 sha256 对比，确认**当前基线一致（应全绿）**。若不一致，先 `tool_integrity.py --update` 重钉到一致——把这个不一致如实写进 worklog，不许假装基线干净。

## 任务 1：tool_integrity 加 `--check` 子命令（独立验证）
现在只有 `--update`。新增 `--check`：读已钉 checksum，逐文件重算 sha256，**全部匹配 ⇒ exit 0 打印 OK；任一不匹配或缺失 ⇒ exit 1，逐个列出文件名 + 期望/实际前缀**。不打印任何密钥类内容，只比对哈希。

## 任务 2：三个判定入口开头强制 integrity check（核心）
在 `gate_engine.py`、`atom_evidence_replay.py`、`poison_drill.py` **开始跑任何规则/判决之前**，调用 integrity check：
- 校验失败 ⇒ **立即 fail-loud exit 1**，打印"判定核心被改动且未重钉，拒绝运行"，绝不继续跑规则；
- 校验通过才正常往下。
- 注意顺序：integrity check 要在任何"读库/编译"之前，且**不能因 check 自身报错而把真红灯掩盖**（check 代码本身要稳）。
- **副作用纪律**：今后任何人改这五个 CORE_TOOLS 后必须 `--update` 重钉，否则下次门禁自红——这正是设计目标（改判定核心必须显式留痕）。

## 任务 3：正反例毒样例（回归锁，必做）
新增测试（或 poison 载荷）证明：
1. **篡改**：临时给一个 CORE_TOOLS（如 gate_engine.py）加一个无关字节、重算 ⇒ `--check` 必红、列出该文件；
2. **正常**：未改动 ⇒ `--check` exit 0；
3. **入口拦截**：在篡改态下跑 `gate_engine.py`（任意子命令）必须 exit 1 拒绝运行，**不许静默放行**；
4. 还原后全绿。
（测试里篡改要在临时副本/monkeypatch 上做，**绝不真改正式 gate_engine.py 后忘记还原**；还原后跑一次 --check 确认绿。）

---
## 收工验收（fresh，且本批碰判定核心，必须全量）
- `tool_integrity.py --check` 基线 exit 0；
- 篡改一例（临时）→ 三入口之一确实拒绝运行；还原后复跑恢复绿；
- gate 61/141（block=0 warn=136）· poison 107/107（双指标 100%）· replay confirm=56/refute=0 · pytest fast -n auto 全绿 · slow -n0 全绿；
- 改了 CORE_TOOLS 就 `tool_integrity.py --update` 重钉，并确认重钉后 --check 仍 exit 0；
- 受控目录 evidence/atoms 零改动；写 `_worklog_567.md`（含基线核对、篡改/还原证据、偏差表）。

## 本批只做 #1/#2，不碰
PoC#3（`--update` 无认证自签）、#4（RULE-COVERAGE 靠 grep 字符串）、#5（poison_exemptions 无签名自批）——这三个要加认证/签名机制，等 trae 564 完整规格；本批只把"静默篡改不被发现"这个根堵上。也不碰 B2 性能 / M3 / ruff。
