# 518 终极收债批 · P0+P1 一次性投喂提示词

> 日期：2026-09-14
> 本文件不是新调研。是501-517全部内化后，把P0+P1共8个任务打包成一个可直接投喂的提示词。
> 读完516（总验收）和517（五层完成度）后得出。

---

## 投喂提示词（直接复制以下内容给苦力Agent）

```
你是CPP-Bible仓库的执行Agent。仓库在 C:\CodeLearnling\note\note\C++\CPP-Bible。
先 Read tools/cppbible.py 和 tools/gate_engine.py 的前100行，理解入口和规则注册方式。
严格遵守铁律：不push、不golden sync、存量零误伤、一任务一commit+pytest、worklog记录实测数字。

=== 任务1：并行replay（最高ROI） ===
现状：atom_evidence_replay.py串行跑56张卡，298s。32核机器。
要求：
1. 加 ProcessPoolExecutor(max_workers=8) 并行跑多张卡
2. 每进程独立临时目录（避免replay_lock冲突）
3. 保留 --incremental 和 --full 模式
4. 预期：298s→~50s
5. 跑全量replay确认 confirm=56/refute=0/infra_error=0
6. 一个commit

=== 任务2：重试退避 ===
现状：编译器失败直接infra_error，不重试。
要求：
1. 新建 tools/retry.py，提供 retry_with_backoff(func, max_retries=3, base=0.5, cap=5.0)
2. 指数退避+jitter：delay = min(base * 2^attempt, cap) * (0.5 + random())
3. 只重试 TimeoutError/ConnectionError/SubprocessError，不重试 AssertionError
4. 在 atom_evidence_replay.py 的编译调用处包装
5. pytest 2个：成功不重试 / 超时重试3次后失败
6. 一个commit

=== 任务3：备份脚本 ===
现状：ahead 176个commit未push，无自动备份。
要求：
1. 新建 tools/backup.py
2. snapshot()：复制 golden_state.json / artifact_versions.json / data/*.db 到 data/backups/YYYYMMDD-HHMM/
3. list()：列出备份
4. restore(path)：从备份恢复
5. 保留策略：最近10份
6. pytest 1个：snapshot→restore→文件一致
7. 一个commit

=== 任务4：FTS5全文搜索 ===
现状：247份架构文档靠grep找。
要求：
1. 新建 tools/doc_search.py
2. 用 SQLite FTS5 + trigram tokenizer
3. 索引 References/architecture_架构演进/*.md
4. 增量更新（按mtime）
5. CLI: python tools/doc_search.py "混沌" --top-k 5
6. 返回标题+路径+snippet高亮
7. 一个commit

=== 任务5：集中config ===
现状：magic number散在各工具。
要求：
1. 新建 config/config.yaml
2. 收集：replay timeout(600)、parallel workers(8)、circuit breaker threshold(5)、cooldown(60)
3. 不要求所有工具都改读config——先建文件，gate_engine.py读3个关键值
4. 一个commit

=== 任务6：规则三阶段 ===
现状：新规则一上线就block，可能误伤存量。
要求：
1. 在 gate_engine.py 的 SEVERITY_MAP 旁加 STAGING_MAP
2. 新规则默认 warn，加 staging: "warning"
3. 已有规则不动
4. pytest 1个：新规则warn不block
5. 一个commit

=== 收工验证（全部任务完成后）===
跑以下命令，结果写入 worklog_518.md：
1. python tools/gate_engine.py --check （block=0 warn=32左右）
2. python tools/atom_evidence_replay.py --check （confirm=56）
3. python tools/poison_drill.py （exit 0）
4. pytest -m fast （全绿）
5. 记录：replay时间从多少降到多少
6. git log --oneline 看几个commit
不要push。不要golden accept。
```

---

## 为什么是这6个任务

| 任务 | 解决517的哪个层 | ROI |
|---|---|---|
| 并行replay | L0性能 | 298s→50s |
| 重试退避 | L0韧性 | 减少假refute |
| 备份 | L0数据安全 | P0紧急 |
| FTS5搜索 | L0检索 | 247文档毫秒级 |
| 集中config | L0可维护 | 消灭magic number |
| 规则三阶段 | L1质量 | 防误伤 |

**这6个做完，L0从71%到~95%。**
L2调度（model_router/task_queue）和L3智能（诊断/RAG）留到下一批。

---

## 执行后应该看到什么

- replay时间：298s → ~50s（6x加速）
- tools/多了 retry.py / backup.py / doc_search.py（+3个）
- config/config.yaml 存在
- gate_engine.py 有 STAGING_MAP
- worklog_518.md 记录全部实测数字

---

*文档编号 518。这是执行提示词，不是设计文档。投喂后等结果。*
