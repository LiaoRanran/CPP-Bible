# 370_差模型杂活_工作树卫生清理与只读盘点

> 投喂对象：当前可用的编码 Agent（杂活窗口）。自包含，不依赖对话。
> 一句话边界：**只做 2 个删除 + 1 份只读盘点报告**。你不做判断、不提交、不编译、不入库、不 push。所有数字必须来自当条命令的实跑输出，禁止编造。
> 环境：Windows PowerShell，仓库根 `C:\CodeLearnling\note\note\C++\CPP-Bible`。先 `cd` 到该目录。
> 总原则（fail-closed）：任何一步的实测结果与本提示词的预期不符，**立即停手**，在最终报告里写"异常：预期 X，实测 Y"，不要猜测、不要自行修复、不要换命令硬来。

---

## 步骤 0　环境确认（只读）

跑 `git status -sb | Select-Object -First 1` 与 `git --version`。
预期：分支 `master...origin/master [ahead 10]`。若 ahead 不是 10，或不在 master，停手报告。

## 步骤 1　删除两个已失效的临时提交信息文件

只允许删除这两个、且仅在仓库根目录：

- `commit_msg_lock.txt`
- `commit_msg_race.txt`

背景（已由监工核实，供你判断，不需你重新论证）：它们是早前用 `git commit -F` 提交 01e3873 / 054ee95 时写的临时信息文件，两个 commit 已完成，文件 untracked、无代码引用。

**每个文件删除前必须逐条核对**：
1. `git status --porcelain -- <file>` 必须显示 `??`（untracked）。
2. 若显示 `M` / `A` / 空（已跟踪或已暂存）等任何其他状态 → **不删**，记入报告。
3. 核对通过后用 `Remove-Item <file>` 删除。若权限/审批失败，**不要换强删命令、不要反复重试**，记"待人工删除"即可。

明确**不要删** `build\commit_msg.txt`（它在 .gitignore 内，`git check-ignore build/commit_msg.txt` 可验证）。删除范围不得扩大到任何其他文件。

## 步骤 2　只读盘点（不修改任何文件，报告直接在最终回复输出，不要落盘新文件）

### 2.1 untracked 全量分类
跑 `git status --porcelain`，把所有 `??` 项按目录归类，统计份数并列出编号范围：
- ① `References/architecture_架构演进/` 架构文档（预期 327–369 一批，含两个 369 文件）；
- ② `docs/kernel/` 文档（预期 `CI_加速落地方案.md`）；
- ③ `tools/` 脚本（预期 `book_atom_sync.py`）；
- ④ 其他（若有，逐个列出）。
每一类都标注："**待入库资产，禁止删除/移动/改名**"。你只盘点，不做入库。

### 2.2 golden_state.json 滞后核对（只看不改）
- 跑 `git diff tools/golden_state.json`，报告 **HEAD 已提交基线** 与 **工作树基线** 两组数字（atoms_total / evidence_total / replay_confirm）。
- 用文件计数得到盘上实际原子数与证据卡数：`atoms/**/*.md` 份数减 README、`evidence/**/*.md` 份数减 README（misconceptions 可不数）。
- 在报告里明确写出三层数字，形如"HEAD a/b → 工作树 c/d → 盘上实际 e/f"，并原样注明："**工作树改动不要 commit、不要 revert、不要 sync，留给后续 golden_lock sync 处理**"。你不得运行 golden_lock。

### 2.3 新工具登记核对（只读 grep）
检查 `tools/book_atom_sync.py` 是否登记在 `tools/cppbible.py` 的质量/cmd_check 元组里（grep `book_atom_sync`）。报告"已登记 / 未登记"，**不修改**任何文件。

### 2.4 只读门禁基线
跑 `python tools/gate_engine.py --check`，贴汇总行与每条命中的规则 ID（预期 block=0、warn=8）。
**禁止**运行 atom_evidence_replay、golden_lock、poison_drill、ci_local_precheck，禁止任何 g++ 编译——它们会重编译、改工件或改基线。只允许 gate 这一条只读命令。

### 2.5 未推送 commit 清单
跑 `git log --pretty=format:'%h %s' origin/master..master`，列出全部（预期 10 条）及标题。

## 步骤 3　最终交付：一份《工作树卫生报告》

在最终回复中用 Markdown 输出，包含：
1. **已删除**：两个文件名 + 各自删前 `git status --porcelain` 证据；删除失败的如实写"待人工删除"。
2. **盘点 2.1–2.5** 的全部结果（分类计数、三层基线数字、登记核对、gate 汇总、ahead 清单）。
3. **未做事项与原因**：明确写出你没有 push / add / commit / revert / sync / 编译 / 入库，以及没有处理任何 ZZ/TMP 探针文件。

---

## 硬性禁止（违反即任务作废）

- 禁止任何写 git 历史/索引的操作：`push / add / commit / revert / reset / checkout / stash / merge / rebase`。
- 禁止修改、移动、改名、删除 `atoms/` `evidence/` `misconceptions/` `Examples/` `tools/` `docs/` `References/` `.github/` 下的任何文件；删除仅限根目录那两个点名的 txt。
- 禁止运行 replay / golden_lock / poison_drill / ci_local_precheck / g++ 等会编译或改基线的命令；只读 `gate_engine.py --check` 除外。
- 禁止寻找、重建或"补"任何 `ZZ-TMP` / 探针 / 临时文件（已确认不存在；若意外发现同类命名文件，只在报告里列出，不处理）。
- 禁止把 untracked 文件自行整理、归类、入库或删除。
- 禁止安装任何软件、改环境变量、动 .gitignore 或 CI 配置。
- 数字一律来自实跑；命令报错就贴报错原文，不要凭印象填。

## 验收标准（监工事后核对）

- 根目录两个 `commit_msg_*.txt` 消失，`git status` 不再列出；其余 untracked 文件数量/分类与报告一致。
- `gate_engine.py --check` 仍为 block=0、warn=8（删除不得影响门禁）。
- `tools/golden_state.json` 仍为未提交的 M 状态、内容未被你改动（`git diff` 与你报告一致）。
- 无新增 commit、无 push、工作树除两个 txt 被删外无其他变化。
