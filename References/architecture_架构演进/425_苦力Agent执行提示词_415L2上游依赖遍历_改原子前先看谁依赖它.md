---
id: 425
title: 苦力Agent执行提示词 415L2上游依赖遍历 改原子前先看谁依赖它
status: active
type: architecture-note
created_at: 2026-09-13
---
# 425 苦力Agent执行提示词：415 L2 上游依赖遍历（改一颗原子前先看谁依赖它）

> 投喂对象：苦力Agent。前置条件：417（ATOM-REL-CONFLICT）已完成（复用 relations 解析入口）。任务：实现上游依赖遍历——给定一颗原子，列出所有直接依赖它的原子。零风险——只读 relations，不修改任何文件。

---

## 铁律

1. **不 push**：只 commit
2. **工具改动分离提交**
3. **零风险**：只读 atoms/*.md 的 relations，不修改任何文件
4. **配 pytest**
5. **先 Read 再改**：Read `tools/gate_engine.py` 的 `_relations_norm` 实现（417 可能已扩展），复用解析入口

---

## 背景

415 调研指出：改一颗原子，哪些原子受影响？当前靠人记，应该机器算。

最小可行闭环 = 上游遍历：给定原子 A，列出所有"prerequisite/specializes/realizes 指向 A"的原子。不做下游、不做加权、不做优先级——先解决 80% 的问题（改之前先看谁依赖我）。

---

## 实现步骤

### Step 1：Read 确认 relations 解析

```python
# Read tools/gate_engine.py，确认：
# 1. _relations_norm 的当前实现（417 可能已扩展 CONFLICT_REL）
# 2. DAG_REL 的定义
# 3. 如何加载所有原子的 frontmatter
```

### Step 2：创建 tools/impact_analysis.py

```python
#!/usr/bin/env python3
"""
上游依赖遍历：给定原子 id，列出所有直接依赖它的原子。
用法：
  python tools/impact_analysis.py upstream ATOM-MEM-RAII-001
  python tools/impact_analysis.py downstream ATOM-MEM-RAII-001  # 可选，P2
  python tools/impact_analysis.py graph --format dot              # 可选，P2
输出：JSON
零风险：只读 relations。
"""
```

### Step 3：核心功能——upstream（上游遍历）

```python
def upstream(atom_id, rel_types=None):
    """
    列出所有直接依赖 atom_id 的原子。
    依赖关系 = prerequisite / specializes / realizes（不包括 contrasts/see_also）。
    
    返回：
    [
      {"atom": "ATOM-MEM-MOVE-001", "rel_type": "prerequisite", "path": "atoms/mem/..."},
      {"atom": "ATOM-MEM-LEAK-001", "rel_type": "specializes", "path": "atoms/mem/..."},
    ]
    """
    # 1. 加载所有原子的 relations（用 _relations_norm 解析）
    # 2. 对每颗原子 B：
    #    for rel in B.relations:
    #        if rel.type in {prerequisite, specializes, realizes} and atom_id in rel.targets:
    #            result.append({"atom": B.id, "rel_type": rel.type, "path": B.path})
    # 3. 按 rel_type 分组排序
```

### Step 4：扩展功能（P2，可选）

#### 4.1 downstream（下游遍历）

```python
def downstream(atom_id):
    """列出 atom_id 直接依赖的原子（它的 prerequisite/specializes/realizes 指向谁）。"""
```

#### 4.2 transitive（传递闭包，P3）

```python
def transitive_upstream(atom_id, max_depth=3):
    """上游的上游，最多 max_depth 层。"""
```

#### 4.3 graph 输出（P3）

```python
def export_graph(format="dot"):
    """导出全部 relations 为 Graphviz DOT 格式，可视化依赖图。"""
```

### Step 5：注册到 cppbible.py

```bash
cppbible impact upstream ATOM-MEM-RAII-001
cppbible impact downstream ATOM-MEM-RAII-001
```

### Step 6：pytest

```python
class TestImpactAnalysis:
    def test_upstream_finds_dependents(self):
        # 构造 A.prereq=B → upstream(B) 包含 A
        ...

    def test_upstream_excludes_contrasts(self):
        # A.contrasts=B → upstream(B) 不包含 A（contrasts 不是依赖）
        ...

    def test_upstream_empty_for_isolated_atom(self):
        # 没有原子依赖的原子 → 空列表
        ...

    def test_real_atoms(self):
        # 对存量 27 颗原子跑 upstream，输出非空或空列表都合法
        ...

    def test_json_output_format(self):
        # 输出格式与四工具 --json 一致
        ...
```

### Step 7：验证闭环

不是"工具写了"，是：
```
选一颗已知被依赖的原子（如 ATOM-MEM-RAII-001）→ upstream 列出正确的依赖者
选一颗孤立原子 → upstream 返回空
```

**验收标准**：对存量 27 颗原子全部能运行，不报错；至少 5 颗有上游依赖（如果 relations 写了的话）。

### Step 8：全量门禁

```bash
python tools/impact_analysis.py upstream ATOM-MEM-RAII-001
python tools/gate_engine.py --check
python tools/atom_evidence_replay.py --check
python tools/poison_drill.py
python -m pytest tests/ -v
```

---

## 验收标准

- [ ] `tools/impact_analysis.py` 存在，upstream 功能可用
- [ ] 对存量 27 颗原子全部能运行，不报错
- [ ] 正确区分"依赖"（prerequisite/specializes/realizes）和"引用"（contrasts/see_also）
- [ ] `cppbible impact upstream <id>` 可执行
- [ ] 输出 JSON 格式与四工具一致
- [ ] pytest 含 TestImpactAnalysis（≥5 例）
- [ ] 零风险：不修改任何原子/证据卡
- [ ] gate block=0，replay confirm=56
- [ ] 独立 commit，message 含 `feat(tools): 415 L2 上游依赖遍历 改原子前先看谁依赖它`

---

## 不做的事

- 不做 γ^k 加权传播（太复杂，留待后续）
- 不做影响优先级排序（留待后续）
- 不做自动修改（只查询）
- 不修改存量原子的 relations
- 不 push
- 不混 414/417/420/421/423/424 的改动

---

## 为什么这是 P2

415 L2 的完整形态是"双向遍历+加权+优先级"，但最小可行闭环是"上游遍历"——改一颗原子前，先看谁依赖它。这已经能解决 80% 的问题（防止改了基础原子导致连锁失效而不知道）。

本工具零风险（只读），依赖 417 的 relations 解析扩展。等 417 完成后可立即投喂。
