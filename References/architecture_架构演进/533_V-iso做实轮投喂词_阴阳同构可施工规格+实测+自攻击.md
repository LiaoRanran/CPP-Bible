# 533 · V-iso 做实轮：阴阳同构从理念打到可施工规格（投喂强模型 trae · 第三轮）

> **你的身份**：V-iso 阴阳同构的**总设计师 + 第一个攻击者**。前两轮你完成了现状批判（529）和下一代蓝图（_arch_v2/03）；这一轮不要再画战略，要把你自己押上的最大赌注 **V-iso** 从"理念"打成"苦力 Agent 照做就能落地、且绕不过去"的精确规格，并用 FENCE-001 真实夹具实测。
> **仓库**：`C:\CodeLearnling\note\note\C++\CPP-Bible`，Windows；门禁用 `.venv\Scripts\python.exe`（有 PyYAML 6.0.3）。
> **纪律**：只读 + 产出，不改正式文件、不 commit/push/accept；所有产出落 `_arch_v2_round2/`。
> **已知，别重复**：V-iso 的动机（六轮逃逸都是"外延过机制空"）、9 个门控开关、synthesis 三分类、G-llm-screen 的 EIR 门——你上轮已写，本轮直接继承，不要重述。

---

## 第 0 步 · 精读 V-iso 将落到的真实代码（别凭蓝图想象改法）

读这些，把行号和真实结构记进你的设计：
1. `tools/atom_evidence_replay.py`：三分类怎么产生（confirm/refute/infra_error）、`card_fingerprint`（:1486）、run_match_file 怎么逐字比对（:1271-1310）、P0-A 独立重编译怎么做的（找到它的函数，V-iso 阴面编译要复用同一套编译器提取，不许另造）。
2. `tools/gate_engine.py`：现有 `OBSERVATION-NEEDS-ARTIFACT`（:1943）、`INFERENCE-NOT-MACHINE-VERIFIED`（:2024）、`_register_all`（:2642）——V-iso 新规则要注册在哪、复用 principal_ok 还是单独判。
3. 真实样板卡：`atoms/conc/ATOM-CONC-FENCE-001.md` + `evidence/conc/EV-CONC-001.md` + 它的 fixture（`Examples/atoms/_atom_fence_vs_atomic.cpp` 之类，先 ls 找真名）+ `.out` + `.asm`。**这条卡是 V-iso 的天然样板**：它本就有"删 fence 后循环被消除"的对照。

---

## 任务 1 · V-iso 可施工精确规格（苦力照做级，不许停在"要做对照"）

把 frontmatter schema、replay 分支、diff 测量、迁移策略、毒样例全部写到苦力能照做：

1. **`negative_controls:` 精确 schema**：每字段名/类型/必填/默认值写死；v1 支持哪些、v2 留哪些（你上轮说 v1 只读数翻转、v2 符号翻转——说清 v1 边界为什么这么划）。
2. **阴面最小 diff 的机器判据（这是建设者特别要求钉死的护栏，你上轮自承是"最大赌注"）**：
   - 阴夹具必须是阳夹具的**最小变量改动**。"最小 diff"怎么机器测？给可执行判据：是文本 diff 行数？AST 节点改动数？还是"改动点必须命中 claim 指向的那一处机制"？
   - Writer 造一个**完全不同的程序**冒充阴面（diff 巨大）必须被机器识破。给出具体算法和阈值，及误报风险。
   - 明确："破坏充分性"如何机器可判（不能只靠 Writer 自说"我破坏了机制"）。
3. **replay 新分支**：`refute:negative_control_passed` 的触发条件、与现有三分类的关系（它算 refute 还是新类？为什么）、阴面失败算不算 infra（编译失败怎么区分"真破坏"vs"夹具写坏"）。
4. **存量 56 卡迁移策略**：逐类说清哪些 observation 能做阴面（机械对照型）、哪些**原则上做不出阴面**并自动归 inference（trae 上轮说这是"分类信号"——列出判据，别含糊）；先 warn 观察期，给出 56 卡的分批迁移顺序和每批数量估计。
5. **毒样例设计思路**：阳夹具通过+阴夹具必须失败的正反对各举一例；"阴面 diff 过大"怎么反例；"阴面编译失败"怎么区分真假破坏。
6. **与 530 T4（活性判据）合并实施的接口**：活性三条件 + 阴面翻转，三者齐备才 observation 走机验——写出合并后的完整判定伪代码。

## 任务 2 · FENCE-001 阴面实测（拿真实数据，别估）

在 `_arch_v2_round2/probe_fence_iso/` 里（沙箱，不动正式夹具）：
1. 基于 FENCE-001 现有阳夹具，**实际构造一个阴面**（删掉/绕过那道 fence，看循环是否被编译器消除、run_match 读数是否变化）。
2. 实测并报出：
   - 阳面、阴面各自的编译命令、运行时间、退出码；
   - 阴面断言是否真翻转（run_match 哪个 key 变了/变没了）；
   - 阳→阴的真实 diff（用你任务1定的方法量出来）；
   - V-iso 给这一颗原子增加的**真实**耗时（对比 replay 现状）。
3. 诚实报告：FENCE-001 的阴面是否"破坏充分"？还是存在"删了 fence 但输出没变"的尴尬情形（若有，说明这个 claim 的边界）。

## 任务 3 · 你自己攻击 V-iso 设计（上轮蓝图里你没打自己的部分）

切换攻击者，回答：
1. **最小 diff 可绕吗**：Writer 改 1 行但语义天翻地覆（如把变量改别名+删掉关键锁同时只动一行），机器 diff 判据会不会被骗？
2. **阴面疲劳/形式化**：Writer 批量造"走形式的阴面"（比如把常量 42 改成 43，但 claim 根本不依赖 42）——如何在机器层识别"阴面与 claim 无关"？
3. **成本曲线**：56 卡全做阴面，nightly replay 真实加时多少（用任务2的单点成本外推，并给外推的不确定性）？哪类 observation 不该做阴面以免成本爆炸？
4. **活雷腐化**：V-honeypot 的"活雷"会随规则修复而过时（雷被修了还当雷），怎么保证雷集是活的、不变成永远绿的摆设？
5. **边界漏网**：是否存在一种 observation——它真有判别力，但**结构上构造不出单变量阴面**？若有，归 inference 会不会把强 claim 错降级？给这种边界的判据。

---

## 任务 4 · 产出 533 施工规格（苦力可照做，编号续 _arch_v2 与 530）

写 `_arch_v2_round2/533_V-iso施工规格.md`，结构：
- FENCE-001 实测证据（任务2真实数字）；
- V-iso 可施工规格（任务1全部，schema/伪代码/迁移表/diff判据）；
- 你对自己设计的攻击与回应（任务3）；
- 苦力执行批次（一任务一 commit、阴阳毒样例、pytest、存量 0 block、与 530 T4 合并），每条挂"为什么现在做"+"机器验收判据"；
- 明确"现在做 vs 攒数据 vs 等模型"三档。

## 验收（建设者逐条核验）
- 任务1每个 schema/判据必须可执行，不许"建议加字段"这种废话；任务2每个数字必须来自你实跑的命令输出；任务3的每个攻击必须给出它为什么真能绕过，不是假设。
- 区分【实测/推断/待实验】；找不到的就说没测到，不编。
- 保留六轮验证资产（fail-closed/P0-A/人签/毒样例），为推翻任何一个给更强理由。
- `git status` 证明正式目录零改动。

## 开头一句话
你读完 FENCE-001 真实夹具并动手做了阴面后，V-iso 这个最大赌注**最可能在哪一步塌**（不是理论风险，是你实做时绊到的那个）？
