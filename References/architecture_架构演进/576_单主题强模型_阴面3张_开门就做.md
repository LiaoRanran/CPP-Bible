# 576 · 单主题强模型包：V-iso 阴面扩 3 张（开门第一件就做）

> 这包**只有一件事**：给 V-iso 阴阳同构扩 3 张真阴面。前两批（573 C、574 C）阴面都因排在大包末尾、预算被全量验证吃掉而连续轮空——本包把它单独拎出来交给最强模型，**不掺任何其他任务、不跑全量 mutation（16 分钟那种一律不跑）**。时间预算建议：20% 侦察选卡 → 70% 造阴面 + 真编译翻转迭代 → 10% 留证收工。解释器 `.venv\Scripts\python.exe`，g++ = `C:\Qt\Tools\mingw1530_64\bin\g++.exe`，CCACHE_DISABLE=1，cl 永久 unavailable。**做几张算几张、判据一条不降；不 push、不 golden accept、不 block 化既有结论。**

## 任务 0（先复跑基线，别省）
- EV-CONC-001 现有阴面（`Examples/atoms/_atom_fence_vs_atomic.nc1.cpp` + 卡面 negative_controls）replay 仍 flip verified（阳=2 阴=0）；gate（63 条）、poison（114/114）、replay（confirm=56）、`tool_integrity --check` exit0 记下来。
- 读 `tools/viso_diff.py` 全部判据与 schema、`atom_evidence_replay.py` 的 `check_negative_controls` 阴性判决分支（④c / negative_control_passed / negative_control_broken）、533/535 阴面规格、553 已落地的 B3（阴面 fixture 身份锚：stem 必须 == 阳夹具主干名 + nc 后缀）与 B5（非块式 negative_controls 硬化）。**阴面文件名、frontmatter 形态必须过这两道已落地的闸。**

## 任务 1：选 3 张卡（先侦察 19 张 run_match 候选池）
- 从带 run_match / artifact_assert 的候选卡中，**只挑机制最清晰、可单变量干预、能真编译**的 3 张；worklog 逐张写选卡理由（机制是什么、anchor 在哪、删什么翻转什么）。
- 优先选「删除某机制依赖行 → 某工件计数/键/区间断言确定翻转」的卡；**避开**纯存在性、纯计时、机制需要双改动点才能在运行级显现的卡（533 已证明这类单变量运行级阴面结构性不可构造，登记即可，不要硬造）。
- 每张先把自然语言 claim 拆成 **anchor（机制载体函数/区间）/ remove（机制依赖的 1–3 行）/ retain（必须逐字保留的其余）** 三要素；拆不出三要素的卡换掉，不要勉强。

## 任务 2：逐张造真阴面并过全部判据
每张阴面：
1. 阴夹具 = 阳夹具的最小 delete-only diff：定点删 remove 的 1–3 行；同编译器同参编译，**断言必须在阴面上翻转失败**（artifact 计数/符号区间/run 键至少一个确定翻转；run stdout 翻转不是必须——533 证明旗舰卡就只翻汇编区间、stdout 零翻转，用 artifact 通道）。
2. 过 `tools/viso_diff.py` 全部判据：delete-only / 单 hunk / 删 1–3 行 / ≤40 token / 改动率 ≤2% / 删除点 100% 在 anchor 函数体 / remove 命中 / retain 逐字保留 / Itanium 符号同源 / 阴面 fixture stem == 阳主干名+nc 后缀 / negative_controls 块式写法 / **实测翻转**。
3. 形式阴面必须被机器拒（先自攻一遍）：冒名阴面（借别卡产物）、非单变量（连循环带守卫一起删）、注释走私、删错位置（探针翻了但目标断言没翻）、改 1 行语义天翻地覆但非 delete-only、多变量——逐一确认判据抓得住；抓不住的，先补 viso_diff 判据并配测试再继续（判据补强算本包范围，但改后要重钉、配正反例）。
4. 每张独立 commit + replay 翻转留证（贴阳面/阴面关键计数与 verdict）。

## 任务 3：留证与收工
- 收工 gate 63 条·block=0、poison 数随新增阴面载荷更新、replay 原有 confirm=56 结论不变（新增阴面是增量，不改既有 56 卡信任）、`--check` exit0、pytest fast 绿、受控目录零残留、ruff 启用族 0。
- 写 `_worklog_576.md`：19 张候选池侦察表、选中 3 张理由与三要素、每张阳/阴计数与翻转、自攻六类形式阴面的处置、哪些卡结构性做不出单变量运行级阴面（登记）、偏差表、**实际完成张数**。
- **做不完 3 张就停在已完成张数（1 张也交），判据一条不许降，不许为凑数收录不翻转/冒名/多变量阴面。**

## 硬纪律 / 不做
- 阴面宁少勿假；不跑全量 mutation、不做与阴面无关的重构；不 block 化、不自动 accept、不 push；
- 不做 M5/活性锚（归 575）、PoC（归 577）；阳夹具字节复制后产物 sha 可能仅差 `.file` 基名行——阴面**永不锚 sha**（533 已证），锚翻转性质。
