# 569 · 收口 mutation 自发现真洞 M3：区间锚定丢失（单点互斥判定）

承接 `12d484d`（568 性能批，监工已亲验 fast 35s / golden 复用 3.2s 与真编译 129s 三数一致）。解释器只用 `.venv\Scripts\python.exe`；一任务一 commit；不 push、不 golden accept。本批动 `gate_engine.py`（CORE_TOOLS），**存量零误伤是硬约束，两次撞过存量（541 block 0→38），必须照 541 已探明的精确形状做。**

## 背景（监工侦察已实锤）
- M3 算子把卡上 `contains_in/absent_in`（区间锚定断言）降级成 `contains/absent`（全文存在性），判别力掉一档却无人告警；543 后 M3 仍有逃逸（区间降级 / 去 -Werror 两类，当时仅 13 条可判）。
- 根因仍在：`gate_engine.py:1708 if kind in ("contains_in", "absent_in"):` —— 判别力检查只覆盖两种锚定 kind，非锚定 `contains/contains_any/absent_any` 被 `continue` 跳过。
- **注意**：558 B1 已落地一条 warn（"全文 kind 残留 symbol"降级指纹，存量 0 命中），EV-CONC-001 的 2 条 M3 已收口。**不许重复造规则**。

## 任务 0（先量清，再决定做不做）
1. 跑 `mutation_fuzz.py --operators M3`（按卡批模式，快），量出**当前**真实逃逸数与逐条定性，区分两个子情形：
   - (a) 区间锚定丢失（contains_in→contains / absent_in→absent）；
   - (b) 去 -Werror（编译 flag 被删，警告不再当错误）。
2. grep 1708 区域 + 读 558 B1 已落地的规则，确认 (a)/(b) 各还剩几条、558 已覆盖哪部分。
3. 若 T0 发现 M3 已被 558 全部收口（逃逸=0），**就不做任务 1，如实写 worklog"已饱和"**，只做任务 3。

## 任务 1（仅当 T0 确认还有逃逸）：单点互斥判定
严格照 `_worklog_541.md §1.3` 的形状，**这三条是用血换来的，逐条遵守**：
1. 对非锚定 kind（`contains`/`contains_any`/`absent_any`）+ 通用/恒真文本，出 **warn**；该 warn 必须与既有"通用符号⇒block"路径**互斥/合并成单点判定**——同一条目已被 block 就不要再出 warn（541 试验 2 因此打散了 2 个既有测试期望）。
2. **"空 / 纯中文 text ⇒ block"必须保持只对 `contains_in/absent_in` 可达**，绝不扩到新 kind——541 试验 1 一扩就 block 0→38。
3. 严重度从 **warn 起步**（558 已量降级指纹存量 0 命中，理论 warn 也 0 增量；若实测 warn 增量>0，逐条列出卡 id 交人，不许直接升 block）。
- (b) 去 -Werror 若是独立子情形，单独判：它属编译 flag 层而非断言 kind 层，若本批形状覆盖不了就**只登记、不硬做**，写进 worklog 交下一批。

## 任务 2：正反例毒样例 + 复跑验证
- 毒样例：M3 降级卡（contains_in→contains）应被新 warn 抓住；干净卡（正常 contains_in / 正常 contains）必须放行（clean_pass）。
- 复跑 `mutation_fuzz.py --operators M3`：逃逸数 T0→收口后逐值对比，贴数；
- 既有 gate 测试全绿（541 打散过 2 例，本批不许再打散）；
- 改了 `gate_engine.py` ⇒ 同 commit `tool_integrity.py --update` 重钉，`--check` exit 0。

## 任务 3（小修，568 遗留）：mutation_fuzz 退出自检
给 run_fuzz 加"退出即校验受控目录零差异"：跑完（含异常路径）自动 `git diff --quiet -- evidence/ atoms/`，有残留就 fail-loud 报出未还原的卡（EV-CONC-001 曾被 M4 变异打断残留）。配一例测试（污染副本→断言自检报红）。

---
## 收工验收（fresh）
- 任务 0 的 M3 改前逃逸数（贴实跑，不许抄 543 的旧数）；
- 任务 1 后：**gate 仍 block=0**（warn 增量逐条列出且存量卡 id 可解释）· M3 该收口子情形逃逸→0 · 既有 gate 测试全绿 · 毒样例正反例通过 · `--check` exit 0；
- poison 107/107（双指标 100%）· replay confirm=56 · pytest fast（现应 ~35s）/slow 全绿 · 受控目录零残留；
- **回退条件**：存量 block>0、或既有测试红、或 warn 增量无法逐条解释 ⇒ git 回退该规则、保留 T0/T3，如实写 worklog；
- 写 `_worklog_569.md`：T0 实测、(a)/(b) 分类、541 三条件如何遵守、偏差表。

## 本批不做
PoC#3/#4/#5（等 trae 564）、golden 多 worker fork 并行、ruff 908 项全量清（另开一批先钉 select）、命题级 signed_by。
