# G5 下一轮指令：第一批原子化 + Clang 注释 + 第二批 4 原子启动

> 2026-09-11 · 给执行 Agent（Writer/Gatekeeper 角色）· 监工验收通过，人审已授权。

---

## 一、当前状态（接地，不要假设）

- 第一批 8 颗 MEM 原子草稿在 `goldens/mem/`，误解 ID 修复已完成（新建 MIS-MEM-013/014/015/016，8 颗草稿引用全部同步）
- 监工独立验收：gate block=0 · 新建 MIS 条目质量合格 · 无残留错误引用
- 20 张证据卡 `evidence/mem/EV-MEM-001..020`，replay confirm=20/0
- 人审已授权签署（human:liaoranran），可进入原子化
- 本地工作树：6 个草稿 modified + 4 个 MIS 新建 + References/11-24 共 14 份未跟踪 + docs/G5_MEM_batch_quality_inspection.md 未跟踪
- origin/master 仍在 c2ac07b，本地 ahead 9（含第一批 8 原子的 9 个提交）

---

## 二、第一步：第一批原子化（按依赖顺序，必须遵守）

### 依赖链

```
批次 1（可并行）：RAII-001、VALUE-001、PERF-001、ALIGN-001
批次 2（依赖批次 1）：NEW-001（prereq RAII）、UNIQUE-001（prereq RAII）
批次 3（依赖批次 2）：SHARED-001（prereq UNIQUE）
批次 4（依赖批次 3）：WEAK-001（prereq SHARED）
```

### 每颗原子化操作

1. `git mv goldens/mem/ATOM-MEM-XXX-001_draft.md atoms/mem/ATOM-MEM-XXX-001.md`（保留历史）
2. frontmatter 修改：
   - `status: draft` → `verified`
   - 新增 `verified_by: human:liaoranran`
   - 新增 `verified_at: 2026-09-11`
   - rubric 自评：4/5 → **5/5（人审授予，2026-09-11）**，补三条锚定依据（参照 G4 三样板格式：统一解释有增量 / 量化到机器证据 / 过程本身有教学价值——按每颗实际内容写，不要套话）
3. 每原子化一颗后跑 `gate_engine.py --check`，确认 ATOM-REL-TARGET 不新增（按依赖顺序，目标应该已存在）
4. 全部 8 颗原子化后跑 `golden_lock.py sync`——EV-SERVES-EXIST 债（12 条）应自动清零，verified_atoms 4→12、atoms_total 4→12
5. 顺带：VALUE-001/PERF-001 原子化后，MOVE-002 的 ATOM-REL-TARGET 存量债（指向 VALUE/PERF）自动清零

### 原子化后立即验证

- `gate_engine.py --check` block=0
- `golden_lock.py check` 恶化 0、EV-SERVES-EXIST 清零确认
- `atom_evidence_replay.py --check` confirm=20/0

---

## 三、第二步：Clang 列注释补全（20 张证据卡）

### 背景

第一批 20 张证据卡的 `claim_boundary.compilers` 只有 GCC 15.3.0，缺 Clang 列标注。按 G2 永久边界（GCC + Clang 双编译器实测 + MSVC 以标准条文代替），mechanism 类原子可接受单编译器，但必须标注"Clang 列待 CI 回填"，与 B/C 样板做法一致。

### 操作

对 `evidence/mem/EV-MEM-001.md` 到 `EV-MEM-020.md` 每张卡：
- 在 `claim_boundary` 或 `actual` 字段旁补一行注释：`# Clang 列待 CI Cross-check 步回填（GCC 15.3.0 已实测）`
- 不要改 actual 数值、不要改 artifact_sha256、不要重跑夹具
- 纯注释添加，不影响 replay 校验

### 验证

- `atom_evidence_replay.py --check` 仍 confirm=20/0（注释不影响校验）
- 20 张卡均含 Clang 待回填标注

---

## 四、第三步：References 入库 + commit + push

### 入库文件

- `References/11_` 到 `References/24_` 共 14 份调研/指令文档（00-10 已在 c2ac07b 入库）
- `docs/G5_MEM_batch_quality_inspection.md`（监工质检报告）
- `misconceptions/MIS-MEM-013.md` 到 `016.md`（4 个新建误解条目）

### 提交要求

- 中文提交走 `git commit -F <UTF-8 文件>`（不用 -m "中文"，避免 GBK 乱码）
- 建议分 2-3 个提交：
  1. 第一批原子化（8 颗 atoms/mem/ + frontmatter 签署）
  2. Clang 注释 + MIS 条目 + 质检报告
  3. References/11-24 调研文档入库
- push 前跑 WSL 预检（`tools/ci_local_precheck.py`），全绿再推
- 推送后观察 CI，不带红进下一步

---

## 五、第四步：第二批 4 原子启动（第一批原子化+推送完成后）

### 第二批候选（4 个，与第一批互补）

| 原子 ID | 主题 | 类型 | 前置依赖 | 证据卡需求 |
|---|---|---|---|---|
| ATOM-MEM-VALUE-002 | 引用折叠与完美转发（T&& + std::forward） | mechanism | VALUE-001（已原子化） | 引用折叠编译期证明（static_assert）+ forward 保持值类别实测 |
| ATOM-MEM-RAII-002 | 拷贝控制五件套与 Rule of 0/3/5 | mechanism | RAII-001（已原子化） | 编译器自动生成条件实测 + =default/=delete 行为对比 |
| ATOM-MEM-ALLOC-001 | allocator 接口与 std::allocator 实现 | mechanism | 无前置 | allocate/deallocate + construct/destroy 分离实测 |
| ATOM-MEM-PERF-002 | 小对象优化（SSO / 小对象缓冲） | pitfall | 无前置 | std::string SSO 阈值实测（≤15 字节 0 分配）+ 汇编层确认 |

### 生产流程（每颗，与第一批一致）

1. **Writer**：写 `goldens/mem/ATOM-MEM-XXX-002_draft.md`（五件套：frontmatter + 正文 + 证据卡 + 夹具 + 误解引用）
2. **红队**：按 S1-S6 逐条攻击，抓硬编码期望/伪证据/术语错误
3. **升级**：红队问题修复后自评 4/5（不超 4）
4. **Gatekeeper**：replay + gate_engine + golden_lock 自证
5. **交监工质检** → 人审 → 原子化

### 硬性要求（与第一批一致）

- 每颗至少 2 张证据卡（mechanism 类 1 张主证 + 1 张证伪对照）
- 证据卡必须有 `command`（完整复现命令）、`artifact` + `artifact_sha256`、`actual.run_*`（机器输出）
- 工件与断言同代（改 .cpp 或改计数必须重生成换哈希）
- 产物写 build/、路径正斜杠
- 计数/计时类 -O0/-O2 双跑、计数器 volatile
- UB 类证据卡必须 WSL 过 sanitizer
- 误解引用必须查语义匹配（不只查存在性）——引用前先读目标 MIS 条目确认主题匹配
- 新建误解条目用 MIS-MEM-017 起编号（013-016 已被第一批占用）

### 第二批不做的（留到第三批）

- UNIQUE-002（自定义 deleter）、SHARED-002（make_shared）、LEAK-001（ASan）、PERF-003（内存池）——这 4 个放下一批，避免一次 8 个导致质量稀释

---

## 六、铁律（重申，违反即拦截）

1. **verified 唯 human:liaoranran 可签**——第一批已授权，第二批不得自签
2. **5 分唯人审授予**——第二批最高自评 4
3. **工件与断言同代**——改 .cpp 或改卡里计数必须重生成工件换 sha256
4. **产物写 build/**——不在仓库根留 .exe
5. **中文提交走 -F UTF-8 文件**——不用 -m "中文"
6. **查同名必须 rglob**——新建文件前递归查
7. **验证不带 PATH 前置**——用 toolchain.resolve_gpp() 钉编译器
8. **硬编码期望会被 S3 拦**——expected.run 是描述，actual.run 是机器输出
9. **误解引用查语义匹配**——不只查存在性，引用前读目标 MIS 条目
10. **按依赖顺序原子化**——SHARED 依赖 UNIQUE、WEAK 依赖 SHARED，不得跳序

---

## 七、交付物清单

- [ ] 第一批 8 颗原子化（atoms/mem/，status verified + verified_by + verified_at）
- [ ] golden_lock sync（EV-SERVES-EXIST 清零、verified_atoms 4→12）
- [ ] 20 张证据卡 Clang 列注释补全
- [ ] References/11-24 + 质检报告 + MIS-013/014/015/016 入库
- [ ] commit + push（WSL 预检全绿后）
- [ ] CI 观察（不带红进第二批）
- [ ] 第二批 4 原子草稿（VALUE-002 / RAII-002 / ALLOC-001 / PERF-002）+ 证据卡 + 门禁自证
- [ ] 第二批交监工质检（不自行原子化、不自行置 verified）

**完成后报告：原子化提交号、golden_lock 前后对比、CI 结果、第二批 4 颗的门禁自证数据。**
