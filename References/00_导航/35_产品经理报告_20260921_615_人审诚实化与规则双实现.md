# 35 · 产品经理报告（2026-09-21，615 后）：人审诚实化与规则双实现（止血优先）

> 一句话：**615 不做新能力，做"止血"——把"名实之差"堵上：388 条人审的真相被诚实重述、一把尺子被独立第二实现验出隐性知识、关键尺子进了完整性保护集**。

---

## 一、本批治的是什么"名实之差"

### 1. 人审：388 条 ≠ 388 次独立判断
- 真相（`_arch_v19/05`）：388 条 = **1 名审阅者 × 5 个理由模板**，**逐条独立判断 0**；
  其中 **194 条是镜像边**（命题↔MIS 对称推断，一次判断算两条，不提供独立信息）。
- 615 把每条打标（`data/human_review_honesty_labels.jsonl`）：`batch_authorization 388 / item_by_item 0 / mirror 194`。
- 并出 **30 条真逐条复核决策清单**（`data/human_review_item_by_item_30_615.md`）：17 条 modify(OUT7) + 13 条
  approve（rubber-stamp 风险），每条含"判断要点 + 建议结论 + 须人回答的是非题"。
- **意义**：W2 论证图的人类确认强度从"388 条计数资产"诚实降级为"信息资产"，并把"如何恢复独立票"的路径给出来。

### 2. 一把尺子：独立第二实现验出"隐性知识"
- 对 `EV-MATRIX-UNBACKED` 做**异质第二实现**（不复用官方代码）：一致率 **68.4%（13/19）**，6 张卡分歧。
- 根因：官方**隐性预处理**（除剥 `actual` 还剥 `artifact_sha256` 行，2026-09-12 P12 毒样例逼出）——
  64 位 sha 的纯数字片段被"CI run 号"锚误收 ⇒ 毒卡放行。
- 615 出**规则定义补全提案**（把这行预处理显式写进文档）；补上后一致率 **100%**。
- **意义**：这是「规则正确性知识部分埋在预处理里」的一次可复算实证（DDC/差分测试思想的微缩）。

### 3. 尺子入根：判决逻辑进完整性保护
- `tools/tool_integrity.py` 新增 `# ruler` 节：保护集 **12 → 22**（+10：mutation_fuzz/golden_lock/
  replay_invariants/d5_*/attack_edge_generator/bkt_solver/learner_mastery_update/tool_integrity 自身）。

### 4. warn 治理：防"温水煮青蛙"
- `warn_governance`（不增锁：新 warn 不自动采纳 / 观察期 3 批 / 采纳须人审 / 到期 10 批重评估）。
- `exemption_expiry`（27 条 legacy 豁免到期制：到期批次 = 创建批次 + 10）。
- `goodhart_monitor`：**危险分 80.1/100**（block=0 + warn 31→186 增长 + reject=0 驱动）——与 `_arch_v19/08` 判断一致。

### 5. 学习门：仍未开（诚实）
- `learner_transition_detector` 判定：门 **closed**（triggered 0 / ready 5 / not_ready 22；真实事件 0/50）。

---

## 二、关键数字（615 后）

| 指标 | 值 |
|---|---|
| 受保护文件（tool_integrity） | **22**（core5+test_config2+supply_chain5+ruler10） |
| 人审逐条独立判断 | **0 / 388**（诚实重述） |
| 镜像边 | **194 / 388** |
| 尺子双实现一致率 | 68.4% → （提案后）**100%** |
| Goodhart 危险分 | **80.1/100** |
| 学习者门状态 | **closed**（真实事件 0/50） |
| 27 条 legacy 豁免 | 全 **active**（未到期） |

---

## 三、交人项（615 新增）
1. **30 条逐条复核**是否执行（需人审；清单已就绪）。
2. `EV-MATRIX` 规则定义补全提案**是否采纳**（写入规则定义文档）。
3. **warn 不增锁机制**是否启用（改 `golden_lock` 流程需人审）。
4. **27 条豁免到期后**续期/修复/删除（需人审）。
5. 工具合并 **3 族**、data 整理 **方案** 是否执行（616 人审授权）。
6. CI 四 job 实跑确认（614 未 push）。

## 四、边界与诚实声明
- 本批**未跑任何监工门禁**（`gate/poison/replay/tool_integrity --check`）；数字取自 standing baseline 与历史记录。
- `EV-MATRIX` 提案**只是纸面**：未改 `evidence/`、未改 `gate_engine.py`。
- 学习者闭环**真实事件仍为 0**，门未开；615 未采集真实学习行为。
- 单用户结构上限不变：人审独立性、信任根闭环、自进化 L3+ 仍不可达。

---

*数据来源：615 批次各任务实测 + `data/615_baseline.md`；所有数字可复算。*
