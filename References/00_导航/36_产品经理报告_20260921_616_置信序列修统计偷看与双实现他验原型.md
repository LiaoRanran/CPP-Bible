# 36 · 产品经理报告（2026-09-21，616 后）：置信序列修统计偷看 + 双实现锁 + 他验原型 + 交人项

> 一句话：**616 不做新能力，做"方法论止血"——把"连续偷看 CP 上界"的口径偷换用置信序列（e-process）纠正为 anytime 0.9062%（旧 0.3370% 的 2.69×，虚报 13.80%→0.00%）；EV-MATRIX 规则定义独立补全后双实现锁 100%；warn 治理/Goodhart 入度量；他验三件套出架构+原型+路线图；交人项台账（615 遗留 7 + 616 新增 3）。**

---

## 一、本批治的是什么"名实之差"

### 1. 统计偷看：CP 上界在连续查看下方法论无效（P0）
- 历史：v1→v7 每批在**同一批累积数据**上重看上界（≈9 次公开复算）= 连续偷看，固定样本 CP 的 95% 只在"只看一次"时成立。
- **A1** `confidence_sequence.py`（Beta-混合 e-process，**纯标准库**）：anytime 有效，理论与经验虚报 ≤5%，不依赖"看一次"。
- **A2 量化**（真实 p=0.01、每 20 样本偷看）：固定样本 CP 名义 5% → 经验虚报 **13.80%**（实际覆盖率 86.2%）；置信序列 **0.00%**；anytime 上界 **0.9062%**（= 单侧 CP 的 **2.69×**，v7）；v1→v7 相对代价 1.11×→2.69×（逃逸率越低代价越大）。
- 样本代价：0 逃逸下达到 0.5% 上界，CP 需 n=**598**，anytime CS 需 n=**2127**（3.56×）。
- **A3** `metrics_collector` 加 `cs_*`（**保留** `cp_*`）+ `peeking_correction`，实现双口径看板。

### 2. 一把尺子：规则定义独立补全 → 双实现锁 100%
- 615 B1/B2：natural 第二实现适用 **19** 卡 / 一致 **68.4%**（13/19）；根因是官方**隐性预处理**（剥 `actual:` 段 + 剥 `artifact_sha256:` 行，64 位 sha 数字片段被「CI run 号」锚误收）。
- **B1** 出**规则定义 v2 独立文档**（`data/ev_matrix_rule_definition_v2.md`），把隐性预处理显式写进定义。
- **B2** `ev_matrix_unbacked_v2` 补全语义，一致率 **100%**。
- **B3** `ev_matrix_dual_impl_lock.py`：import `gate_engine` 单规则函数 vs 独立实现 = **19/19 = 100%**，基线 `data/ev_matrix_dual_impl_baseline_616.json`（pass，阈值 0.95，official_hits 16）。

### 3. warn 治理 / Goodhart 入度量
- **C1** `warn_governance` 工作流（五桶 → 观察期 → 采纳建议 → 到期提醒）。
- **C2** `exemption_expiry` 27 条 legacy 处置清单（到期 = 创建批次 + 10）。
- **C3** Goodhart 入 `metrics_collector`（`collect_curves()["goodhart"]`）。

### 4. 他验三件套：从无到有的架构 + 原型
- **D1** 架构（`data/independent_verification_architecture_616.md`）：针对"生成者=判断者=同一主体"的结构性独立票缺失。
- **D2** 原型（`independent_verifier_prototype.py`）：HMAC VSA 凭证 + 5 示例。
- **D3** 路线图（5 阶段；阶段 2 需 10 规则 + Merkle 透明日志授权，签名升级需密钥托管）。

### 5. 交人项台账（E1）
- 615 遗留 **7** + 616 新增 **3**（`data/human_decision_tracking_616.md`），只跟踪不裁决。

---

## 二、关键数字（616 后）

| 指标 | 值 |
|---|---|
| 置信序列 anytime 上界（v7） | **0.9062%**（CP 0.3370% 的 2.69×） |
| 偷看虚报（固定样本 CP vs 置信序列） | 13.80% → **0.00%** |
| CS/CP 相对代价（v1→v7） | 1.11× → **2.69×** |
| 0.5% 上界样本代价（CP vs CS） | 598 vs **2127**（3.56×） |
| EV-MATRIX 双实现锁 | **19/19 = 100%**（阈值 0.95） |
| 尺子双实现（615→616） | 68.4% → **100%** |
| 受保护文件（tool_integrity） | **22**（core5+test_config2+supply_chain5+ruler10） |
| Goodhart 危险分 | **80.1/100** |
| 学习者门状态 | **closed**（真实事件 0/50） |
| 交人项（615 遗留 + 616 新增） | **7 + 3 = 10** |

---

## 三、交人项（616 新增）

1. **置信序列是否正式启用**（cs_* 取代 cp_* 作对外口径）——双口径已入 metrics，哪个对外须人定（P0）。
2. **EV-MATRIX 规则定义补全**是否合并进 `evidence/`（615 遗留，616 已出独立文档 `data/ev_matrix_rule_definition_v2.md`）。
3. **他验架构是否进入实施**（阶段 2 授权 + 密钥托管）。
4. **warn 不增锁机制**是否启用（改 `golden_lock` 流程）。
5. **27 条 legacy 豁免到期处置**（到期 = 创建批次 + 10）。
6. **保形预测 / 弃权三态**是否做（留 617+）。

## 四、边界与诚实声明

- 本批**未跑任何监工门禁**（`gate/poison/replay/tool_integrity --check`）；数字取自 standing baseline 与历史记录（`data/616_baseline.md`），可复算。
- **品牌级口径纠正**：此前"1/1406，C-P95 ≤0.40%"在**连续查看**场景下方法论无效；修正后 anytime 上界 **0.9062%**。这不是结果变差，而是口径从"看一次"纠正为"随时看"的诚实代价（非数据造假，是口径误用）。里程碑终点的一次性声明仍可用 CP（0.3370%），但看板不得继续引用。
- EV-MATRIX 规则定义 v2 只是**独立文档**，尚未合并进 `evidence/` 与 `gate_engine.py`；双实现锁基线已落地（pass），属"观测"性质，未自动采纳。
- 他验为**架构 + 原型**，未进入生产实施。
- 单用户结构上限不变：人审独立性、信任根闭环、自进化 L3+ 仍不可达。

---

*数据来源：616 批次各任务实测 + `data/616_baseline.md`；所有数字可复算。*
