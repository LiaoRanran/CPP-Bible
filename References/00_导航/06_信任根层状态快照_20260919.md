# 信任根层状态快照（2026-09-19）

> 自动生成，整合 Merkle / governance / supply_chain / overturned / 585 红队封堵数据。只读快照。

## 1 · Merkle 完整性（601 落地）

| 指标 | 值 |
|---|---|
| 跟踪目录 | 5 个 |
| atoms/ | 29 个文件 |
| evidence/ | 57 个文件 |
| Examples/ | 1541 个文件 |
| Book/ | 183 个文件 |
| mutation_baselines/ | 7 个（full_baseline_v1-v7.json） |
| Merkle 根 | data/supply_chain/merkle_roots.json |
| --check 结果 | OK（目录级 Merkle 根与当前内容一致，警告 0） |
| 包含证明 | ✓（inclusion proof） |
| 一致性证明 | ✓（consistency proof） |

**探针实证**（597/600）：纯标准库 Merkle 树，chain/tamper/inclusion/consistency 全部通过，树高 5-7。

## 2 · 治理文档防护（governance_doc_guard.py，591 落地，607 扩展）

| 指标 | 值 |
|---|---|
| manifest 跟踪文件 | 505 个（607 扩展后，从 344→505） |
| 扫描面 | 根级 .md + _arch_*/** + _auto/inbox + References/** |
| 弱化指令扫描 | high=55 / medium=186 / low=38 |
| self_hash | ✓（manifest 自校验，防篡改） |
| verify 结果 | ✓（manifest 一致，self_hash 已校验） |
| auto-update | ✓（内容变更时机械重录，不构成语义认可） |

**high=55 说明**：均为历史投喂词里的描述性讨论（"降级为 warn""跳过门禁""豁免毒样例"被当作该做法本身在讨论），需人审逐条确认是"描述"还是"真实攻击指令"。工具只做机械重录，不自行判定语义。

**607 扩展**：扫描面从根级 .md 扩展到 _arch_*/**（156 个调研文档）+ _auto/inbox（4 个提示词）+ 根级 PM_*（1 个），净增 161 个文件。

## 3 · 供应链溯源（supply_chain.py，600 调研，601 部分落地）

| 指标 | 值 |
|---|---|
| in-toto layout | 已定义（600 调研 schema） |
| link 记录 | 0 条（系统绝不自动记录步骤，需显式 link create） |
| chain verify | ✓（link 逐条一致 · 顺序/依赖成立 · inspection 全绿） |
| Merkle 根集成 | ✓（tool_integrity --update 重建 merkle_roots.json） |
| SLSA 级别评估 | 当前 ≈ L0（单用户不可达 L2+） |
| OpenTimestamps | 未落地（阶段 3，需联网+外部 CLI，只证"存在时间"不证"身份"） |

**600 调研结论**：
- 性价比最高 = 阶段 1 Merkle（已落地，601）
- 结构性上限 = 阶段 3 OpenTimestamps（只证存在时间不证身份）
- 最意外 = 585 攻击3（人签自证）已被 592 的 git 作者绑定部分封堵（旧调研过时）
- 真盲点 = 攻击1 的"规则/策略定义文件不在 tool_integrity 覆盖内"（与 governance manifest 缺口同源，已补）

## 4 · 工具完整性（tool_integrity.py）

| 指标 | 值 |
|---|---|
| CORE_TOOLS 钉住 | 5 个（gate_engine / atom_evidence_replay / poison_drill / toolchain / cppbible） |
| test_config 钉住 | 2 个（TEST_CONFIG_TOOLS） |
| supply_chain 钉住 | 5 个文件 |
| --check 结果 | OK（core 5 + test_config 2 + supply_chain 5 全一致） |
| --check-test-config | ✓（conftest 启动自检） |
| --check-merkle | ✓（Merkle 根校验） |
| --update | 苦力唯一允许跑的 integrity 命令（改 CORE 后同 commit 重钉） |

**conftest 钩子**（591 落地）：pytest 启动时自动 --check-test-config，防得住"只改内容不改哈希"，防不住"改钩子+重签基准"（纵深防御，非架构级）。

## 5 · 推翻事件通道（overturned_events.py，592 落地）

| 指标 | 值 |
|---|---|
| 通道文件 | data/overturned_events.jsonl（0 字节，入库） |
| 事件数 | 0 条（等待首次真实推翻） |
| 写入面 | overturned_events.append() / metrics_collector --log-overturned |
| git 作者绑定 | ✓（human 须过 git 作者绑定，fail-closed） |
| 系统自动产生 | 绝不（第一原则） |
| 通道初始化 | ✓（overturned_channel_initialized=True） |

**592 修复**：metrics_collector 自报 baseline_version=v7/frozen_at_commit=d36d5c8（从基线文件自身读），新增 *_raw 全精度字段，survival 补 M5/M2/M6，单时点文案点明"尺子变更史 v1→v7"。

## 6 · 585 红队三攻击封堵状态

| 攻击 | 描述 | 封堵状态 | 残余风险 |
|---|---|---|---|
| 攻击1 | 规则/证据文件不在 tool_integrity 覆盖内，可篡改后重签 | ✓ Merkle 5 目录 + governance manifest + supply_chain | 进程内运行时篡改（需运行时规则指纹，W 档） |
| 攻击2 | 豁免台账可篡改（删豁免→覆盖率虚高） | ✓ poison 豁免台账钉死 + 27 legacy 透明 + 新豁免 fail-closed | 无 |
| 攻击3 | 人签自证（git 作者可自设，jsonl 可直改） | ✓ 592 git 作者绑定 + 人审步骤显式授权 | 单用户结构性上限（git 作者=自设，无第三方时间戳） |

**601 回归锁**（tests/test_585_attack_regression_601.py，12 passed）：
- 攻击1 四层纵深（core/supply_chain/Merkle/manifest-self_hash）+ 多层同时检出
- 攻击2 豁免台账钉死
- 攻击3 git 作者绑定 + 人审步骤显式授权 + 作者自设残余风险显式证伪

## 7 · 信任根层成熟度评估

| 维度 | 状态 | 评分 |
|---|---|---|
| Merkle 完整性 | 5 目录，inclusion/consistency 证明，--check OK | 9/10 |
| 治理文档防护 | 505 文件 manifest，self_hash，弱化扫描，auto-update | 8/10 |
| 供应链溯源 | Merkle 已落地，in-toto schema 已定义，link 0 条 | 6/10 |
| 工具完整性 | CORE 5+test 2+supply 5 全钉，conftest 启动自检 | 9/10 |
| 推翻通道 | 通道已建，git 作者绑定，0 事件，fail-closed | 7/10 |
| 585 红队封堵 | 三攻击全封堵，12 回归锁，残余风险显式登记 | 8/10 |
| 单用户结构性上限 | git 作者可自设，无第三方时间戳，L2+ 不可达 | 5/10 |

**信任根层综合评分：8.0/10**（工程层已成熟，单用户身份认证是结构性上限）

## 8 · 下一步优先级

1. **in-toto link 记录**：首次显式 link create（系统绝不自动，需人触发）
2. **OpenTimestamps**：阶段 3，需联网+外部 CLI，只证存在时间（交人裁决是否值得）
3. **governance high=55 人审**：逐条确认是"描述"还是"真实攻击指令"（人审权力）
4. **manifest 自身加固**：是否入 git 签名（当前与 .tool_checksums 同类信任边界，交人）
5. **conftest 钩子架构升级**：独立 pytest 启动脚本（防"改钩子+重签基准"，交人）
6. **首次真实推翻**：等待 mutation 端到端验证产出第一个 overturned event

---

*数据源：tools/tool_integrity.py --check / tools/governance_doc_guard.py verify / tools/supply_chain.py chain verify / data/governance_docs_manifest.json / data/supply_chain/merkle_roots.json / data/overturned_events.jsonl / tests/test_585_attack_regression_601.py*
