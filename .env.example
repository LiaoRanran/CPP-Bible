# 环境变量样例 —— 复制为 .env 并按需修改
# 说明：工具链路径的「唯一事实源」是 toolchain.toml；
#       这些变量仅用于覆盖默认行为或注入 CI 参数，非必需。

# --- 工具链覆盖（一般不需要；优先改 toolchain.toml）---
# CPPBIBLE_GCC=
# CPPBIBLE_PYTHON=
# CPPBIBLE_CLANG=

# --- 发布 / 构建 ---
# CPPBIBLE_PAGES_URL=https://liaoranran.github.io/CPP-Bible/
# CPPBIBLE_RELEASE_STAGE=dev        # dev | ci | release

# --- CI 专用（勿在本地提交）---
# GITHUB_TOKEN=
